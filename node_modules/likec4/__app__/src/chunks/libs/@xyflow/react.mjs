import { i as e } from "../../rolldown-runtime.mjs";
import { a as t, c as n, i as r, l as i, o as a, r as o, s, u as c } from "../d3.mjs";
import l, { createContext as u, forwardRef as d, memo as f, useCallback as p, useContext as m, useEffect as h, useLayoutEffect as g, useMemo as _, useRef as v, useState as y } from "react";
import { Fragment as b, jsx as x, jsxs as S } from "react/jsx-runtime";
import C from "use-sync-external-store/shim/with-selector";
import { createPortal as w } from "react-dom";
//#region ../../node_modules/.pnpm/classcat@5.0.5/node_modules/classcat/index.js
function cc(e) {
	if (typeof e == "string" || typeof e == "number") return "" + e;
	let t = "";
	if (Array.isArray(e)) for (let n = 0, r; n < e.length; n++) (r = cc(e[n])) !== "" && (t += (t && " ") + r);
	else for (let n in e) e[n] && (t += (t && " ") + n);
	return t;
}
//#endregion
//#region ../../node_modules/.pnpm/@xyflow+system@0.0.82/node_modules/@xyflow/system/dist/esm/index.js
var T = {
	error001: (e = "react") => `Seems like you have not used ${e === "svelte" ? "SvelteFlowProvider" : "ReactFlowProvider"} as an ancestor. Help: https://${e}flow.dev/error#001`,
	error002: () => "It looks like you've created a new nodeTypes or edgeTypes object. If this wasn't on purpose please define the nodeTypes/edgeTypes outside of the component or memoize them.",
	error003: (e) => `Node type "${e}" not found. Using fallback type "default".`,
	error004: () => "The parent container needs a width and a height to render the graph.",
	error005: () => "Only child nodes can use a parent extent.",
	error006: () => "Can't create edge. An edge needs a source and a target.",
	error007: (e) => `The old edge with id=${e} does not exist.`,
	error009: (e) => `Marker type "${e}" doesn't exist.`,
	error008: (e, { id: t, sourceHandle: n, targetHandle: r }) => `Couldn't create edge for ${e} handle id: "${e === "source" ? n : r}", edge id: ${t}.`,
	error010: () => "Handle: No node id found. Make sure to only use a Handle inside a custom Node.",
	error011: (e) => `Edge type "${e}" not found. Using fallback type "default".`,
	error012: (e) => `Node with id "${e}" does not exist, it may have been removed. This can happen when a node is deleted before the "onNodeClick" handler is called.`,
	error013: (e = "react") => `It seems that you haven't loaded the styles. Please import '@xyflow/${e}/dist/style.css' or base.css to make sure everything is working properly.`,
	error014: () => "useNodeConnections: No node ID found. Call useNodeConnections inside a custom Node or provide a node ID.",
	error015: () => "It seems that you are trying to drag a node that is not initialized. Please use onNodesChange as explained in the docs.",
	error016: (e) => `Edge with id "${e}" does not exist, it may have been removed. This can happen when an edge is deleted before the "onEdgeClick" handler is called.`
}, E = [[-Infinity, -Infinity], [Infinity, Infinity]], D = [
	"Enter",
	" ",
	"Escape"
], O = {
	"node.a11yDescription.default": "Press enter or space to select a node. Press delete to remove it and escape to cancel.",
	"node.a11yDescription.keyboardDisabled": "Press enter or space to select a node. You can then use the arrow keys to move the node around. Press delete to remove it and escape to cancel.",
	"node.a11yDescription.ariaLiveMessage": ({ direction: e, x: t, y: n }) => `Moved selected node ${e}. New position, x: ${t}, y: ${n}`,
	"edge.a11yDescription.default": "Press enter or space to select an edge. You can then press delete to remove it or escape to cancel.",
	"controls.ariaLabel": "Control Panel",
	"controls.zoomIn.ariaLabel": "Zoom In",
	"controls.zoomOut.ariaLabel": "Zoom Out",
	"controls.fitView.ariaLabel": "Fit View",
	"controls.interactive.ariaLabel": "Toggle Interactivity",
	"minimap.ariaLabel": "Mini Map",
	"handle.ariaLabel": "Handle"
}, k;
(function(e) {
	e.Strict = "strict", e.Loose = "loose";
})(k ||= {});
var A;
(function(e) {
	e.Free = "free", e.Vertical = "vertical", e.Horizontal = "horizontal";
})(A ||= {});
var j;
(function(e) {
	e.Partial = "partial", e.Full = "full";
})(j ||= {});
var M = {
	inProgress: !1,
	isValid: null,
	from: null,
	fromHandle: null,
	fromPosition: null,
	fromNode: null,
	to: null,
	toHandle: null,
	toPosition: null,
	toNode: null,
	pointer: null
}, N;
(function(e) {
	e.Bezier = "default", e.Straight = "straight", e.Step = "step", e.SmoothStep = "smoothstep", e.SimpleBezier = "simplebezier";
})(N ||= {});
var P;
(function(e) {
	e.Arrow = "arrow", e.ArrowClosed = "arrowclosed";
})(P ||= {});
var F;
(function(e) {
	e.Left = "left", e.Top = "top", e.Right = "right", e.Bottom = "bottom";
})(F ||= {});
var I = {
	[F.Left]: F.Right,
	[F.Right]: F.Left,
	[F.Top]: F.Bottom,
	[F.Bottom]: F.Top
}, isEdgeBase = (e) => !!e && typeof e == "object" && "id" in e && "source" in e && "target" in e, isNodeBase = (e) => !!e && typeof e == "object" && "id" in e && "position" in e && !("source" in e) && !("target" in e), isInternalNodeBase = (e) => !!e && typeof e == "object" && "id" in e && "internals" in e && !("source" in e) && !("target" in e), getNodePositionWithOrigin = (e, t = [0, 0]) => {
	let { width: n, height: r } = getNodeDimensions(e), i = e.origin ?? t, a = n * i[0], o = r * i[1];
	return {
		x: e.position.x - a,
		y: e.position.y - o
	};
}, getNodesBounds = (e, t = { nodeOrigin: [0, 0] }) => {
	if (e.length === 0) return {
		x: 0,
		y: 0,
		width: 0,
		height: 0
	};
	let n = !1, r = e.reduce((e, r) => {
		let i = typeof r == "string", a = !t.nodeLookup && !i ? r : void 0;
		return t.nodeLookup && (a = i ? t.nodeLookup.get(r) : isInternalNodeBase(r) ? r : t.nodeLookup.get(r.id)), a ? (n = !0, getBoundsOfBoxes(e, nodeToBox(a, t.nodeOrigin))) : e;
	}, {
		x: Infinity,
		y: Infinity,
		x2: -Infinity,
		y2: -Infinity
	});
	return n ? boxToRect(r) : {
		x: 0,
		y: 0,
		width: 0,
		height: 0
	};
}, getInternalNodesBounds = (e, t = {}) => {
	let n = {
		x: Infinity,
		y: Infinity,
		x2: -Infinity,
		y2: -Infinity
	}, r = !1;
	return e.forEach((e) => {
		(t.filter === void 0 || t.filter(e)) && (n = getBoundsOfBoxes(n, nodeToBox(e)), r = !0);
	}), r ? boxToRect(n) : {
		x: 0,
		y: 0,
		width: 0,
		height: 0
	};
}, getNodesInside = (e, t, [n, r, i] = [
	0,
	0,
	1
], a = !1, o = !1) => {
	let s = (t.x - n) / i, c = (t.y - r) / i, l = t.width / i, u = t.height / i, d = [];
	for (let t of e.values()) {
		let { measured: e, selectable: n = !0, hidden: r = !1 } = t;
		if (o && !n || r) continue;
		let i = e.width ?? t.width ?? t.initialWidth ?? 0, f = e.height ?? t.height ?? t.initialHeight ?? 0, { x: p, y: m } = t.internals.positionAbsolute, h = getRectsOverlappingArea(s, c, l, u, p, m, i, f), g = i * f, _ = a && h > 0;
		(!t.internals.handleBounds || _ || h >= g || t.dragging) && d.push(t);
	}
	return d;
}, getConnectedEdges = (e, t) => {
	let n = /* @__PURE__ */ new Set();
	return e.forEach((e) => {
		n.add(e.id);
	}), t.filter((e) => n.has(e.source) || n.has(e.target));
};
function getFitViewNodes(e, t) {
	let n = /* @__PURE__ */ new Map(), r = t?.nodes ? new Set(t.nodes.map((e) => e.id)) : null;
	return e.forEach((e) => {
		let i;
		if (t?.includeHiddenNodes) {
			let { width: t, height: n } = getNodeDimensions(e);
			i = t > 0 && n > 0;
		} else i = !!(e.measured.width && e.measured.height && !e.hidden);
		i && (!r || r.has(e.id)) && n.set(e.id, e);
	}), n;
}
async function fitViewport({ nodes: e, width: t, height: n, panZoom: r, minZoom: i, maxZoom: a }, o) {
	if (e.size === 0) return !0;
	let s = getViewportForBounds(getInternalNodesBounds(getFitViewNodes(e, o)), t, n, o?.minZoom ?? i, o?.maxZoom ?? a, o?.padding ?? .1);
	return await r.setViewport(s, {
		duration: o?.duration,
		ease: o?.ease,
		interpolate: o?.interpolate
	}), !0;
}
function calculateNodePosition({ nodeId: e, nextPosition: t, nodeLookup: n, nodeOrigin: r = [0, 0], nodeExtent: i, onError: a }) {
	let o = n.get(e), s = o.parentId ? n.get(o.parentId) : void 0, { x: c, y: l } = s ? s.internals.positionAbsolute : {
		x: 0,
		y: 0
	}, u = o.origin ?? r, d = o.extent || i;
	if (o.extent === "parent" && !o.expandParent) {
		if (!s) a?.("005", T.error005());
		else {
			let { width: e, height: t } = getNodeDimensions(s);
			e && t && (d = [[c, l], [c + e, l + t]]);
		}
	} else s && isCoordinateExtent(o.extent) && (d = [[o.extent[0][0] + c, o.extent[0][1] + l], [o.extent[1][0] + c, o.extent[1][1] + l]]);
	let f = isCoordinateExtent(d) ? clampPosition(t, d, o.measured) : t;
	return (o.measured.width === void 0 || o.measured.height === void 0) && a?.("015", T.error015()), {
		position: {
			x: f.x - c + (o.measured.width ?? 0) * u[0],
			y: f.y - l + (o.measured.height ?? 0) * u[1]
		},
		positionAbsolute: f
	};
}
async function getElementsToRemove({ nodesToRemove: e = [], edgesToRemove: t = [], nodes: n, edges: r, onBeforeDelete: i }) {
	let a = new Set(e.map((e) => e.id)), o = [];
	for (let e of n) {
		if (e.deletable === !1) continue;
		let t = a.has(e.id), n = !t && e.parentId && o.find((t) => t.id === e.parentId);
		(t || n) && o.push(e);
	}
	let s = new Set(t.map((e) => e.id)), c = r.filter((e) => e.deletable !== !1), l = getConnectedEdges(o, c);
	for (let e of c) s.has(e.id) && !l.find((t) => t.id === e.id) && l.push(e);
	if (!i) return {
		edges: l,
		nodes: o
	};
	let u = await i({
		nodes: o,
		edges: l
	});
	return typeof u == "boolean" ? u ? {
		edges: l,
		nodes: o
	} : {
		edges: [],
		nodes: []
	} : u;
}
var clamp = (e, t = 0, n = 1) => Math.min(Math.max(e, t), n), clampPosition = (e = {
	x: 0,
	y: 0
}, t, n) => ({
	x: clamp(e.x, t[0][0], t[1][0] - (n?.width ?? 0)),
	y: clamp(e.y, t[0][1], t[1][1] - (n?.height ?? 0))
});
function clampPositionToParent(e, t, n) {
	let { width: r, height: i } = getNodeDimensions(n), { x: a, y: o } = n.internals.positionAbsolute;
	return clampPosition(e, [[a, o], [a + r, o + i]], t);
}
var calcAutoPanVelocity = (e, t, n) => e < t ? clamp(Math.abs(e - t), 1, t) / t : e > n ? -clamp(Math.abs(e - n), 1, t) / t : 0, calcAutoPan = (e, t, n = 15, r = 40) => [calcAutoPanVelocity(e.x, r, t.width - r) * n, calcAutoPanVelocity(e.y, r, t.height - r) * n], getBoundsOfBoxes = (e, t) => ({
	x: Math.min(e.x, t.x),
	y: Math.min(e.y, t.y),
	x2: Math.max(e.x2, t.x2),
	y2: Math.max(e.y2, t.y2)
}), rectToBox = ({ x: e, y: t, width: n, height: r }) => ({
	x: e,
	y: t,
	x2: e + n,
	y2: t + r
}), boxToRect = ({ x: e, y: t, x2: n, y2: r }) => ({
	x: e,
	y: t,
	width: n - e,
	height: r - t
}), nodeToRect = (e, t = [0, 0]) => {
	let { x: n, y: r } = isInternalNodeBase(e) ? e.internals.positionAbsolute : getNodePositionWithOrigin(e, t);
	return {
		x: n,
		y: r,
		width: e.measured?.width ?? e.width ?? e.initialWidth ?? 0,
		height: e.measured?.height ?? e.height ?? e.initialHeight ?? 0
	};
}, nodeToBox = (e, t = [0, 0]) => {
	let { x: n, y: r } = isInternalNodeBase(e) ? e.internals.positionAbsolute : getNodePositionWithOrigin(e, t);
	return {
		x: n,
		y: r,
		x2: n + (e.measured?.width ?? e.width ?? e.initialWidth ?? 0),
		y2: r + (e.measured?.height ?? e.height ?? e.initialHeight ?? 0)
	};
}, getBoundsOfRects = (e, t) => boxToRect(getBoundsOfBoxes(rectToBox(e), rectToBox(t))), getRectsOverlappingArea = (e, t, n, r, i, a, o, s) => {
	let c = Math.max(0, Math.min(e + n, i + o) - Math.max(e, i)), l = Math.max(0, Math.min(t + r, a + s) - Math.max(t, a));
	return Math.ceil(c * l);
}, getOverlappingArea = (e, t) => getRectsOverlappingArea(e.x, e.y, e.width, e.height, t.x, t.y, t.width, t.height), isRectObject = (e) => isNumeric(e.width) && isNumeric(e.height) && isNumeric(e.x) && isNumeric(e.y), isNumeric = (e) => !isNaN(e) && isFinite(e), createDevWarn = (e, t) => (e, t) => {}, snapPosition = (e, t = [1, 1]) => ({
	x: t[0] * Math.round(e.x / t[0]),
	y: t[1] * Math.round(e.y / t[1])
}), pointToRendererPoint = ({ x: e, y: t }, [n, r, i], a = !1, o = [1, 1]) => {
	let s = {
		x: (e - n) / i,
		y: (t - r) / i
	};
	return a ? snapPosition(s, o) : s;
}, rendererPointToPoint = ({ x: e, y: t }, [n, r, i]) => ({
	x: e * i + n,
	y: t * i + r
});
function parsePadding(e, t) {
	if (typeof e == "number") return Math.floor((t - t / (1 + e)) * .5);
	if (typeof e == "string" && e.endsWith("px")) {
		let t = parseFloat(e);
		if (!Number.isNaN(t)) return Math.floor(t);
	}
	if (typeof e == "string" && e.endsWith("%")) {
		let n = parseFloat(e);
		if (!Number.isNaN(n)) return Math.floor(t * n * .01);
	}
	return console.error(`The padding value "${e}" is invalid. Please provide a number or a string with a valid unit (px or %).`), 0;
}
function parsePaddings(e, t, n) {
	if (typeof e == "string" || typeof e == "number") {
		let r = parsePadding(e, n), i = parsePadding(e, t);
		return {
			top: r,
			right: i,
			bottom: r,
			left: i,
			x: i * 2,
			y: r * 2
		};
	}
	if (typeof e == "object") {
		let r = parsePadding(e.top ?? e.y ?? 0, n), i = parsePadding(e.bottom ?? e.y ?? 0, n), a = parsePadding(e.left ?? e.x ?? 0, t), o = parsePadding(e.right ?? e.x ?? 0, t);
		return {
			top: r,
			right: o,
			bottom: i,
			left: a,
			x: a + o,
			y: r + i
		};
	}
	return {
		top: 0,
		right: 0,
		bottom: 0,
		left: 0,
		x: 0,
		y: 0
	};
}
function calculateAppliedPaddings(e, t, n, r, i, a) {
	let { x: o, y: s } = rendererPointToPoint(e, [
		t,
		n,
		r
	]), { x: c, y: l } = rendererPointToPoint({
		x: e.x + e.width,
		y: e.y + e.height
	}, [
		t,
		n,
		r
	]), u = i - c, d = a - l;
	return {
		left: Math.floor(o),
		top: Math.floor(s),
		right: Math.floor(u),
		bottom: Math.floor(d)
	};
}
var getViewportForBounds = (e, t, n, r, i, a) => {
	let o = parsePaddings(a, t, n), s = (t - o.x) / e.width, c = (n - o.y) / e.height, l = clamp(Math.min(s, c), r, i), u = e.x + e.width / 2, d = e.y + e.height / 2, f = t / 2 - u * l, p = n / 2 - d * l, m = calculateAppliedPaddings(e, f, p, l, t, n), h = {
		left: Math.min(m.left - o.left, 0),
		top: Math.min(m.top - o.top, 0),
		right: Math.min(m.right - o.right, 0),
		bottom: Math.min(m.bottom - o.bottom, 0)
	};
	return {
		x: f - h.left + h.right,
		y: p - h.top + h.bottom,
		zoom: l
	};
}, isMacOs = () => typeof navigator < "u" && navigator?.userAgent?.indexOf("Mac") >= 0;
function isCoordinateExtent(e) {
	return e != null && e !== "parent";
}
function getNodeDimensions(e) {
	return {
		width: e.measured?.width ?? e.width ?? e.initialWidth ?? 0,
		height: e.measured?.height ?? e.height ?? e.initialHeight ?? 0
	};
}
function nodeHasDimensions(e) {
	return (e.measured?.width ?? e.width ?? e.initialWidth) !== void 0 && (e.measured?.height ?? e.height ?? e.initialHeight) !== void 0;
}
function evaluateAbsolutePosition(e, t = {
	width: 0,
	height: 0
}, n, r, i) {
	let a = { ...e }, o = r.get(n);
	if (o) {
		let e = o.origin || i;
		a.x += o.internals.positionAbsolute.x - (t.width ?? 0) * e[0], a.y += o.internals.positionAbsolute.y - (t.height ?? 0) * e[1];
	}
	return a;
}
function areSetsEqual(e, t) {
	if (e.size !== t.size) return !1;
	for (let n of e) if (!t.has(n)) return !1;
	return !0;
}
function withResolvers() {
	let e, t;
	return {
		promise: new Promise((n, r) => {
			e = n, t = r;
		}),
		resolve: e,
		reject: t
	};
}
function mergeAriaLabelConfig(e) {
	return {
		...O,
		...e || {}
	};
}
function getConnectionStatus(e) {
	return e === null ? null : e ? "valid" : "invalid";
}
function getPointerPosition(e, { snapGrid: t = [0, 0], snapToGrid: n = !1, transform: r, containerBounds: i }) {
	let { x: a, y: o } = getEventPosition(e), s = pointToRendererPoint({
		x: a - (i?.left ?? 0),
		y: o - (i?.top ?? 0)
	}, r), { x: c, y: l } = n ? snapPosition(s, t) : s;
	return {
		xSnapped: c,
		ySnapped: l,
		...s
	};
}
var getDimensions = (e) => ({
	width: e.offsetWidth,
	height: e.offsetHeight
}), getHostForElement = (e) => e?.getRootNode?.() || window?.document, L = [
	"INPUT",
	"SELECT",
	"TEXTAREA"
];
function isInputDOMNode(e) {
	let t = e.composedPath?.()?.[0] || e.target;
	return t?.nodeType === 1 ? L.includes(t.nodeName) || t.hasAttribute("contenteditable") || !!t.closest(".nokey") : !1;
}
var isMouseEvent = (e) => "clientX" in e, getEventPosition = (e, t) => {
	let n = isMouseEvent(e), r = n ? e.clientX : e.touches?.[0].clientX, i = n ? e.clientY : e.touches?.[0].clientY;
	return {
		x: r - (t?.left ?? 0),
		y: i - (t?.top ?? 0)
	};
}, getHandleBounds = (e, t, n, r, i) => {
	let a = t.querySelectorAll(`.${e}`);
	return !a || !a.length ? null : Array.from(a).map((t) => {
		let a = t.getBoundingClientRect();
		return {
			id: t.getAttribute("data-handleid"),
			type: e,
			nodeId: i,
			position: t.getAttribute("data-handlepos"),
			x: (a.left - n.left) / r,
			y: (a.top - n.top) / r,
			...getDimensions(t)
		};
	});
};
function getBezierEdgeCenter({ sourceX: e, sourceY: t, targetX: n, targetY: r, sourceControlX: i, sourceControlY: a, targetControlX: o, targetControlY: s }) {
	let c = e * .125 + i * .375 + o * .375 + n * .125, l = t * .125 + a * .375 + s * .375 + r * .125;
	return [
		c,
		l,
		Math.abs(c - e),
		Math.abs(l - t)
	];
}
function calculateControlOffset(e, t) {
	return e >= 0 ? .5 * e : t * 25 * Math.sqrt(-e);
}
function getControlWithCurvature({ pos: e, x1: t, y1: n, x2: r, y2: i, c: a }) {
	switch (e) {
		case F.Left: return [t - calculateControlOffset(t - r, a), n];
		case F.Right: return [t + calculateControlOffset(r - t, a), n];
		case F.Top: return [t, n - calculateControlOffset(n - i, a)];
		case F.Bottom: return [t, n + calculateControlOffset(i - n, a)];
	}
}
function getBezierPath({ sourceX: e, sourceY: t, sourcePosition: n = F.Bottom, targetX: r, targetY: i, targetPosition: a = F.Top, curvature: o = .25 }) {
	let [s, c] = getControlWithCurvature({
		pos: n,
		x1: e,
		y1: t,
		x2: r,
		y2: i,
		c: o
	}), [l, u] = getControlWithCurvature({
		pos: a,
		x1: r,
		y1: i,
		x2: e,
		y2: t,
		c: o
	}), [d, f, p, m] = getBezierEdgeCenter({
		sourceX: e,
		sourceY: t,
		targetX: r,
		targetY: i,
		sourceControlX: s,
		sourceControlY: c,
		targetControlX: l,
		targetControlY: u
	});
	return [
		`M${e},${t} C${s},${c} ${l},${u} ${r},${i}`,
		d,
		f,
		p,
		m
	];
}
function getEdgeCenter({ sourceX: e, sourceY: t, targetX: n, targetY: r }) {
	let i = Math.abs(n - e) / 2, a = n < e ? n + i : n - i, o = Math.abs(r - t) / 2;
	return [
		a,
		r < t ? r + o : r - o,
		i,
		o
	];
}
function getElevatedEdgeZIndex({ sourceNode: e, targetNode: t, selected: n = !1, zIndex: r = 0, elevateOnSelect: i = !1, zIndexMode: a = "basic" }) {
	return a === "manual" ? r : (i && n ? r + 1e3 : r) + Math.max(e.parentId || i && e.selected ? e.internals.z : 0, t.parentId || i && t.selected ? t.internals.z : 0);
}
function isEdgeVisible({ sourceNode: e, targetNode: t, width: n, height: r, transform: i }) {
	let a = getBoundsOfBoxes(nodeToBox(e), nodeToBox(t));
	return a.x === a.x2 && (a.x2 += 1), a.y === a.y2 && (a.y2 += 1), getOverlappingArea({
		x: -i[0] / i[2],
		y: -i[1] / i[2],
		width: n / i[2],
		height: r / i[2]
	}, boxToRect(a)) > 0;
}
var getEdgeId = ({ source: e, sourceHandle: t, target: n, targetHandle: r }) => `xy-edge__${e}${t || ""}-${n}${r || ""}`, connectionExists = (e, t) => t.some((t) => t.source === e.source && t.target === e.target && (t.sourceHandle === e.sourceHandle || !t.sourceHandle && !e.sourceHandle) && (t.targetHandle === e.targetHandle || !t.targetHandle && !e.targetHandle)), R = /* @__PURE__ */ e((e, t, n = {}) => {
	if (!e.source || !e.target) return n.onError?.("006", T.error006()), t;
	let r = n.getEdgeId || getEdgeId, i;
	return i = isEdgeBase(e) ? { ...e } : {
		...e,
		id: r(e)
	}, connectionExists(i, t) ? t : (i.sourceHandle === null && delete i.sourceHandle, i.targetHandle === null && delete i.targetHandle, t.concat(i));
}, "addEdge");
function getStraightPath({ sourceX: e, sourceY: t, targetX: n, targetY: r }) {
	let [i, a, o, s] = getEdgeCenter({
		sourceX: e,
		sourceY: t,
		targetX: n,
		targetY: r
	});
	return [
		`M ${e},${t}L ${n},${r}`,
		i,
		a,
		o,
		s
	];
}
var z = {
	[F.Left]: {
		x: -1,
		y: 0
	},
	[F.Right]: {
		x: 1,
		y: 0
	},
	[F.Top]: {
		x: 0,
		y: -1
	},
	[F.Bottom]: {
		x: 0,
		y: 1
	}
}, getDirection = ({ source: e, sourcePosition: t = F.Bottom, target: n }) => t === F.Left || t === F.Right ? e.x < n.x ? {
	x: 1,
	y: 0
} : {
	x: -1,
	y: 0
} : e.y < n.y ? {
	x: 0,
	y: 1
} : {
	x: 0,
	y: -1
}, distance = (e, t) => Math.sqrt((t.x - e.x) ** 2 + (t.y - e.y) ** 2);
function getPoints({ source: e, sourcePosition: t = F.Bottom, target: n, targetPosition: r = F.Top, center: i, offset: a, stepPosition: o }) {
	let s = z[t], c = z[r], l = {
		x: e.x + s.x * a,
		y: e.y + s.y * a
	}, u = {
		x: n.x + c.x * a,
		y: n.y + c.y * a
	}, d = getDirection({
		source: l,
		sourcePosition: t,
		target: u
	}), f = d.x === 0 ? "y" : "x", p = d[f], m = [], h, g, _ = {
		x: 0,
		y: 0
	}, v = {
		x: 0,
		y: 0
	}, [, , y, b] = getEdgeCenter({
		sourceX: e.x,
		sourceY: e.y,
		targetX: n.x,
		targetY: n.y
	});
	if (s[f] * c[f] === -1) {
		f === "x" ? (h = i.x ?? l.x + (u.x - l.x) * o, g = i.y ?? (l.y + u.y) / 2) : (h = i.x ?? (l.x + u.x) / 2, g = i.y ?? l.y + (u.y - l.y) * o);
		let e = [{
			x: h,
			y: l.y
		}, {
			x: h,
			y: u.y
		}], t = [{
			x: l.x,
			y: g
		}, {
			x: u.x,
			y: g
		}];
		m = s[f] === p ? f === "x" ? e : t : f === "x" ? t : e;
	} else {
		let i = [{
			x: l.x,
			y: u.y
		}], o = [{
			x: u.x,
			y: l.y
		}];
		if (m = f === "x" ? s.x === p ? o : i : s.y === p ? i : o, t === r) {
			let t = Math.abs(e[f] - n[f]);
			if (t <= a) {
				let r = Math.min(a - 1, a - t);
				s[f] === p ? _[f] = (l[f] > e[f] ? -1 : 1) * r : v[f] = (u[f] > n[f] ? -1 : 1) * r;
			}
		}
		if (t !== r) {
			let e = f === "x" ? "y" : "x", t = s[f] === c[e], n = l[e] > u[e], r = l[e] < u[e];
			(s[f] === 1 && (!t && n || t && r) || s[f] !== 1 && (!t && r || t && n)) && (m = f === "x" ? i : o);
		}
		let d = {
			x: l.x + _.x,
			y: l.y + _.y
		}, y = {
			x: u.x + v.x,
			y: u.y + v.y
		};
		Math.max(Math.abs(d.x - m[0].x), Math.abs(y.x - m[0].x)) >= Math.max(Math.abs(d.y - m[0].y), Math.abs(y.y - m[0].y)) ? (h = (d.x + y.x) / 2, g = m[0].y) : (h = m[0].x, g = (d.y + y.y) / 2);
	}
	let x = {
		x: l.x + _.x,
		y: l.y + _.y
	}, S = {
		x: u.x + v.x,
		y: u.y + v.y
	};
	return [
		[
			e,
			...x.x !== m[0].x || x.y !== m[0].y ? [x] : [],
			...m,
			...S.x !== m[m.length - 1].x || S.y !== m[m.length - 1].y ? [S] : [],
			n
		],
		h,
		g,
		y,
		b
	];
}
function getBend(e, t, n, r) {
	let i = Math.min(distance(e, t) / 2, distance(t, n) / 2, r), { x: a, y: o } = t;
	if (e.x === a && a === n.x || e.y === o && o === n.y) return `L${a} ${o}`;
	if (e.y === o) {
		let t = e.x < n.x ? -1 : 1, r = e.y < n.y ? 1 : -1;
		return `L ${a + i * t},${o}Q ${a},${o} ${a},${o + i * r}`;
	}
	let s = e.x < n.x ? 1 : -1;
	return `L ${a},${o + i * (e.y < n.y ? -1 : 1)}Q ${a},${o} ${a + i * s},${o}`;
}
function getSmoothStepPath({ sourceX: e, sourceY: t, sourcePosition: n = F.Bottom, targetX: r, targetY: i, targetPosition: a = F.Top, borderRadius: o = 5, centerX: s, centerY: c, offset: l = 20, stepPosition: u = .5 }) {
	let [d, f, p, m, h] = getPoints({
		source: {
			x: e,
			y: t
		},
		sourcePosition: n,
		target: {
			x: r,
			y: i
		},
		targetPosition: a,
		center: {
			x: s,
			y: c
		},
		offset: l,
		stepPosition: u
	}), g = `M${d[0].x} ${d[0].y}`;
	for (let e = 1; e < d.length - 1; e++) g += getBend(d[e - 1], d[e], d[e + 1], o);
	return g += `L${d[d.length - 1].x} ${d[d.length - 1].y}`, [
		g,
		f,
		p,
		m,
		h
	];
}
function isNodeInitialized(e) {
	return e && !!(e.internals.handleBounds || e.handles?.length) && !!(e.measured.width || e.width || e.initialWidth);
}
function getEdgePosition(e) {
	let { sourceNode: t, targetNode: n } = e;
	if (!isNodeInitialized(t) || !isNodeInitialized(n)) return null;
	let r = t.internals.handleBounds || toHandleBounds(t.handles), i = n.internals.handleBounds || toHandleBounds(n.handles), a = getHandle$1(r?.source ?? [], e.sourceHandle), o = getHandle$1(e.connectionMode === k.Strict ? i?.target ?? [] : (i?.target ?? []).concat(i?.source ?? []), e.targetHandle);
	if (!a || !o) return e.onError?.("008", T.error008(a ? "target" : "source", {
		id: e.id,
		sourceHandle: e.sourceHandle,
		targetHandle: e.targetHandle
	})), null;
	let s = a?.position || F.Bottom, c = o?.position || F.Top, l = getHandlePosition(t, a, s), u = getHandlePosition(n, o, c);
	return {
		sourceX: l.x,
		sourceY: l.y,
		targetX: u.x,
		targetY: u.y,
		sourcePosition: s,
		targetPosition: c
	};
}
function toHandleBounds(e) {
	if (!e) return null;
	let t = [], n = [];
	for (let r of e) r.width = r.width ?? 1, r.height = r.height ?? 1, r.type === "source" ? t.push(r) : r.type === "target" && n.push(r);
	return {
		source: t,
		target: n
	};
}
function getHandlePosition(e, t, n = F.Left, r = !1) {
	let i = (t?.x ?? 0) + e.internals.positionAbsolute.x, a = (t?.y ?? 0) + e.internals.positionAbsolute.y, { width: o, height: s } = t ?? getNodeDimensions(e);
	if (r) return {
		x: i + o / 2,
		y: a + s / 2
	};
	switch (t?.position ?? n) {
		case F.Top: return {
			x: i + o / 2,
			y: a
		};
		case F.Right: return {
			x: i + o,
			y: a + s / 2
		};
		case F.Bottom: return {
			x: i + o / 2,
			y: a + s
		};
		case F.Left: return {
			x: i,
			y: a + s / 2
		};
	}
}
function getHandle$1(e, t) {
	return e && (t ? e.find((e) => e.id === t) : e[0]) || null;
}
function getMarkerId(e, t) {
	return e ? typeof e == "string" ? e : `${t ? `${t}__` : ""}${Object.keys(e).sort().map((t) => `${t}=${e[t]}`).join("&")}` : "";
}
function createMarkerIds(e, { id: t, defaultColor: n, defaultMarkerStart: r, defaultMarkerEnd: i }) {
	let a = /* @__PURE__ */ new Set();
	return e.reduce((e, o) => ([o.markerStart || r, o.markerEnd || i].forEach((r) => {
		if (r && typeof r == "object") {
			let i = getMarkerId(r, t);
			a.has(i) || (e.push({
				id: i,
				color: r.color || n,
				...r
			}), a.add(i));
		}
	}), e), []).sort((e, t) => e.id.localeCompare(t.id));
}
function getNodeToolbarTransform(e, t, n, r, i) {
	let a = .5;
	i === "start" ? a = 0 : i === "end" && (a = 1);
	let o = [(e.x + e.width * a) * t.zoom + t.x, e.y * t.zoom + t.y - r], s = [-100 * a, -100];
	switch (n) {
		case F.Right:
			o = [(e.x + e.width) * t.zoom + t.x + r, (e.y + e.height * a) * t.zoom + t.y], s = [0, -100 * a];
			break;
		case F.Bottom:
			o[1] = (e.y + e.height) * t.zoom + t.y + r, s[1] = 0;
			break;
		case F.Left: o = [e.x * t.zoom + t.x - r, (e.y + e.height * a) * t.zoom + t.y], s = [-100, -100 * a];
	}
	return `translate(${o[0]}px, ${o[1]}px) translate(${s[0]}%, ${s[1]}%)`;
}
var B = 1e3, V = 10, H = {
	nodeOrigin: [0, 0],
	nodeExtent: E,
	elevateNodesOnSelect: !0,
	zIndexMode: "basic",
	defaults: {}
}, U = {
	...H,
	checkEquality: !0
};
function mergeObjects(e, t) {
	let n = { ...e };
	for (let e in t) t[e] !== void 0 && (n[e] = t[e]);
	return n;
}
function updateAbsolutePositions(e, t, n) {
	let r = mergeObjects(H, n);
	for (let n of e.values()) if (n.parentId) updateChildNode(n, e, t, r);
	else {
		let e = clampPosition(getNodePositionWithOrigin(n, r.nodeOrigin), isCoordinateExtent(n.extent) ? n.extent : r.nodeExtent, getNodeDimensions(n));
		n.internals.positionAbsolute = e;
	}
}
function parseHandles(e, t) {
	if (!e.handles) return e.measured ? t?.internals.handleBounds : void 0;
	let n = [], r = [];
	for (let t of e.handles) {
		let i = {
			id: t.id,
			width: t.width ?? 1,
			height: t.height ?? 1,
			nodeId: e.id,
			x: t.x,
			y: t.y,
			position: t.position,
			type: t.type
		};
		t.type === "source" ? n.push(i) : t.type === "target" && r.push(i);
	}
	return {
		source: n,
		target: r
	};
}
function isManualZIndexMode(e) {
	return e === "manual";
}
function adoptUserNodes(e, t, n, r = {}) {
	let i = mergeObjects(U, r), a = { i: 0 }, o = new Map(t), s = i?.elevateNodesOnSelect && !isManualZIndexMode(i.zIndexMode) ? B : 0, c = e.length > 0, l = !1;
	t.clear(), n.clear();
	for (let u of e) {
		let e = o.get(u.id);
		if (i.checkEquality && u === e?.internals.userNode) t.set(u.id, e);
		else {
			let n = clampPosition(getNodePositionWithOrigin(u, i.nodeOrigin), isCoordinateExtent(u.extent) ? u.extent : i.nodeExtent, getNodeDimensions(u));
			e = {
				...i.defaults,
				...u,
				measured: {
					width: u.measured?.width,
					height: u.measured?.height
				},
				internals: {
					positionAbsolute: n,
					handleBounds: parseHandles(u, e),
					z: calculateZ(u, s, i.zIndexMode),
					userNode: u
				}
			}, t.set(u.id, e);
		}
		(e.measured === void 0 || e.measured.width === void 0 || e.measured.height === void 0) && !e.hidden && (c = !1), u.parentId && updateChildNode(e, t, n, r, a), l ||= u.selected ?? !1;
	}
	return {
		nodesInitialized: c,
		hasSelectedNodes: l
	};
}
function updateParentLookup(e, t) {
	if (!e.parentId) return;
	let n = t.get(e.parentId);
	n ? n.set(e.id, e) : t.set(e.parentId, /* @__PURE__ */ new Map([[e.id, e]]));
}
function updateChildNode(e, t, n, r, i) {
	let { elevateNodesOnSelect: a, nodeOrigin: o, nodeExtent: s, zIndexMode: c } = mergeObjects(H, r), l = e.parentId, u = t.get(l);
	if (!u) {
		console.warn(`Parent node ${l} not found. Please make sure that parent nodes are in front of their child nodes in the nodes array.`);
		return;
	}
	updateParentLookup(e, n), i && !u.parentId && u.internals.rootParentIndex === void 0 && c === "auto" && (u.internals.rootParentIndex = ++i.i, u.internals.z = u.internals.z + i.i * V), i && u.internals.rootParentIndex !== void 0 && (i.i = u.internals.rootParentIndex);
	let { x: d, y: f, z: p } = calculateChildXYZ(e, u, o, s, a && !isManualZIndexMode(c) ? B : 0, c), { positionAbsolute: m } = e.internals, h = d !== m.x || f !== m.y;
	(h || p !== e.internals.z) && t.set(e.id, {
		...e,
		internals: {
			...e.internals,
			positionAbsolute: h ? {
				x: d,
				y: f
			} : m,
			z: p
		}
	});
}
function calculateZ(e, t, n) {
	let r = isNumeric(e.zIndex) ? e.zIndex : 0;
	return isManualZIndexMode(n) ? r : r + (e.selected ? t : 0);
}
function calculateChildXYZ(e, t, n, r, i, a) {
	let { x: o, y: s } = t.internals.positionAbsolute, c = getNodeDimensions(e), l = getNodePositionWithOrigin(e, n), u = isCoordinateExtent(e.extent) ? clampPosition(l, e.extent, c) : l, d = clampPosition({
		x: o + u.x,
		y: s + u.y
	}, r, c);
	e.extent === "parent" && (d = clampPositionToParent(d, c, t));
	let f = calculateZ(e, i, a), p = t.internals.z ?? 0;
	return {
		x: d.x,
		y: d.y,
		z: p >= f ? p + 1 : f
	};
}
function handleExpandParent(e, t, n, r = [0, 0]) {
	let i = [], a = /* @__PURE__ */ new Map();
	for (let n of e) {
		let e = t.get(n.parentId);
		if (!e) continue;
		let r = getBoundsOfRects(a.get(n.parentId)?.expandedRect ?? nodeToRect(e), n.rect);
		a.set(n.parentId, {
			expandedRect: r,
			parent: e
		});
	}
	return a.size > 0 && a.forEach(({ expandedRect: t, parent: a }, o) => {
		let s = a.internals.positionAbsolute, c = getNodeDimensions(a), l = a.origin ?? r, u = t.x < s.x ? Math.round(Math.abs(s.x - t.x)) : 0, d = t.y < s.y ? Math.round(Math.abs(s.y - t.y)) : 0, f = Math.max(c.width, Math.round(t.width)), p = Math.max(c.height, Math.round(t.height)), m = (f - c.width) * l[0], h = (p - c.height) * l[1];
		(u > 0 || d > 0 || m || h) && (i.push({
			id: o,
			type: "position",
			position: {
				x: a.position.x - u + m,
				y: a.position.y - d + h
			}
		}), n.get(o)?.forEach((t) => {
			e.some((e) => e.id === t.id) || i.push({
				id: t.id,
				type: "position",
				position: {
					x: t.position.x + u,
					y: t.position.y + d
				}
			});
		})), (c.width < t.width || c.height < t.height || u || d) && i.push({
			id: o,
			type: "dimensions",
			setAttributes: !0,
			dimensions: {
				width: f + (u ? l[0] * u - m : 0),
				height: p + (d ? l[1] * d - h : 0)
			}
		});
	}), i;
}
function updateNodeInternals(e, t, n, r, i, a, o) {
	let s = r?.querySelector(".xyflow__viewport"), c = !1;
	if (!s) return {
		changes: [],
		updatedInternals: c
	};
	let l = [], u = window.getComputedStyle(s), { m22: d } = new window.DOMMatrixReadOnly(u.transform), f = [];
	for (let r of e.values()) {
		let e = t.get(r.id);
		if (!e) continue;
		if (e.hidden) {
			t.set(e.id, {
				...e,
				internals: {
					...e.internals,
					handleBounds: void 0
				}
			}), c = !0;
			continue;
		}
		let s = getDimensions(r.nodeElement), u = e.measured.width !== s.width || e.measured.height !== s.height;
		if (s.width && s.height && (u || !e.internals.handleBounds || r.force)) {
			let p = r.nodeElement.getBoundingClientRect(), m = isCoordinateExtent(e.extent) ? e.extent : a, { positionAbsolute: h } = e.internals;
			if (e.parentId && e.extent === "parent") {
				let n = t.get(e.parentId);
				n && (h = clampPositionToParent(h, s, n));
			} else m && (h = clampPosition(h, m, s));
			let g = {
				...e,
				measured: s,
				internals: {
					...e.internals,
					positionAbsolute: h,
					handleBounds: {
						source: getHandleBounds("source", r.nodeElement, p, d, e.id),
						target: getHandleBounds("target", r.nodeElement, p, d, e.id)
					}
				}
			};
			t.set(e.id, g), e.parentId && updateChildNode(g, t, n, {
				nodeOrigin: i,
				zIndexMode: o
			}), c = !0, u && (l.push({
				id: e.id,
				type: "dimensions",
				dimensions: s
			}), e.expandParent && e.parentId && f.push({
				id: e.id,
				parentId: e.parentId,
				rect: nodeToRect(g, i)
			}));
		}
	}
	if (f.length > 0) {
		let e = handleExpandParent(f, t, n, i);
		l.push(...e);
	}
	return {
		changes: l,
		updatedInternals: c
	};
}
async function panBy({ delta: e, panZoom: t, transform: n, translateExtent: r, width: i, height: a }) {
	if (!t || !e.x && !e.y) return !1;
	let o = await t.setViewportConstrained({
		x: n[0] + e.x,
		y: n[1] + e.y,
		zoom: n[2]
	}, [[0, 0], [i, a]], r);
	return !!o && (o.x !== n[0] || o.y !== n[1] || o.k !== n[2]);
}
function addConnectionToLookup(e, t, n, r, i, a) {
	let o = i, s = r.get(o) || /* @__PURE__ */ new Map();
	r.set(o, s.set(n, t)), o = `${i}-${e}`;
	let c = r.get(o) || /* @__PURE__ */ new Map();
	if (r.set(o, c.set(n, t)), a) {
		o = `${i}-${e}-${a}`;
		let s = r.get(o) || /* @__PURE__ */ new Map();
		r.set(o, s.set(n, t));
	}
}
function updateConnectionLookup(e, t, n) {
	e.clear(), t.clear();
	for (let r of n) {
		let { source: n, target: i, sourceHandle: a = null, targetHandle: o = null } = r, s = {
			edgeId: r.id,
			source: n,
			target: i,
			sourceHandle: a,
			targetHandle: o
		}, c = `${n}-${a}--${i}-${o}`;
		addConnectionToLookup("source", s, `${i}-${o}--${n}-${a}`, e, n, a), addConnectionToLookup("target", s, c, e, i, o), t.set(r.id, r);
	}
}
function shallowNodeData(e, t) {
	if (e === null || t === null) return !1;
	let n = Array.isArray(e) ? e : [e], r = Array.isArray(t) ? t : [t];
	if (n.length !== r.length) return !1;
	for (let e = 0; e < n.length; e++) if (n[e].id !== r[e].id || n[e].type !== r[e].type || !Object.is(n[e].data, r[e].data)) return !1;
	return !0;
}
function isParentSelected(e, t) {
	if (!e.parentId) return !1;
	let n = t.get(e.parentId);
	return n ? n.selected ? !0 : isParentSelected(n, t) : !1;
}
function hasSelector(e, t, n) {
	let r = e;
	do {
		if (r?.matches?.(t)) return !0;
		if (r === n) return !1;
		r = r?.parentElement;
	} while (r);
	return !1;
}
function getDragItems(e, t, n, r) {
	let i = /* @__PURE__ */ new Map();
	for (let [a, o] of e) if ((o.selected || o.id === r) && (!o.parentId || !isParentSelected(o, e)) && (o.draggable || t && o.draggable === void 0)) {
		let t = e.get(a);
		t && i.set(a, {
			id: a,
			position: t.position || {
				x: 0,
				y: 0
			},
			distance: {
				x: n.x - t.internals.positionAbsolute.x,
				y: n.y - t.internals.positionAbsolute.y
			},
			extent: t.extent,
			parentId: t.parentId,
			origin: t.origin,
			expandParent: t.expandParent,
			internals: { positionAbsolute: t.internals.positionAbsolute || {
				x: 0,
				y: 0
			} },
			measured: {
				width: t.measured.width ?? 0,
				height: t.measured.height ?? 0
			}
		});
	}
	return i;
}
function getEventHandlerParams({ nodeId: e, dragItems: t, nodeLookup: n, dragging: r = !0 }) {
	let i = [];
	for (let [e, a] of t) {
		let t = n.get(e)?.internals.userNode;
		t && i.push({
			...t,
			position: a.position,
			dragging: r
		});
	}
	if (!e) return [i[0], i];
	let a = n.get(e)?.internals.userNode;
	return [a ? {
		...a,
		position: t.get(e)?.position || a.position,
		dragging: r
	} : i[0], i];
}
function calculateSnapOffset({ dragItems: e, snapGrid: t, x: n, y: r }) {
	let i = e.values().next().value;
	if (!i) return null;
	let a = {
		x: n - i.distance.x,
		y: r - i.distance.y
	}, o = snapPosition(a, t);
	return {
		x: o.x - a.x,
		y: o.y - a.y
	};
}
function XYDrag({ onNodeMouseDown: e, getStoreItems: t, onDragStart: r, onDrag: i, onDragStop: a }) {
	let o = {
		x: null,
		y: null
	}, s = 0, l = /* @__PURE__ */ new Map(), u = !1, d = {
		x: 0,
		y: 0
	}, f = null, p = !1, m = null, h = !1, g = !1, _ = null;
	function update({ noDragClassName: v, handleSelector: y, domNode: b, isSelectable: x, nodeId: S, nodeClickDistance: C = 0 }) {
		m = c(b);
		function updateNodes({ x: e, y: n }) {
			let { nodeLookup: r, nodeExtent: a, snapGrid: s, snapToGrid: c, nodeOrigin: u, onNodeDrag: d, onSelectionDrag: f, onError: p, updateNodePositions: m } = t();
			o = {
				x: e,
				y: n
			};
			let h = !1, v = l.size > 1, y = v && a ? rectToBox(getInternalNodesBounds(l)) : null, b = v && c ? calculateSnapOffset({
				dragItems: l,
				snapGrid: s,
				x: e,
				y: n
			}) : null;
			for (let [t, i] of l) {
				if (!r.has(t)) continue;
				let o = {
					x: e - i.distance.x,
					y: n - i.distance.y
				};
				c && (o = b ? {
					x: Math.round(o.x + b.x),
					y: Math.round(o.y + b.y)
				} : snapPosition(o, s));
				let l = null;
				if (v && a && !i.extent && y) {
					let { positionAbsolute: e } = i.internals, t = e.x - y.x + a[0][0], n = e.x + i.measured.width - y.x2 + a[1][0], r = e.y - y.y + a[0][1], o = e.y + i.measured.height - y.y2 + a[1][1];
					l = [[t, r], [n, o]];
				}
				let { position: d, positionAbsolute: f } = calculateNodePosition({
					nodeId: t,
					nextPosition: o,
					nodeLookup: r,
					nodeExtent: l || a,
					nodeOrigin: u,
					onError: p
				});
				h = h || i.position.x !== d.x || i.position.y !== d.y, i.position = d, i.internals.positionAbsolute = f;
			}
			if (g ||= h, h && (m(l, !0), _ && (i || d || !S && f))) {
				let [e, t] = getEventHandlerParams({
					nodeId: S,
					dragItems: l,
					nodeLookup: r
				});
				i?.(_, l, e, t), d?.(_, e, t), S || f?.(_, t);
			}
		}
		async function autoPan() {
			if (!f) return;
			let { transform: e, panBy: n, autoPanSpeed: r, autoPanOnNodeDrag: i } = t();
			if (!i) {
				u = !1, cancelAnimationFrame(s);
				return;
			}
			let [a, c] = calcAutoPan(d, f, r);
			(a !== 0 || c !== 0) && (o.x = (o.x ?? 0) - a / e[2], o.y = (o.y ?? 0) - c / e[2], await n({
				x: a,
				y: c
			}) && updateNodes(o)), s = requestAnimationFrame(autoPan);
		}
		function startDrag(n) {
			let { nodeLookup: i, multiSelectionActive: a, nodesDraggable: s, transform: c, snapGrid: u, snapToGrid: d, selectNodesOnDrag: m, onNodeDragStart: h, onSelectionDragStart: g, unselectNodesAndEdges: _ } = t();
			p = !0, (!m || !x) && !a && S && (i.get(S)?.selected || _()), x && m && S && e?.(S);
			let v = getPointerPosition(n.sourceEvent, {
				transform: c,
				snapGrid: u,
				snapToGrid: d,
				containerBounds: f
			});
			if (o = v, l = getDragItems(i, s, v, S), l.size > 0 && (r || h || !S && g)) {
				let [e, t] = getEventHandlerParams({
					nodeId: S,
					dragItems: l,
					nodeLookup: i
				});
				r?.(n.sourceEvent, l, e, t), h?.(n.sourceEvent, e, t), S || g?.(n.sourceEvent, t);
			}
		}
		let w = n().clickDistance(C).on("start", (e) => {
			let { domNode: n, nodeDragThreshold: r, transform: i, snapGrid: a, snapToGrid: s } = t();
			f = n?.getBoundingClientRect() || null, h = !1, g = !1, _ = e.sourceEvent, r === 0 && startDrag(e), o = getPointerPosition(e.sourceEvent, {
				transform: i,
				snapGrid: a,
				snapToGrid: s,
				containerBounds: f
			}), d = getEventPosition(e.sourceEvent, f);
		}).on("drag", (e) => {
			let { autoPanOnNodeDrag: n, transform: r, snapGrid: i, snapToGrid: a, nodeDragThreshold: s, nodeLookup: c } = t(), m = getPointerPosition(e.sourceEvent, {
				transform: r,
				snapGrid: i,
				snapToGrid: a,
				containerBounds: f
			});
			if (_ = e.sourceEvent, (e.sourceEvent.type === "touchmove" && e.sourceEvent.touches.length > 1 || S && !c.has(S)) && (h = !0), !h) {
				if (!u && n && p && (u = !0, autoPan()), !p) {
					let t = getEventPosition(e.sourceEvent, f), n = t.x - d.x, r = t.y - d.y;
					Math.sqrt(n * n + r * r) > s && startDrag(e);
				}
				(o.x !== m.xSnapped || o.y !== m.ySnapped) && l && p && (d = getEventPosition(e.sourceEvent, f), updateNodes(m));
			}
		}).on("end", (e) => {
			if (!p || h) {
				h && l.size > 0 && t().updateNodePositions(l, !1);
				return;
			}
			if (u = !1, p = !1, cancelAnimationFrame(s), l.size > 0) {
				let { nodeLookup: n, updateNodePositions: r, onNodeDragStop: i, onSelectionDragStop: o } = t();
				if (g &&= (r(l, !1), !1), a || i || !S && o) {
					let [t, r] = getEventHandlerParams({
						nodeId: S,
						dragItems: l,
						nodeLookup: n,
						dragging: !1
					});
					a?.(e.sourceEvent, l, t, r), i?.(e.sourceEvent, t, r), S || o?.(e.sourceEvent, r);
				}
			}
		}).filter((e) => {
			let t = e.target;
			return !e.button && (!v || !hasSelector(t, `.${v}`, b)) && (!y || hasSelector(t, y, b));
		});
		m.call(w);
	}
	function destroy() {
		m?.on(".drag", null);
	}
	return {
		update,
		destroy
	};
}
function getNodesWithinDistance(e, t, n) {
	let r = [], i = {
		x: e.x - n,
		y: e.y - n,
		width: n * 2,
		height: n * 2
	};
	for (let e of t.values()) getOverlappingArea(i, nodeToRect(e)) > 0 && r.push(e);
	return r;
}
var W = 250;
function getClosestHandle(e, t, n, r) {
	let i = [], a = Infinity, o = getNodesWithinDistance(e, n, t + W);
	for (let n of o) {
		let o = [...n.internals.handleBounds?.source ?? [], ...n.internals.handleBounds?.target ?? []];
		for (let s of o) {
			if (r.nodeId === s.nodeId && r.type === s.type && r.id === s.id) continue;
			let { x: o, y: c } = getHandlePosition(n, s, s.position, !0), l = Math.sqrt((o - e.x) ** 2 + (c - e.y) ** 2);
			l > t || (l < a ? (i = [{
				...s,
				x: o,
				y: c
			}], a = l) : l === a && i.push({
				...s,
				x: o,
				y: c
			}));
		}
	}
	if (!i.length) return null;
	if (i.length > 1) {
		let e = r.type === "source" ? "target" : "source";
		return i.find((t) => t.type === e) ?? i[0];
	}
	return i[0];
}
function getHandle(e, t, n, r, i, a = !1) {
	let o = r.get(e);
	if (!o) return null;
	let s = i === "strict" ? o.internals.handleBounds?.[t] : [...o.internals.handleBounds?.source ?? [], ...o.internals.handleBounds?.target ?? []], c = (n ? s?.find((e) => e.id === n) : s?.[0]) ?? null;
	return c && a ? {
		...c,
		...getHandlePosition(o, c, c.position, !0)
	} : c;
}
function getHandleType(e, t) {
	return e || (t?.classList.contains("target") ? "target" : t?.classList.contains("source") ? "source" : null);
}
function isConnectionValid(e, t) {
	let n = null;
	return t ? n = !0 : e && !t && (n = !1), n;
}
var alwaysValid = () => !0;
function onPointerDown(e, { connectionMode: t, connectionRadius: n, handleId: r, nodeId: i, edgeUpdaterType: a, isTarget: o, domNode: s, nodeLookup: c, lib: l, autoPanOnConnect: u, flowId: d, panBy: f, cancelConnection: p, onConnectStart: m, onConnect: h, onConnectEnd: g, isValidConnection: _ = alwaysValid, onReconnectEnd: v, updateConnection: y, getTransform: b, getFromHandle: x, autoPanSpeed: S, dragThreshold: C = 1, handleDomNode: w }) {
	let T = getHostForElement(e.target), E = 0, D, { x: O, y: k } = getEventPosition(e), A = getHandleType(a, w), j = s?.getBoundingClientRect(), M = !1;
	if (!j || !A) return;
	let N = getHandle(i, A, r, c, t);
	if (!N) return;
	let P = getEventPosition(e, j), L = !1, R = null, z = !1, B = null;
	function autoPan() {
		if (!u || !j) return;
		let [e, t] = calcAutoPan(P, j, S);
		f({
			x: e,
			y: t
		}), E = requestAnimationFrame(autoPan);
	}
	let V = {
		...N,
		nodeId: i,
		type: A,
		position: N.position
	}, H = c.get(i), U = {
		inProgress: !0,
		isValid: null,
		from: getHandlePosition(H, V, F.Left, !0),
		fromHandle: V,
		fromPosition: V.position,
		fromNode: H,
		to: P,
		toHandle: null,
		toPosition: I[V.position],
		toNode: null,
		pointer: P
	};
	function startConnection() {
		M = !0, y(U), m?.(e, {
			nodeId: i,
			handleId: r,
			handleType: A
		});
	}
	C === 0 && startConnection();
	function onPointerMove(e) {
		if (!M) {
			let { x: t, y: n } = getEventPosition(e), r = t - O, i = n - k;
			if (!(r * r + i * i > C * C)) return;
			startConnection();
		}
		if (!x() || !V) {
			onPointerUp(e);
			return;
		}
		let a = b();
		P = getEventPosition(e, j), D = getClosestHandle(pointToRendererPoint(P, a, !1, [1, 1]), n, c, V), L ||= (autoPan(), !0);
		let s = isValidHandle(e, {
			handle: D,
			connectionMode: t,
			fromNodeId: i,
			fromHandleId: r,
			fromType: o ? "target" : "source",
			isValidConnection: _,
			doc: T,
			lib: l,
			flowId: d,
			nodeLookup: c
		});
		B = s.handleDomNode, R = s.connection, z = isConnectionValid(!!D, s.isValid);
		let u = c.get(i), f = u ? getHandlePosition(u, V, F.Left, !0) : U.from, p = {
			...U,
			from: f,
			isValid: z,
			to: s.toHandle && z ? rendererPointToPoint({
				x: s.toHandle.x,
				y: s.toHandle.y
			}, a) : P,
			toHandle: s.toHandle,
			toPosition: z && s.toHandle ? s.toHandle.position : I[V.position],
			toNode: s.toHandle ? c.get(s.toHandle.nodeId) : null,
			pointer: P
		};
		y(p), U = p;
	}
	function onPointerUp(e) {
		if (!("touches" in e && e.touches.length > 0)) {
			if (M) {
				(D || B) && R && z && h?.(R);
				let { inProgress: t, ...n } = U, r = {
					...n,
					toPosition: U.toHandle ? U.toPosition : null
				};
				g?.(e, r), a && v?.(e, r);
			}
			p(), cancelAnimationFrame(E), L = !1, z = !1, R = null, B = null, T.removeEventListener("mousemove", onPointerMove), T.removeEventListener("mouseup", onPointerUp), T.removeEventListener("touchmove", onPointerMove), T.removeEventListener("touchend", onPointerUp);
		}
	}
	T.addEventListener("mousemove", onPointerMove), T.addEventListener("mouseup", onPointerUp), T.addEventListener("touchmove", onPointerMove), T.addEventListener("touchend", onPointerUp);
}
function isValidHandle(e, { handle: t, connectionMode: n, fromNodeId: r, fromHandleId: i, fromType: a, doc: o, lib: s, flowId: c, isValidConnection: l = alwaysValid, nodeLookup: u }) {
	let d = a === "target", f = t ? o.querySelector(`.${s}-flow__handle[data-id="${c}-${t?.nodeId}-${t?.id}-${t?.type}"]`) : null, { x: p, y: m } = getEventPosition(e), h = o.elementFromPoint(p, m), g = h?.classList.contains(`${s}-flow__handle`) ? h : f, _ = {
		handleDomNode: g,
		isValid: !1,
		connection: null,
		toHandle: null
	};
	if (g) {
		let e = getHandleType(void 0, g), t = g.getAttribute("data-nodeid"), a = g.getAttribute("data-handleid"), o = g.classList.contains("connectable"), s = g.classList.contains("connectableend");
		if (!t || !e) return _;
		let c = {
			source: d ? t : r,
			sourceHandle: d ? a : i,
			target: d ? r : t,
			targetHandle: d ? i : a
		};
		_.connection = c, _.isValid = o && s && (n === k.Strict ? d && e === "source" || !d && e === "target" : t !== r || a !== i) && l(c), _.toHandle = getHandle(t, e, a, u, n, !0);
	}
	return _;
}
var G = {
	onPointerDown,
	isValid: isValidHandle
};
function XYMinimap({ domNode: e, panZoom: t, getTransform: n, getViewScale: r }) {
	let a = c(e);
	function update({ translateExtent: e, width: i, height: s, zoomStep: c = 1, pannable: l = !0, zoomable: u = !0, inversePan: d = !1 }) {
		let zoomHandler = (e) => {
			if (e.sourceEvent.type !== "wheel" || !t) return;
			let r = n(), i = e.sourceEvent.ctrlKey && isMacOs() ? 10 : 1, a = -e.sourceEvent.deltaY * (e.sourceEvent.deltaMode === 1 ? .05 : e.sourceEvent.deltaMode ? 1 : .002) * c, o = r[2] * 2 ** (a * i);
			t.scaleTo(o);
		}, f = [0, 0], panStartHandler = (e) => {
			(e.sourceEvent.type === "mousedown" || e.sourceEvent.type === "touchstart") && (f = [e.sourceEvent.clientX ?? e.sourceEvent.touches[0].clientX, e.sourceEvent.clientY ?? e.sourceEvent.touches[0].clientY]);
		}, panHandler = (a) => {
			let o = n();
			if (a.sourceEvent.type !== "mousemove" && a.sourceEvent.type !== "touchmove" || !t) return;
			let c = [a.sourceEvent.clientX ?? a.sourceEvent.touches[0].clientX, a.sourceEvent.clientY ?? a.sourceEvent.touches[0].clientY], l = [c[0] - f[0], c[1] - f[1]];
			f = c;
			let u = r() * Math.max(o[2], Math.log(o[2])) * (d ? -1 : 1), p = {
				x: o[0] - l[0] * u,
				y: o[1] - l[1] * u
			}, m = [[0, 0], [i, s]];
			t.setViewportConstrained({
				x: p.x,
				y: p.y,
				zoom: o[2]
			}, m, e);
		}, p = o().on("start", panStartHandler).on("zoom", l ? panHandler : null).on("zoom.wheel", u ? zoomHandler : null);
		a.call(p, {});
	}
	function destroy() {
		a.on("zoom", null);
	}
	return {
		update,
		destroy,
		pointer: i
	};
}
var transformToViewport = (e) => ({
	x: e.x,
	y: e.y,
	zoom: e.k
}), viewportToTransform = ({ x: e, y: t, zoom: n }) => r.translate(e, t).scale(n), isWrappedWithClass = (e, t) => e.target.closest(`.${t}`), isRightClickPan = (e, t) => t === 2 && Array.isArray(e) && e.includes(2), defaultEase = (e) => ((e *= 2) <= 1 ? e * e * e : (e -= 2) * e * e + 2) / 2, getD3Transition = (e, t = 0, n = defaultEase, r = () => {}) => {
	let i = typeof t == "number" && t > 0;
	return i || r(), i ? e.transition().duration(t).ease(n).on("end", r) : e;
}, wheelDelta = (e) => {
	let t = e.ctrlKey && isMacOs() ? 10 : 1;
	return -e.deltaY * (e.deltaMode === 1 ? .05 : e.deltaMode ? 1 : .002) * t;
};
function createPanOnScrollHandler({ zoomPanValues: e, noWheelClassName: t, d3Selection: n, d3Zoom: r, panOnScrollMode: a, panOnScrollSpeed: o, zoomOnPinch: s, onPanZoomStart: c, onPanZoom: l, onPanZoomEnd: u }) {
	return (d) => {
		if (isWrappedWithClass(d, t)) return d.ctrlKey && d.preventDefault(), !1;
		d.preventDefault(), d.stopImmediatePropagation();
		let f = n.property("__zoom").k || 1;
		if (d.ctrlKey && s) {
			let e = i(d), t = f * 2 ** wheelDelta(d);
			r.scaleTo(n, t, e, d);
			return;
		}
		let p = d.deltaMode === 1 ? 20 : 1, m = a === A.Vertical ? 0 : d.deltaX * p, h = a === A.Horizontal ? 0 : d.deltaY * p;
		!isMacOs() && d.shiftKey && a !== A.Vertical && (m = d.deltaY * p, h = 0), r.translateBy(n, -(m / f) * o, -(h / f) * o, { internal: !0 });
		let g = transformToViewport(n.property("__zoom"));
		clearTimeout(e.panScrollTimeout), e.isPanScrolling ? l?.(d, g) : (e.isPanScrolling = !0, c?.(d, g)), e.panScrollTimeout = setTimeout(() => {
			u?.(d, g), e.isPanScrolling = !1;
		}, 150);
	};
}
function createZoomOnScrollHandler({ noWheelClassName: e, preventScrolling: t, d3ZoomHandler: n }) {
	return function(r, i) {
		let a = r.type === "wheel", o = !t && a && !r.ctrlKey, s = isWrappedWithClass(r, e);
		if (r.ctrlKey && a && s && r.preventDefault(), o || s) return null;
		r.preventDefault(), n.call(this, r, i);
	};
}
function createPanZoomStartHandler({ zoomPanValues: e, onDraggingChange: t, onPanZoomStart: n }) {
	return (r) => {
		if (r.sourceEvent?.internal) return;
		let i = transformToViewport(r.transform);
		e.mouseButton = r.sourceEvent?.button || 0, e.isZoomingOrPanning = !0, e.prevViewport = i, r.sourceEvent?.type === "mousedown" && t(!0), n && n?.(r.sourceEvent, i);
	};
}
function createPanZoomHandler({ zoomPanValues: e, panOnDrag: t, onPaneContextMenu: n, onTransformChange: r, onPanZoom: i }) {
	return (a) => {
		e.usedRightMouseButton = !!(n && isRightClickPan(t, e.mouseButton ?? 0)), a.sourceEvent?.sync || r([
			a.transform.x,
			a.transform.y,
			a.transform.k
		]), i && !a.sourceEvent?.internal && i?.(a.sourceEvent, transformToViewport(a.transform));
	};
}
function createPanZoomEndHandler({ zoomPanValues: e, panOnDrag: t, panOnScroll: n, onDraggingChange: r, onPanZoomEnd: i, onPaneContextMenu: a }) {
	return (o) => {
		if (!o.sourceEvent?.internal && (e.isZoomingOrPanning = !1, a && isRightClickPan(t, e.mouseButton ?? 0) && !e.usedRightMouseButton && o.sourceEvent && a(o.sourceEvent), e.usedRightMouseButton = !1, r(!1), i)) {
			let t = transformToViewport(o.transform);
			e.prevViewport = t, clearTimeout(e.timerId), e.timerId = setTimeout(() => {
				i?.(o.sourceEvent, t);
			}, n ? 150 : 0);
		}
	};
}
function createFilter({ panActivationKeyPressed: e, zoomActivationKeyPressed: t, zoomOnScroll: n, zoomOnPinch: r, panOnDrag: i, panOnScroll: a, zoomOnDoubleClick: o, userSelectionActive: s, noWheelClassName: c, noPanClassName: l, lib: u, connectionInProgress: d }) {
	return (f) => {
		let p = t || n, m = r && f.ctrlKey, h = f.type === "wheel";
		if (f.button === 1 && f.type === "mousedown" && (isWrappedWithClass(f, `${u}-flow__node`) || isWrappedWithClass(f, `${u}-flow__edge`) || isWrappedWithClass(f, `${u}-flow__selection`) || isWrappedWithClass(f, `${u}-flow__nodesselection`))) return !0;
		if (!i && !p && !a && !o && !r || s || d && !h || isWrappedWithClass(f, c) && h || isWrappedWithClass(f, l) && (!h || a && h && !t) || !r && f.ctrlKey && h) return !1;
		if (!r && f.type === "touchstart" && f.touches?.length > 1) return f.preventDefault(), !1;
		if (!p && !a && !m && h || !i && (f.type === "mousedown" || f.type === "touchstart") || Array.isArray(i) && !i.includes(f.button) && f.type === "mousedown") return !1;
		let g = Array.isArray(i) && i.includes(f.button) || !f.button || f.button <= 1;
		return (!f.ctrlKey || h || e) && g;
	};
}
function XYPanZoom({ domNode: e, minZoom: n, maxZoom: r, translateExtent: i, viewport: l, onPanZoom: u, onPanZoomStart: d, onPanZoomEnd: f, onDraggingChange: p }) {
	let m = {
		isZoomingOrPanning: !1,
		usedRightMouseButton: !1,
		prevViewport: {},
		mouseButton: 0,
		timerId: void 0,
		panScrollTimeout: void 0,
		isPanScrolling: !1
	}, h = e.getBoundingClientRect(), g = [[0, 0], [h.width, h.height]];
	(typeof ResizeObserver < "u" ? new ResizeObserver((e) => {
		let t = e[0];
		t && (g = [[0, 0], [t.contentRect.width, t.contentRect.height]]);
	}) : null)?.observe(e);
	let _ = o().extent(() => g).scaleExtent([n, r]).translateExtent(i), v = c(e).call(_);
	setViewportConstrained({
		x: l.x,
		y: l.y,
		zoom: clamp(l.zoom, n, r)
	}, [[0, 0], [h.width, h.height]], i);
	let y = v.on("wheel.zoom"), b = v.on("dblclick.zoom");
	_.wheelDelta(wheelDelta);
	async function setTransform(e, t) {
		return v ? new Promise((n) => {
			_?.interpolate(t?.interpolate === "linear" ? s : a).transform(getD3Transition(v, t?.duration, t?.ease, () => n(!0)), e);
		}) : !1;
	}
	function update({ noWheelClassName: e, noPanClassName: t, onPaneContextMenu: n, userSelectionActive: r, panOnScroll: i, panOnDrag: a, panOnScrollMode: o, panOnScrollSpeed: s, preventScrolling: c, zoomOnPinch: l, zoomOnScroll: h, zoomOnDoubleClick: g, panActivationKeyPressed: x = !1, zoomActivationKeyPressed: S, lib: C, onTransformChange: w, connectionInProgress: T, paneClickDistance: E, selectionOnDrag: D }) {
		r && !m.isZoomingOrPanning && destroy();
		let O = i && !S && !r;
		_.clickDistance(D ? Infinity : !isNumeric(E) || E < 0 ? 0 : E);
		let k = O ? createPanOnScrollHandler({
			zoomPanValues: m,
			noWheelClassName: e,
			d3Selection: v,
			d3Zoom: _,
			panOnScrollMode: o,
			panOnScrollSpeed: s,
			zoomOnPinch: l,
			onPanZoomStart: d,
			onPanZoom: u,
			onPanZoomEnd: f
		}) : createZoomOnScrollHandler({
			noWheelClassName: e,
			preventScrolling: c,
			d3ZoomHandler: y
		});
		v.on("wheel.zoom", k, { passive: !1 });
		let A = createPanZoomStartHandler({
			zoomPanValues: m,
			onDraggingChange: p,
			onPanZoomStart: d
		});
		_.on("start", A);
		let j = createPanZoomHandler({
			zoomPanValues: m,
			panOnDrag: a,
			onPaneContextMenu: !!n,
			onPanZoom: u,
			onTransformChange: w
		});
		_.on("zoom", j);
		let M = createPanZoomEndHandler({
			zoomPanValues: m,
			panOnDrag: a,
			panOnScroll: i,
			onPaneContextMenu: n,
			onPanZoomEnd: f,
			onDraggingChange: p
		});
		_.on("end", M);
		let N = createFilter({
			panActivationKeyPressed: x,
			zoomActivationKeyPressed: S,
			panOnDrag: a,
			zoomOnScroll: h,
			panOnScroll: i,
			zoomOnDoubleClick: g,
			zoomOnPinch: l,
			userSelectionActive: r,
			noPanClassName: t,
			noWheelClassName: e,
			lib: C,
			connectionInProgress: T
		});
		_.filter(N), g ? v.on("dblclick.zoom", b) : v.on("dblclick.zoom", null);
	}
	function destroy() {
		_.on("zoom", null);
	}
	async function setViewportConstrained(e, t, n) {
		let r = viewportToTransform(e), i = _?.constrain()(r, t, n);
		return i && await setTransform(i), i;
	}
	async function setViewport(e, t) {
		let n = viewportToTransform(e);
		return await setTransform(n, t), n;
	}
	function syncViewport(e) {
		if (v) {
			let t = viewportToTransform(e), n = v.property("__zoom");
			(n.k !== e.zoom || n.x !== e.x || n.y !== e.y) && _?.transform(v, t, null, { sync: !0 });
		}
	}
	function getViewport() {
		let e = v ? t(v.node()) : {
			x: 0,
			y: 0,
			k: 1
		};
		return {
			x: e.x,
			y: e.y,
			zoom: e.k
		};
	}
	async function scaleTo(e, t) {
		return v ? new Promise((n) => {
			_?.interpolate(t?.interpolate === "linear" ? s : a).scaleTo(getD3Transition(v, t?.duration, t?.ease, () => n(!0)), e);
		}) : !1;
	}
	async function scaleBy(e, t) {
		return v ? new Promise((n) => {
			_?.interpolate(t?.interpolate === "linear" ? s : a).scaleBy(getD3Transition(v, t?.duration, t?.ease, () => n(!0)), e);
		}) : !1;
	}
	function setScaleExtent(e) {
		_?.scaleExtent(e);
	}
	function setTranslateExtent(e) {
		_?.translateExtent(e);
	}
	function setClickDistance(e) {
		let t = !isNumeric(e) || e < 0 ? 0 : e;
		_?.clickDistance(t);
	}
	return {
		update,
		destroy,
		setViewport,
		setViewportConstrained,
		getViewport,
		scaleTo,
		scaleBy,
		setScaleExtent,
		setTranslateExtent,
		syncViewport,
		setClickDistance
	};
}
var K;
(function(e) {
	e.Line = "line", e.Handle = "handle";
})(K ||= {});
function getResizeDirection({ width: e, prevWidth: t, height: n, prevHeight: r, affectsX: i, affectsY: a }) {
	let o = e - t, s = n - r, c = [o > 0 ? 1 : o < 0 ? -1 : 0, s > 0 ? 1 : s < 0 ? -1 : 0];
	return o && i && (c[0] *= -1), s && a && (c[1] *= -1), c;
}
function getControlDirection(e) {
	return {
		isHorizontal: e.includes("right") || e.includes("left"),
		isVertical: e.includes("bottom") || e.includes("top"),
		affectsX: e.includes("left"),
		affectsY: e.includes("top")
	};
}
function getLowerExtentClamp(e, t) {
	return Math.max(0, t - e);
}
function getUpperExtentClamp(e, t) {
	return Math.max(0, e - t);
}
function getSizeClamp(e, t, n) {
	return Math.max(0, t - e, e - n);
}
function xor(e, t) {
	return e ? !t : t;
}
function getDimensionsAfterResize(e, t, n, r, i, a, o, s) {
	let { affectsX: c, affectsY: l } = t, { isHorizontal: u, isVertical: d } = t, f = u && d, { xSnapped: p, ySnapped: m } = n, { minWidth: h, maxWidth: g, minHeight: _, maxHeight: v } = r, { x: y, y: b, width: x, height: S, aspectRatio: C } = e, w = Math.floor(u ? p - e.pointerX : 0), T = Math.floor(d ? m - e.pointerY : 0), E = x + (c ? -w : w), D = S + (l ? -T : T), O = -a[0] * x, k = -a[1] * S, A = getSizeClamp(E, h, g), j = getSizeClamp(D, _, v);
	if (o) {
		let e = 0, t = 0;
		c && w < 0 ? e = getLowerExtentClamp(y + w + O, o[0][0]) : !c && w > 0 && (e = getUpperExtentClamp(y + E + O, o[1][0])), l && T < 0 ? t = getLowerExtentClamp(b + T + k, o[0][1]) : !l && T > 0 && (t = getUpperExtentClamp(b + D + k, o[1][1])), A = Math.max(A, e), j = Math.max(j, t);
	}
	if (s) {
		let e = 0, t = 0;
		c && w > 0 ? e = getUpperExtentClamp(y + w, s[0][0]) : !c && w < 0 && (e = getLowerExtentClamp(y + E, s[1][0])), l && T > 0 ? t = getUpperExtentClamp(b + T, s[0][1]) : !l && T < 0 && (t = getLowerExtentClamp(b + D, s[1][1])), A = Math.max(A, e), j = Math.max(j, t);
	}
	if (i) {
		if (u) {
			let e = getSizeClamp(E / C, _, v) * C;
			if (A = Math.max(A, e), o) {
				let e = 0;
				e = !c && !l || c && !l && f ? getUpperExtentClamp(b + k + E / C, o[1][1]) * C : getLowerExtentClamp(b + k + (c ? w : -w) / C, o[0][1]) * C, A = Math.max(A, e);
			}
			if (s) {
				let e = 0;
				e = !c && !l || c && !l && f ? getLowerExtentClamp(b + E / C, s[1][1]) * C : getUpperExtentClamp(b + (c ? w : -w) / C, s[0][1]) * C, A = Math.max(A, e);
			}
		}
		if (d) {
			let e = getSizeClamp(D * C, h, g) / C;
			if (j = Math.max(j, e), o) {
				let e = 0;
				e = !c && !l || l && !c && f ? getUpperExtentClamp(y + D * C + O, o[1][0]) / C : getLowerExtentClamp(y + (l ? T : -T) * C + O, o[0][0]) / C, j = Math.max(j, e);
			}
			if (s) {
				let e = 0;
				e = !c && !l || l && !c && f ? getLowerExtentClamp(y + D * C, s[1][0]) / C : getUpperExtentClamp(y + (l ? T : -T) * C, s[0][0]) / C, j = Math.max(j, e);
			}
		}
	}
	T += T < 0 ? j : -j, w += w < 0 ? A : -A, i && (f ? E > D * C ? T = (xor(c, l) ? -w : w) / C : w = (xor(c, l) ? -T : T) * C : u ? (T = w / C, l = c) : (w = T * C, c = l));
	let M = c ? y + w : y, N = l ? b + T : b;
	return {
		width: x + (c ? -w : w),
		height: S + (l ? -T : T),
		x: a[0] * w * (c ? -1 : 1) + M,
		y: a[1] * T * (l ? -1 : 1) + N
	};
}
var q = {
	width: 0,
	height: 0,
	x: 0,
	y: 0
}, J = {
	...q,
	pointerX: 0,
	pointerY: 0,
	aspectRatio: 1
};
function nodeToChildExtent(e, t, n) {
	let r = t.position.x + e.position.x, i = t.position.y + e.position.y, a = e.measured.width ?? 0, o = e.measured.height ?? 0, s = n[0] * a, c = n[1] * o;
	return [[r - s, i - c], [r + a - s, i + o - c]];
}
function XYResizer({ domNode: e, nodeId: t, getStoreItems: r, onChange: i, onEnd: a }) {
	let o = c(e), s = {
		controlDirection: getControlDirection("bottom-right"),
		boundaries: {
			minWidth: 0,
			minHeight: 0,
			maxWidth: Number.MAX_VALUE,
			maxHeight: Number.MAX_VALUE
		},
		resizeDirection: void 0,
		keepAspectRatio: !1
	};
	function update({ controlPosition: e, boundaries: c, keepAspectRatio: l, resizeDirection: u, onResizeStart: d, onResize: f, onResizeEnd: p, shouldResize: m }) {
		let h = { ...q }, g = { ...J };
		s = {
			boundaries: c,
			resizeDirection: u,
			keepAspectRatio: l,
			controlDirection: getControlDirection(e)
		};
		let _, v = null, y = [], b, x, S, C = !1, w = n().on("start", (e) => {
			let { nodeLookup: n, transform: i, snapGrid: a, snapToGrid: o, nodeOrigin: s, paneDomNode: c } = r();
			if (_ = n.get(t), !_) return;
			v = c?.getBoundingClientRect() ?? null;
			let { xSnapped: l, ySnapped: u } = getPointerPosition(e.sourceEvent, {
				transform: i,
				snapGrid: a,
				snapToGrid: o,
				containerBounds: v
			});
			h = {
				width: _.measured.width ?? 0,
				height: _.measured.height ?? 0,
				x: _.position.x ?? 0,
				y: _.position.y ?? 0
			}, g = {
				...h,
				pointerX: l,
				pointerY: u,
				aspectRatio: h.width / h.height
			}, b = void 0, x = isCoordinateExtent(_.extent) ? _.extent : void 0, _.parentId && (_.extent === "parent" || _.expandParent) && (b = n.get(_.parentId)), b && _.extent === "parent" && (x = [[0, 0], [b.measured.width, b.measured.height]]), y = [], S = void 0;
			for (let [e, r] of n) if (r.parentId === t && (y.push({
				id: e,
				position: { ...r.position },
				extent: r.extent
			}), r.extent === "parent" || r.expandParent)) {
				let e = nodeToChildExtent(r, _, r.origin ?? s);
				S = S ? [[Math.min(e[0][0], S[0][0]), Math.min(e[0][1], S[0][1])], [Math.max(e[1][0], S[1][0]), Math.max(e[1][1], S[1][1])]] : e;
			}
			d?.(e, { ...h });
		}).on("drag", (e) => {
			let { transform: t, snapGrid: n, snapToGrid: a, nodeOrigin: o } = r(), c = getPointerPosition(e.sourceEvent, {
				transform: t,
				snapGrid: n,
				snapToGrid: a,
				containerBounds: v
			}), l = [];
			if (!_) return;
			let { x: u, y: d, width: p, height: w } = h, T = {}, E = _.origin ?? o, { width: D, height: O, x: k, y: A } = getDimensionsAfterResize(g, s.controlDirection, c, s.boundaries, s.keepAspectRatio, E, x, S), j = D !== p, M = O !== w, N = k !== u && j, P = A !== d && M;
			if (!N && !P && !j && !M) return;
			if ((N || P || E[0] === 1 || E[1] === 1) && (T.x = N ? k : h.x, T.y = P ? A : h.y, h.x = T.x, h.y = T.y, y.length > 0)) {
				let e = k - u, t = A - d;
				for (let n of y) n.position = {
					x: n.position.x - e + E[0] * (D - p),
					y: n.position.y - t + E[1] * (O - w)
				}, l.push(n);
			}
			if ((j || M) && (T.width = j && (!s.resizeDirection || s.resizeDirection === "horizontal") ? D : h.width, T.height = M && (!s.resizeDirection || s.resizeDirection === "vertical") ? O : h.height, h.width = T.width, h.height = T.height), b && _.expandParent) {
				let e = E[0] * (T.width ?? 0);
				T.x && T.x < e && (h.x = e, g.x -= T.x - e);
				let t = E[1] * (T.height ?? 0);
				T.y && T.y < t && (h.y = t, g.y -= T.y - t);
			}
			let F = getResizeDirection({
				width: h.width,
				prevWidth: p,
				height: h.height,
				prevHeight: w,
				affectsX: s.controlDirection.affectsX,
				affectsY: s.controlDirection.affectsY
			}), I = {
				...h,
				direction: F
			};
			m?.(e, I) !== !1 && (C = !0, f?.(e, I), i(T, l));
		}).on("end", (e) => {
			C &&= (p?.(e, { ...h }), a?.({ ...h }), !1);
		});
		o.call(w);
	}
	function destroy() {
		o.on(".drag", null);
	}
	return {
		update,
		destroy
	};
}
//#endregion
//#region ../../node_modules/.pnpm/zustand@4.5.7_@types+react@19.2.18_immer@11.1.18_react@19.2.8/node_modules/zustand/esm/vanilla.mjs
var createStoreImpl = (e) => {
	let t, n = /* @__PURE__ */ new Set(), setState = (e, r) => {
		let i = typeof e == "function" ? e(t) : e;
		if (!Object.is(i, t)) {
			let e = t;
			t = r ?? (typeof i != "object" || !i) ? i : Object.assign({}, t, i), n.forEach((n) => n(t, e));
		}
	}, getState = () => t, getInitialState = () => i, subscribe = (e) => (n.add(e), () => n.delete(e)), destroy = () => {
		n.clear();
	}, r = {
		setState,
		getState,
		getInitialState,
		subscribe,
		destroy
	}, i = t = e(setState, getState, r);
	return r;
}, ee = /* @__PURE__ */ e((e) => e ? createStoreImpl(e) : createStoreImpl, "createStore"), { useDebugValue: Y } = l, { useSyncExternalStoreWithSelector: te } = C, identity = (e) => e;
function useStoreWithEqualityFn(e, t = identity, n) {
	let r = te(e.subscribe, e.getState, e.getServerState || e.getInitialState, t, n);
	return Y(r), r;
}
var createWithEqualityFnImpl = (e, t) => {
	let n = ee(e), useBoundStoreWithEqualityFn = (e, r = t) => useStoreWithEqualityFn(n, e, r);
	return Object.assign(useBoundStoreWithEqualityFn, n), useBoundStoreWithEqualityFn;
}, createWithEqualityFn = (e, t) => e ? createWithEqualityFnImpl(e, t) : createWithEqualityFnImpl;
//#endregion
//#region ../../node_modules/.pnpm/zustand@4.5.7_@types+react@19.2.18_immer@11.1.18_react@19.2.8/node_modules/zustand/esm/shallow.mjs
function shallow$1(e, t) {
	if (Object.is(e, t)) return !0;
	if (typeof e != "object" || !e || typeof t != "object" || !t) return !1;
	if (e instanceof Map && t instanceof Map) {
		if (e.size !== t.size) return !1;
		for (let [n, r] of e) if (!Object.is(r, t.get(n))) return !1;
		return !0;
	}
	if (e instanceof Set && t instanceof Set) {
		if (e.size !== t.size) return !1;
		for (let n of e) if (!t.has(n)) return !1;
		return !0;
	}
	let n = Object.keys(e);
	if (n.length !== Object.keys(t).length) return !1;
	for (let r of n) if (!Object.prototype.hasOwnProperty.call(t, r) || !Object.is(e[r], t[r])) return !1;
	return !0;
}
//#endregion
//#region ../../node_modules/.pnpm/@xyflow+react@12.11.6_@types+react-dom@19.2.5_@types+react@19.2.18__@types+react@19.2.1_9d4149e5c73352343a0f166391aa9cf0/node_modules/@xyflow/react/dist/esm/index.js
var X = u(null), ne = X.Provider, re = T.error001("react");
function useStore(e, t) {
	let n = m(X);
	if (n === null) throw Error(re);
	return useStoreWithEqualityFn(n, e, t);
}
function useStoreApi() {
	let e = m(X);
	if (e === null) throw Error(re);
	return _(() => ({
		getState: e.getState,
		setState: e.setState,
		subscribe: e.subscribe
	}), [e]);
}
var ie = { display: "none" }, ae = {
	position: "absolute",
	width: 1,
	height: 1,
	margin: -1,
	border: 0,
	padding: 0,
	overflow: "hidden",
	clip: "rect(0px, 0px, 0px, 0px)",
	clipPath: "inset(100%)"
}, oe = "react-flow__node-desc", se = "react-flow__edge-desc", ce = "react-flow__aria-live", ariaLiveSelector = (e) => e.ariaLiveMessage, ariaLabelConfigSelector = (e) => e.ariaLabelConfig;
function AriaLiveMessage({ rfId: e }) {
	let t = useStore(ariaLiveSelector);
	return x("div", {
		id: `${ce}-${e}`,
		"aria-live": "assertive",
		"aria-atomic": "true",
		style: ae,
		children: t
	});
}
function A11yDescriptions({ rfId: e, disableKeyboardA11y: t }) {
	let n = useStore(ariaLabelConfigSelector);
	return S(b, { children: [
		x("div", {
			id: `${oe}-${e}`,
			style: ie,
			children: t ? n["node.a11yDescription.default"] : n["node.a11yDescription.keyboardDisabled"]
		}),
		x("div", {
			id: `${se}-${e}`,
			style: ie,
			children: n["edge.a11yDescription.default"]
		}),
		!t && x(AriaLiveMessage, { rfId: e })
	] });
}
var Z = d(({ position: e = "top-left", children: t, className: n, style: r, ...i }, a) => {
	let o = `${e}`.split("-");
	return x("div", {
		className: cc([
			"react-flow__panel",
			n,
			...o
		]),
		style: r,
		ref: a,
		...i,
		children: t
	});
});
Z.displayName = "Panel";
var le = "https://reactflow.dev?utm_source=attribution";
function Attribution({ proOptions: e, position: t = "bottom-right" }) {
	return h(() => {}, []), e?.hideAttribution ? null : x(Z, {
		position: t,
		className: "react-flow__attribution",
		"data-message": `Please only hide this attribution when you are subscribed to React Flow Pro: ${le}`,
		children: x("a", {
			href: le,
			target: "_blank",
			rel: "noopener noreferrer",
			"aria-label": "React Flow attribution",
			children: "React Flow"
		})
	});
}
var selector$l = (e) => {
	let t = [], n = [];
	for (let [, n] of e.nodeLookup) n.selected && t.push(n.internals.userNode);
	for (let [, t] of e.edgeLookup) t.selected && n.push(t);
	return {
		selectedNodes: t,
		selectedEdges: n
	};
}, selectId = (e) => e.id;
function areEqual$1(e, t) {
	return shallow$1(e.selectedNodes.map(selectId), t.selectedNodes.map(selectId)) && shallow$1(e.selectedEdges.map(selectId), t.selectedEdges.map(selectId));
}
function SelectionListenerInner({ onSelectionChange: e }) {
	let t = useStoreApi(), { selectedNodes: n, selectedEdges: r } = useStore(selector$l, areEqual$1);
	return h(() => {
		let i = {
			nodes: n,
			edges: r
		};
		e?.(i), t.getState().onSelectionChangeHandlers.forEach((e) => e(i));
	}, [
		n,
		r,
		e
	]), null;
}
var changeSelector = (e) => !!e.onSelectionChangeHandlers;
function SelectionListener({ onSelectionChange: e }) {
	let t = useStore(changeSelector);
	return e || t ? x(SelectionListenerInner, { onSelectionChange: e }) : null;
}
var ue = [0, 0], de = {
	x: 0,
	y: 0,
	zoom: 1
}, fe = [.../* @__PURE__ */ "nodes.edges.defaultNodes.defaultEdges.onConnect.onConnectStart.onConnectEnd.onClickConnectStart.onClickConnectEnd.nodesDraggable.autoPanOnNodeFocus.nodesConnectable.nodesFocusable.edgesFocusable.edgesReconnectable.elevateNodesOnSelect.elevateEdgesOnSelect.minZoom.maxZoom.nodeExtent.onNodesChange.onEdgesChange.elementsSelectable.connectionMode.snapGrid.snapToGrid.translateExtent.connectOnClick.defaultEdgeOptions.fitView.fitViewOptions.onNodesDelete.onEdgesDelete.onDelete.onNodeDrag.onNodeDragStart.onNodeDragStop.onSelectionDrag.onSelectionDragStart.onSelectionDragStop.onMoveStart.onMove.onMoveEnd.noPanClassName.nodeOrigin.autoPanOnConnect.autoPanOnNodeDrag.onError.connectionRadius.isValidConnection.selectNodesOnDrag.nodeDragThreshold.connectionDragThreshold.onBeforeDelete.debug.autoPanSpeed.ariaLabelConfig.zIndexMode".split("."), "rfId"], selector$k = (e) => ({
	setNodes: e.setNodes,
	setEdges: e.setEdges,
	setMinZoom: e.setMinZoom,
	setMaxZoom: e.setMaxZoom,
	setTranslateExtent: e.setTranslateExtent,
	setNodeExtent: e.setNodeExtent,
	reset: e.reset,
	setDefaultNodesAndEdges: e.setDefaultNodesAndEdges
}), pe = {
	translateExtent: E,
	nodeOrigin: ue,
	minZoom: .5,
	maxZoom: 2,
	elementsSelectable: !0,
	noPanClassName: "nopan",
	rfId: "1"
};
function StoreUpdater(e) {
	let { setNodes: t, setEdges: n, setMinZoom: r, setMaxZoom: i, setTranslateExtent: a, setNodeExtent: o, reset: s, setDefaultNodesAndEdges: c } = useStore(selector$k, shallow$1), l = useStoreApi();
	h(() => (c(e.defaultNodes, e.defaultEdges), () => {
		u.current = pe, s();
	}), []);
	let u = v(pe);
	return h(() => {
		for (let s of fe) {
			let c = e[s];
			c !== u.current[s] && e[s] !== void 0 && (s === "nodes" ? t(c) : s === "edges" ? n(c) : s === "minZoom" ? r(c) : s === "maxZoom" ? i(c) : s === "translateExtent" ? a(c) : s === "nodeExtent" ? o(c) : s === "ariaLabelConfig" ? l.setState({ ariaLabelConfig: mergeAriaLabelConfig(c) }) : s === "fitView" ? l.setState({ fitViewQueued: c }) : s === "fitViewOptions" ? l.setState({ fitViewOptions: c }) : l.setState({ [s]: c }));
		}
		u.current = e;
	}, fe.map((t) => e[t])), null;
}
function getMediaQuery() {
	return typeof window > "u" || !window.matchMedia ? null : window.matchMedia("(prefers-color-scheme: dark)");
}
function useColorModeClass(e) {
	let [t, n] = y(e === "system" ? null : e);
	return h(() => {
		if (e !== "system") {
			n(e);
			return;
		}
		let t = getMediaQuery(), updateColorModeClass = () => n(t?.matches ? "dark" : "light");
		return updateColorModeClass(), t?.addEventListener("change", updateColorModeClass), () => {
			t?.removeEventListener("change", updateColorModeClass);
		};
	}, [e]), t === null ? getMediaQuery()?.matches ? "dark" : "light" : t;
}
var me = typeof document < "u" ? document : null;
function useKeyPress(e = null, t = {
	target: me,
	actInsideInputWithModifier: !0
}) {
	let [n, r] = y(!1), i = v(!1), a = v(/* @__PURE__ */ new Set([])), [o, s] = _(() => {
		if (e !== null) {
			let t = (Array.isArray(e) ? e : [e]).filter((e) => typeof e == "string").map((e) => e.replace(/\+/g, "\n").replace("\n\n", "\n+").split("\n"));
			return [t, t.reduce((e, t) => e.concat(...t), [])];
		}
		return [[], []];
	}, [e]);
	return h(() => {
		let n = t?.target ?? me, c = t?.actInsideInputWithModifier ?? !0;
		if (e !== null) {
			let downHandler = (e) => {
				if (i.current = e.ctrlKey || e.metaKey || e.shiftKey || e.altKey, (!i.current || i.current && !c) && isInputDOMNode(e)) return !1;
				let n = useKeyOrCode(e.code, s);
				if (a.current.add(e[n]), isMatchingKey(o, a.current, !1)) {
					let n = e.composedPath?.()?.[0] || e.target, a = n?.nodeName === "BUTTON" || n?.nodeName === "A";
					t.preventDefault !== !1 && (i.current || !a) && e.preventDefault(), r(!0);
				}
			}, upHandler = (e) => {
				let t = useKeyOrCode(e.code, s);
				isMatchingKey(o, a.current, !0) ? (r(!1), a.current.clear()) : a.current.delete(e[t]), e.key === "Meta" && a.current.clear(), i.current = !1;
			}, resetHandler = () => {
				a.current.clear(), r(!1);
			};
			return n?.addEventListener("keydown", downHandler), n?.addEventListener("keyup", upHandler), window.addEventListener("blur", resetHandler), window.addEventListener("contextmenu", resetHandler), () => {
				n?.removeEventListener("keydown", downHandler), n?.removeEventListener("keyup", upHandler), window.removeEventListener("blur", resetHandler), window.removeEventListener("contextmenu", resetHandler);
			};
		}
	}, [e, r]), n;
}
function isMatchingKey(e, t, n) {
	return e.filter((e) => n || e.length === t.size).some((e) => e.every((e) => t.has(e)));
}
function useKeyOrCode(e, t) {
	return t.includes(e) ? "code" : "key";
}
var useViewportHelper = () => {
	let e = useStoreApi();
	return _(() => ({
		zoomIn: async (t) => {
			let { panZoom: n } = e.getState();
			return n ? n.scaleBy(1.2, t) : !1;
		},
		zoomOut: async (t) => {
			let { panZoom: n } = e.getState();
			return n ? n.scaleBy(1 / 1.2, t) : !1;
		},
		zoomTo: async (t, n) => {
			let { panZoom: r } = e.getState();
			return r ? r.scaleTo(t, n) : !1;
		},
		getZoom: () => e.getState().transform[2],
		setViewport: async (t, n) => {
			let { transform: [r, i, a], panZoom: o } = e.getState();
			return o ? (await o.setViewport({
				x: t.x ?? r,
				y: t.y ?? i,
				zoom: t.zoom ?? a
			}, n), !0) : !1;
		},
		getViewport: () => {
			let [t, n, r] = e.getState().transform;
			return {
				x: t,
				y: n,
				zoom: r
			};
		},
		setCenter: async (t, n, r) => e.getState().setCenter(t, n, r),
		fitBounds: async (t, n) => {
			let { width: r, height: i, minZoom: a, maxZoom: o, panZoom: s } = e.getState(), c = getViewportForBounds(t, r, i, a, o, n?.padding ?? .1);
			return s ? (await s.setViewport(c, {
				duration: n?.duration,
				ease: n?.ease,
				interpolate: n?.interpolate
			}), !0) : !1;
		},
		screenToFlowPosition: (t, n = {}) => {
			let { transform: r, snapGrid: i, snapToGrid: a, domNode: o } = e.getState();
			if (!o) return t;
			let { x: s, y: c } = o.getBoundingClientRect(), l = {
				x: t.x - s,
				y: t.y - c
			}, u = n.snapGrid ?? i;
			return pointToRendererPoint(l, r, n.snapToGrid ?? a, u);
		},
		flowToScreenPosition: (t) => {
			let { transform: n, domNode: r } = e.getState();
			if (!r) return t;
			let { x: i, y: a } = r.getBoundingClientRect(), o = rendererPointToPoint(t, n);
			return {
				x: o.x + i,
				y: o.y + a
			};
		}
	}), []);
};
function applyChanges(e, t) {
	let n = [], r = /* @__PURE__ */ new Map(), i = [];
	for (let t of e) if (t.type === "add") {
		i.push(t);
		continue;
	} else if (t.type === "remove" || t.type === "replace") r.set(t.id, [t]);
	else {
		let e = r.get(t.id);
		e ? e.push(t) : r.set(t.id, [t]);
	}
	for (let e of t) {
		let t = r.get(e.id);
		if (!t) {
			n.push(e);
			continue;
		}
		if (t[0].type === "remove") continue;
		if (t[0].type === "replace") {
			n.push({ ...t[0].item });
			continue;
		}
		let i = { ...e };
		for (let e of t) applyChange(e, i);
		n.push(i);
	}
	return i.length && i.forEach((e) => {
		e.index === void 0 ? n.push({ ...e.item }) : n.splice(e.index, 0, { ...e.item });
	}), n;
}
function applyChange(e, t) {
	switch (e.type) {
		case "select":
			t.selected = e.selected;
			break;
		case "position":
			e.position !== void 0 && (t.position = e.position), e.dragging !== void 0 && (t.dragging = e.dragging);
			break;
		case "dimensions": e.dimensions !== void 0 && (t.measured = { ...e.dimensions }, e.setAttributes && ((e.setAttributes === !0 || e.setAttributes === "width") && (t.width = e.dimensions.width), (e.setAttributes === !0 || e.setAttributes === "height") && (t.height = e.dimensions.height))), typeof e.resizing == "boolean" && (t.resizing = e.resizing);
	}
}
function applyNodeChanges(e, t) {
	return applyChanges(e, t);
}
function applyEdgeChanges(e, t) {
	return applyChanges(e, t);
}
function createSelectionChange(e, t) {
	return {
		id: e,
		type: "select",
		selected: t
	};
}
function getSelectionChanges(e, t = /* @__PURE__ */ new Set(), n = !1) {
	let r = [];
	for (let [i, a] of e) {
		let e = t.has(i);
		!(a.selected === void 0 && !e) && a.selected !== e && (n && (a.selected = e), r.push(createSelectionChange(a.id, e)));
	}
	return r;
}
function getElementsDiffChanges({ items: e = [], lookup: t }) {
	let n = [], r = new Map(e.map((e) => [e.id, e]));
	for (let [r, i] of e.entries()) {
		let e = t.get(i.id), a = e?.internals?.userNode ?? e;
		a !== void 0 && a !== i && n.push({
			id: i.id,
			item: i,
			type: "replace"
		}), a === void 0 && n.push({
			item: i,
			type: "add",
			index: r
		});
	}
	for (let [e] of t) r.get(e) === void 0 && n.push({
		id: e,
		type: "remove"
	});
	return n;
}
function elementToRemoveChange(e) {
	return {
		id: e.id,
		type: "remove"
	};
}
var he = createDevWarn("React Flow", "https://reactflow.dev/");
function addEdge(e, t, n = {}) {
	return R(e, t, {
		...n,
		onError: n.onError ?? he
	});
}
var isNode = (e) => isNodeBase(e), isEdge = (e) => isEdgeBase(e);
function fixedForwardRef(e) {
	return d(e);
}
var ge = typeof window < "u" ? g : h;
function useQueue(e) {
	let [t, n] = y(BigInt(0)), [r] = y(() => createQueue(() => n((e) => e + BigInt(1))));
	return ge(() => {
		let t = r.get();
		t.length && (e(t), r.reset());
	}, [t]), r;
}
function createQueue(e) {
	let t = [];
	return {
		get: () => t,
		reset: () => {
			t = [];
		},
		push: (n) => {
			t.push(n), e();
		}
	};
}
var _e = u(null);
function BatchProvider({ children: e }) {
	let t = useStoreApi(), n = useQueue(p((e) => {
		let { nodes: n = [], setNodes: r, hasDefaultNodes: i, onNodesChange: a, nodeLookup: o, fitViewQueued: s, onNodesChangeMiddlewareMap: c } = t.getState(), l = n;
		for (let t of e) l = typeof t == "function" ? t(l) : t;
		let u = getElementsDiffChanges({
			items: l,
			lookup: o
		});
		for (let e of c.values()) u = e(u);
		i && r(l), u.length > 0 ? a?.(u) : s && window.requestAnimationFrame(() => {
			let { fitViewQueued: e, nodes: n, setNodes: r } = t.getState();
			e && r(n);
		});
	}, [])), r = useQueue(p((e) => {
		let { edges: n = [], setEdges: r, hasDefaultEdges: i, onEdgesChange: a, edgeLookup: o } = t.getState(), s = n;
		for (let t of e) s = typeof t == "function" ? t(s) : t;
		i ? r(s) : a && a(getElementsDiffChanges({
			items: s,
			lookup: o
		}));
	}, [])), i = _(() => ({
		nodeQueue: n,
		edgeQueue: r
	}), []);
	return x(_e.Provider, {
		value: i,
		children: e
	});
}
function useBatchContext() {
	let e = m(_e);
	if (!e) throw Error("useBatchContext must be used within a BatchProvider");
	return e;
}
var selector$j = (e) => !!e.panZoom;
function useReactFlow() {
	let e = useViewportHelper(), t = useStoreApi(), n = useBatchContext(), r = useStore(selector$j), i = _(() => {
		let getInternalNode = (e) => t.getState().nodeLookup.get(e), setNodes = (e) => {
			n.nodeQueue.push(e);
		}, setEdges = (e) => {
			n.edgeQueue.push(e);
		}, getNodeRect = (e) => {
			let { nodeLookup: n, nodeOrigin: r } = t.getState(), i = isNode(e) ? e : n.get(e.id), a = i.parentId ? evaluateAbsolutePosition(i.position, i.measured, i.parentId, n, r) : i.position;
			return nodeToRect({
				...i,
				position: a,
				width: i.measured?.width ?? i.width,
				height: i.measured?.height ?? i.height
			});
		}, updateNode = (e, t, n = { replace: !1 }) => {
			setNodes((r) => r.map((r) => {
				if (r.id === e) {
					let e = typeof t == "function" ? t(r) : t;
					return n.replace && isNode(e) ? e : {
						...r,
						...e
					};
				}
				return r;
			}));
		}, updateEdge = (e, t, n = { replace: !1 }) => {
			setEdges((r) => r.map((r) => {
				if (r.id === e) {
					let e = typeof t == "function" ? t(r) : t;
					return n.replace && isEdge(e) ? e : {
						...r,
						...e
					};
				}
				return r;
			}));
		};
		return {
			getNodes: () => t.getState().nodes.map((e) => ({ ...e })),
			getNode: (e) => getInternalNode(e)?.internals.userNode,
			getInternalNode,
			getEdges: () => {
				let { edges: e = [] } = t.getState();
				return e.map((e) => ({ ...e }));
			},
			getEdge: (e) => t.getState().edgeLookup.get(e),
			setNodes,
			setEdges,
			addNodes: (e) => {
				let t = Array.isArray(e) ? e : [e];
				n.nodeQueue.push((e) => [...e, ...t]);
			},
			addEdges: (e) => {
				let t = Array.isArray(e) ? e : [e];
				n.edgeQueue.push((e) => [...e, ...t]);
			},
			toObject: () => {
				let { nodes: e = [], edges: n = [], transform: r } = t.getState(), [i, a, o] = r;
				return {
					nodes: e.map((e) => ({ ...e })),
					edges: n.map((e) => ({ ...e })),
					viewport: {
						x: i,
						y: a,
						zoom: o
					}
				};
			},
			deleteElements: async ({ nodes: e = [], edges: n = [] }) => {
				let { nodes: r, edges: i, onNodesDelete: a, onEdgesDelete: o, triggerNodeChanges: s, triggerEdgeChanges: c, onDelete: l, onBeforeDelete: u } = t.getState(), { nodes: d, edges: f } = await getElementsToRemove({
					nodesToRemove: e,
					edgesToRemove: n,
					nodes: r,
					edges: i,
					onBeforeDelete: u
				}), p = f.length > 0, m = d.length > 0;
				if (p) {
					let e = f.map(elementToRemoveChange);
					o?.(f), c(e);
				}
				if (m) {
					let e = d.map(elementToRemoveChange);
					a?.(d), s(e);
				}
				return (m || p) && l?.({
					nodes: d,
					edges: f
				}), {
					deletedNodes: d,
					deletedEdges: f
				};
			},
			getIntersectingNodes: (e, n = !0, r) => {
				let i = isRectObject(e), a = i ? e : getNodeRect(e), o = r !== void 0;
				return a ? (r || t.getState().nodes).filter((r) => {
					let s = t.getState().nodeLookup.get(r.id);
					if (s && !i && (r.id === e.id || !s.internals.positionAbsolute)) return !1;
					let c = nodeToRect(o ? r : s), l = getOverlappingArea(c, a);
					return n && l > 0 || l >= c.width * c.height || l >= a.width * a.height;
				}) : [];
			},
			isNodeIntersecting: (e, t, n = !0) => {
				let r = isRectObject(e) ? e : getNodeRect(e);
				if (!r) return !1;
				let i = getOverlappingArea(r, t);
				return n && i > 0 || i >= t.width * t.height || i >= r.width * r.height;
			},
			updateNode,
			updateNodeData: (e, t, n = { replace: !1 }) => {
				updateNode(e, (e) => {
					let r = typeof t == "function" ? t(e) : t;
					return n.replace ? {
						...e,
						data: r
					} : {
						...e,
						data: {
							...e.data,
							...r
						}
					};
				}, n);
			},
			updateEdge,
			updateEdgeData: (e, t, n = { replace: !1 }) => {
				updateEdge(e, (e) => {
					let r = typeof t == "function" ? t(e) : t;
					return n.replace ? {
						...e,
						data: r
					} : {
						...e,
						data: {
							...e.data,
							...r
						}
					};
				}, n);
			},
			getNodesBounds: (e) => {
				let { nodeLookup: n, nodeOrigin: r } = t.getState();
				return getNodesBounds(e, {
					nodeLookup: n,
					nodeOrigin: r
				});
			},
			getHandleConnections: ({ type: e, id: n, nodeId: r }) => Array.from(t.getState().connectionLookup.get(`${r}-${e}${n ? `-${n}` : ""}`)?.values() ?? []),
			getNodeConnections: ({ type: e, handleId: n, nodeId: r }) => Array.from(t.getState().connectionLookup.get(`${r}${e ? n ? `-${e}-${n}` : `-${e}` : ""}`)?.values() ?? []),
			fitView: async (e) => {
				let r = t.getState().fitViewResolver ?? withResolvers();
				return t.setState({
					fitViewQueued: !0,
					fitViewOptions: e,
					fitViewResolver: r
				}), n.nodeQueue.push((e) => [...e]), r.promise;
			}
		};
	}, []);
	return _(() => ({
		...i,
		...e,
		viewportInitialized: r
	}), [r]);
}
var selected = (e) => e.selected, ve = typeof window < "u" ? window : void 0;
function useGlobalKeyHandler({ deleteKeyCode: e, multiSelectionKeyCode: t }) {
	let n = useStoreApi(), { deleteElements: r } = useReactFlow(), i = useKeyPress(e, { actInsideInputWithModifier: !1 }), a = useKeyPress(t, { target: ve });
	h(() => {
		if (i) {
			let { edges: e, nodes: t } = n.getState();
			r({
				nodes: t.filter(selected),
				edges: e.filter(selected)
			}), n.setState({ nodesSelectionActive: !1 });
		}
	}, [i]), h(() => {
		n.setState({ multiSelectionActive: a });
	}, [a]);
}
function useResizeHandler(e) {
	let t = useStoreApi();
	h(() => {
		let updateDimensions = () => {
			if (!e.current || !(e.current.checkVisibility?.() ?? !0)) return !1;
			let n = getDimensions(e.current);
			(n.height === 0 || n.width === 0) && t.getState().onError?.("004", T.error004()), t.setState({
				width: n.width || 500,
				height: n.height || 500
			});
		};
		if (e.current) {
			updateDimensions(), window.addEventListener("resize", updateDimensions);
			let t = new ResizeObserver(() => updateDimensions());
			return t.observe(e.current), () => {
				window.removeEventListener("resize", updateDimensions), t && e.current && t.unobserve(e.current);
			};
		}
	}, []);
}
var Q = {
	position: "absolute",
	width: "100%",
	height: "100%",
	top: 0,
	left: 0
}, selector$i = (e) => ({
	userSelectionActive: e.userSelectionActive,
	lib: e.lib,
	connectionInProgress: e.connection.inProgress
});
function ZoomPane({ onPaneContextMenu: e, zoomOnScroll: t = !0, zoomOnPinch: n = !0, panOnScroll: r = !1, panActivationKeyPressed: i, panOnScrollSpeed: a = .5, panOnScrollMode: o = A.Free, zoomOnDoubleClick: s = !0, panOnDrag: c = !0, defaultViewport: l, translateExtent: u, minZoom: d, maxZoom: f, zoomActivationKeyCode: m, preventScrolling: g = !0, children: _, noWheelClassName: y, noPanClassName: b, onViewportChange: S, isControlledViewport: C, paneClickDistance: w, selectionOnDrag: T }) {
	let E = useStoreApi(), D = v(null), { userSelectionActive: O, lib: k, connectionInProgress: j } = useStore(selector$i, shallow$1), M = useKeyPress(m), N = v();
	useResizeHandler(D);
	let P = p((e) => {
		S?.({
			x: e[0],
			y: e[1],
			zoom: e[2]
		}), C || E.setState({ transform: e });
	}, [S, C]);
	return h(() => {
		if (D.current) {
			N.current = XYPanZoom({
				domNode: D.current,
				minZoom: d,
				maxZoom: f,
				translateExtent: u,
				viewport: l,
				onDraggingChange: (e) => E.setState((t) => t.paneDragging === e ? t : { paneDragging: e }),
				onPanZoomStart: (e, t) => {
					let { onViewportChangeStart: n, onMoveStart: r } = E.getState();
					r?.(e, t), n?.(t);
				},
				onPanZoom: (e, t) => {
					let { onViewportChange: n, onMove: r } = E.getState();
					r?.(e, t), n?.(t);
				},
				onPanZoomEnd: (e, t) => {
					let { onViewportChangeEnd: n, onMoveEnd: r } = E.getState();
					r?.(e, t), n?.(t);
				}
			});
			let { x: e, y: t, zoom: n } = N.current.getViewport();
			return E.setState({
				panZoom: N.current,
				transform: [
					e,
					t,
					n
				],
				domNode: D.current.closest(".react-flow")
			}), () => {
				N.current?.destroy();
			};
		}
	}, []), h(() => {
		N.current?.update({
			onPaneContextMenu: e,
			zoomOnScroll: t,
			zoomOnPinch: n,
			panOnScroll: r,
			panActivationKeyPressed: i,
			panOnScrollSpeed: a,
			panOnScrollMode: o,
			zoomOnDoubleClick: s,
			panOnDrag: c,
			zoomActivationKeyPressed: M,
			preventScrolling: g,
			noPanClassName: b,
			userSelectionActive: O,
			noWheelClassName: y,
			lib: k,
			onTransformChange: P,
			connectionInProgress: j,
			selectionOnDrag: T,
			paneClickDistance: w
		});
	}, [
		e,
		t,
		n,
		r,
		i,
		a,
		o,
		s,
		c,
		M,
		g,
		b,
		O,
		y,
		k,
		P,
		j,
		T,
		w
	]), x("div", {
		className: "react-flow__renderer",
		ref: D,
		style: Q,
		children: _
	});
}
var selector$h = (e) => ({
	userSelectionActive: e.userSelectionActive,
	userSelectionRect: e.userSelectionRect
});
function UserSelection() {
	let { userSelectionActive: e, userSelectionRect: t } = useStore(selector$h, shallow$1);
	return e && t ? x("div", {
		className: "react-flow__selection react-flow__container",
		style: {
			width: t.width,
			height: t.height,
			transform: `translate(${t.x}px, ${t.y}px)`
		}
	}) : null;
}
var wrapHandler = (e, t) => (n) => {
	n.target === t.current && e?.(n);
}, selector$g = (e) => ({
	userSelectionActive: e.userSelectionActive,
	elementsSelectable: e.elementsSelectable,
	dragging: e.paneDragging,
	panBy: e.panBy,
	autoPanSpeed: e.autoPanSpeed
});
function Pane({ isSelecting: e, selectionKeyPressed: t, selectionMode: n = j.Full, panOnDrag: r, autoPanOnSelection: i, paneClickDistance: a, selectionOnDrag: o, onSelectionStart: s, onSelectionEnd: c, onPaneClick: l, onPaneContextMenu: u, onPaneScroll: d, onPaneMouseEnter: f, onPaneMouseMove: p, onPaneMouseLeave: m, children: g }) {
	let _ = v(0), y = useStoreApi(), { userSelectionActive: b, elementsSelectable: C, dragging: w, panBy: T, autoPanSpeed: E } = useStore(selector$g, shallow$1), D = C && (e || b), O = v(null), k = v(), A = v(/* @__PURE__ */ new Set()), M = v(/* @__PURE__ */ new Set()), N = v(!1), P = v(!1), F = v({
		x: 0,
		y: 0
	}), I = v(!1), onClick = (e) => {
		if (P.current || N.current || y.getState().connection.inProgress) {
			P.current = !1, N.current = !1;
			return;
		}
		l?.(e), y.getState().resetSelectedElements(), y.setState({ nodesSelectionActive: !1 });
	}, onContextMenu = (e) => {
		if (Array.isArray(r) && r?.includes(2)) {
			e.preventDefault();
			return;
		}
		u?.(e);
	}, L = d ? (e) => d(e) : void 0, onClickCapture = (e) => {
		P.current &&= (e.stopPropagation(), !1);
	}, onPointerDownCapture = (n) => {
		if (n.pointerType === "touch" && r !== !1 && !t) return;
		let { domNode: i, transform: a } = y.getState();
		if (k.current = i?.getBoundingClientRect(), !k.current) return;
		let s = n.target === O.current;
		if (!s && n.target.closest(".nokey") || !e || !(o && s || t) || n.button !== 0 || !n.isPrimary) return;
		n.target?.setPointerCapture?.(n.pointerId), P.current = !1;
		let { x: c, y: l } = getEventPosition(n.nativeEvent, k.current), u = pointToRendererPoint({
			x: c,
			y: l
		}, a);
		y.setState({ userSelectionRect: {
			width: 0,
			height: 0,
			startX: u.x,
			startY: u.y,
			x: c,
			y: l
		} }), s || (n.stopPropagation(), n.preventDefault());
	};
	function commitUserSelectionRect(e, t) {
		let { userSelectionRect: r } = y.getState();
		if (!r) return;
		let { transform: i, nodeLookup: a, edgeLookup: o, connectionLookup: s, triggerNodeChanges: c, triggerEdgeChanges: l, defaultEdgeOptions: u } = y.getState(), d = {
			x: r.startX,
			y: r.startY
		}, { x: f, y: p } = rendererPointToPoint(d, i), m = {
			startX: d.x,
			startY: d.y,
			x: e < f ? e : f,
			y: t < p ? t : p,
			width: Math.abs(e - f),
			height: Math.abs(t - p)
		}, h = A.current, g = M.current;
		A.current = new Set(getNodesInside(a, m, i, n === j.Partial, !0).map((e) => e.id)), M.current = /* @__PURE__ */ new Set();
		let _ = u?.selectable ?? !0;
		for (let e of A.current) {
			let t = s.get(e);
			if (t) for (let { edgeId: e } of t.values()) {
				let t = o.get(e);
				t && (t.selectable ?? _) && M.current.add(e);
			}
		}
		areSetsEqual(h, A.current) || c(getSelectionChanges(a, A.current, !0)), areSetsEqual(g, M.current) || l(getSelectionChanges(o, M.current)), y.setState({
			userSelectionRect: m,
			userSelectionActive: !0,
			nodesSelectionActive: !1
		});
	}
	function autoPan() {
		if (!i || !k.current) return;
		let [e, t] = calcAutoPan(F.current, k.current, E);
		T({
			x: e,
			y: t
		}).then((e) => {
			if (!P.current || !e) {
				_.current = requestAnimationFrame(autoPan);
				return;
			}
			let { x: t, y: n } = F.current;
			commitUserSelectionRect(t, n), _.current = requestAnimationFrame(autoPan);
		});
	}
	let cleanupAutoPan = () => {
		cancelAnimationFrame(_.current), _.current = 0, I.current = !1;
	};
	h(() => () => cleanupAutoPan(), []);
	let onPointerMove = (e) => {
		let { userSelectionRect: n, transform: r, resetSelectedElements: i } = y.getState();
		if (!k.current || !n) return;
		let { x: o, y: c } = getEventPosition(e.nativeEvent, k.current);
		F.current = {
			x: o,
			y: c
		};
		let l = rendererPointToPoint({
			x: n.startX,
			y: n.startY
		}, r);
		if (!P.current) {
			let n = t ? 0 : a;
			if (Math.hypot(o - l.x, c - l.y) <= n) return;
			i(), s?.(e);
		}
		P.current = !0, I.current ||= (autoPan(), !0), commitUserSelectionRect(o, c);
	}, onPointerUp = (e) => {
		if (!D) {
			e.target === O.current && y.getState().connection.inProgress && (N.current = !0);
			return;
		}
		e.button === 0 && (e.target?.releasePointerCapture?.(e.pointerId), !b && e.target === O.current && y.getState().userSelectionRect && onClick?.(e), y.setState({
			userSelectionActive: !1,
			userSelectionRect: null
		}), P.current && (c?.(e), y.setState({ nodesSelectionActive: A.current.size > 0 })), cleanupAutoPan());
	}, onPointerCancel = (e) => {
		e.target?.releasePointerCapture?.(e.pointerId), cleanupAutoPan();
	}, R = r === !0 || Array.isArray(r) && r.includes(0);
	return S("div", {
		className: cc(["react-flow__pane", {
			draggable: R,
			dragging: w,
			selection: e
		}]),
		onClick: D ? void 0 : wrapHandler(onClick, O),
		onContextMenu: wrapHandler(onContextMenu, O),
		onWheel: wrapHandler(L, O),
		onPointerEnter: D ? void 0 : f,
		onPointerMove: D ? onPointerMove : p,
		onPointerUp,
		onPointerCancel: D ? onPointerCancel : void 0,
		onPointerDownCapture: D ? onPointerDownCapture : void 0,
		onClickCapture: D ? onClickCapture : void 0,
		onPointerLeave: m,
		ref: O,
		style: Q,
		children: [g, x(UserSelection, {})]
	});
}
function handleNodeClick({ id: e, store: t, unselect: n = !1, nodeRef: r }) {
	let { addSelectedNodes: i, unselectNodesAndEdges: a, multiSelectionActive: o, nodeLookup: s, onError: c } = t.getState(), l = s.get(e);
	if (!l) {
		c?.("012", T.error012(e));
		return;
	}
	t.setState({ nodesSelectionActive: !1 }), l.selected ? (n || l.selected && o) && (a({
		nodes: [l],
		edges: []
	}), requestAnimationFrame(() => r?.current?.blur())) : i([e]);
}
function useDrag({ nodeRef: e, disabled: t = !1, noDragClassName: n, handleSelector: r, nodeId: i, isSelectable: a, nodeClickDistance: o }) {
	let s = useStoreApi(), [c, l] = y(!1), u = v();
	return h(() => {
		if (!t) return u.current = XYDrag({
			getStoreItems: () => s.getState(),
			onNodeMouseDown: (t) => {
				handleNodeClick({
					id: t,
					store: s,
					nodeRef: e
				});
			},
			onDragStart: () => {
				l(!0);
			},
			onDragStop: () => {
				l(!1);
			}
		}), () => {
			u.current?.destroy(), u.current = void 0;
		};
	}, [
		t,
		s,
		e
	]), h(() => {
		t || !e.current || !u.current || u.current.update({
			noDragClassName: n,
			handleSelector: r,
			domNode: e.current,
			isSelectable: a,
			nodeId: i,
			nodeClickDistance: o
		});
	}, [
		n,
		r,
		t,
		a,
		e,
		i,
		o
	]), c;
}
var selectedAndDraggable = (e) => (t) => t.selected && (t.draggable || e && t.draggable === void 0);
function useMoveSelectedNodes() {
	let e = useStoreApi();
	return p((t) => {
		let { nodeExtent: n, snapToGrid: r, snapGrid: i, nodesDraggable: a, onError: o, updateNodePositions: s, nodeLookup: c, nodeOrigin: l } = e.getState(), u = /* @__PURE__ */ new Map(), d = selectedAndDraggable(a), f = r ? i[0] : 5, p = r ? i[1] : 5, m = t.direction.x * f * t.factor, h = t.direction.y * p * t.factor;
		for (let [, e] of c) {
			if (!d(e)) continue;
			let t = {
				x: e.internals.positionAbsolute.x + m,
				y: e.internals.positionAbsolute.y + h
			};
			r && (t = snapPosition(t, i));
			let { position: a, positionAbsolute: s } = calculateNodePosition({
				nodeId: e.id,
				nextPosition: t,
				nodeLookup: c,
				nodeExtent: n,
				nodeOrigin: l,
				onError: o
			});
			e.position = a, e.internals.positionAbsolute = s, u.set(e.id, e);
		}
		s(u);
	}, []);
}
var ye = u(null), be = ye.Provider;
ye.Consumer;
var useNodeId = () => m(ye), selector$f = (e) => ({
	connectOnClick: e.connectOnClick,
	noPanClassName: e.noPanClassName,
	rfId: e.rfId
}), xe = u(null);
function HandleConfigProvider({ children: e }) {
	let t = useStore(selector$f, shallow$1);
	return x(xe.Provider, {
		value: t,
		children: e
	});
}
function useHandleConfig() {
	let e = m(xe);
	if (!e) throw Error("useHandleConfig must be used within a HandleConfigProvider");
	return e;
}
var Se = {
	connectingFrom: !1,
	connectingTo: !1,
	clickConnecting: !1,
	isPossibleEndHandle: !0,
	connectionInProcess: !1,
	clickConnectionInProcess: !1,
	valid: !1
}, connectingSelector = (e, t, n) => (r) => {
	let { connectionClickStartHandle: i, connectionMode: a, connection: o } = r, { fromHandle: s, toHandle: c, isValid: l } = o;
	if (!s && !i) return Se;
	let u = c?.nodeId === e && c?.id === t && c?.type === n;
	return {
		connectingFrom: s?.nodeId === e && s?.id === t && s?.type === n,
		connectingTo: u,
		clickConnecting: i?.nodeId === e && i?.id === t && i?.type === n,
		isPossibleEndHandle: a === k.Strict ? s?.type !== n : e !== s?.nodeId || t !== s?.id,
		connectionInProcess: !!s,
		clickConnectionInProcess: !!i,
		valid: u && l
	};
};
function HandleComponent({ type: e = "source", position: t = F.Top, isValidConnection: n, isConnectable: r = !0, isConnectableStart: i = !0, isConnectableEnd: a = !0, id: o, onConnect: s, children: c, className: l, onMouseDown: u, onTouchStart: d, ...f }, p) {
	let m = o || null, h = e === "target", g = useStoreApi(), _ = useNodeId(), { connectOnClick: v, noPanClassName: y, rfId: b } = useHandleConfig(), { connectingFrom: S, connectingTo: C, clickConnecting: w, isPossibleEndHandle: E, connectionInProcess: D, clickConnectionInProcess: O, valid: k } = useStore(connectingSelector(_, m, e), shallow$1);
	_ || g.getState().onError?.("010", T.error010());
	let onConnectExtended = (e) => {
		let { defaultEdgeOptions: t, onConnect: n, hasDefaultEdges: r } = g.getState(), i = {
			...t,
			...e
		};
		if (r) {
			let { edges: e, setEdges: t, onError: n } = g.getState();
			t(addEdge(i, e, { onError: n }));
		}
		n?.(i), s?.(i);
	}, onPointerDown = (e) => {
		if (!_) return;
		let t = isMouseEvent(e.nativeEvent);
		if (i && (t && e.button === 0 || !t)) {
			let t = g.getState();
			G.onPointerDown(e.nativeEvent, {
				handleDomNode: e.currentTarget,
				autoPanOnConnect: t.autoPanOnConnect,
				connectionMode: t.connectionMode,
				connectionRadius: t.connectionRadius,
				domNode: t.domNode,
				nodeLookup: t.nodeLookup,
				lib: t.lib,
				isTarget: h,
				handleId: m,
				nodeId: _,
				flowId: t.rfId,
				panBy: t.panBy,
				cancelConnection: t.cancelConnection,
				onConnectStart: t.onConnectStart,
				onConnectEnd: (...e) => g.getState().onConnectEnd?.(...e),
				updateConnection: t.updateConnection,
				onConnect: onConnectExtended,
				isValidConnection: n || ((...e) => g.getState().isValidConnection?.(...e) ?? !0),
				getTransform: () => g.getState().transform,
				getFromHandle: () => g.getState().connection.fromHandle,
				autoPanSpeed: t.autoPanSpeed,
				dragThreshold: t.connectionDragThreshold
			});
		}
		t ? u?.(e) : d?.(e);
	}, onClick = (t) => {
		let { onClickConnectStart: r, onClickConnectEnd: a, connectionClickStartHandle: o, connectionMode: s, isValidConnection: c, lib: l, rfId: u, nodeLookup: d, connection: f } = g.getState();
		if (!_ || !o && !i) return;
		if (!o) {
			r?.(t.nativeEvent, {
				nodeId: _,
				handleId: m,
				handleType: e
			}), g.setState({ connectionClickStartHandle: {
				nodeId: _,
				type: e,
				id: m
			} });
			return;
		}
		let p = getHostForElement(t.target), h = n || c, { connection: v, isValid: y } = G.isValid(t.nativeEvent, {
			handle: {
				nodeId: _,
				id: m,
				type: e
			},
			connectionMode: s,
			fromNodeId: o.nodeId,
			fromHandleId: o.id || null,
			fromType: o.type,
			isValidConnection: h,
			flowId: u,
			doc: p,
			lib: l,
			nodeLookup: d
		});
		y && v && onConnectExtended(v);
		let b = structuredClone(f);
		delete b.inProgress, b.toPosition = b.toHandle ? b.toHandle.position : null, a?.(t, b), g.setState({ connectionClickStartHandle: null });
	};
	return x("div", {
		"data-handleid": m,
		"data-nodeid": _,
		"data-handlepos": t,
		"data-id": `${b}-${_}-${m}-${e}`,
		className: cc([
			"react-flow__handle",
			`react-flow__handle-${t}`,
			"nodrag",
			y,
			l,
			{
				source: !h,
				target: h,
				connectable: r,
				connectablestart: i,
				connectableend: a,
				clickconnecting: w,
				connectingfrom: S,
				connectingto: C,
				valid: k,
				connectionindicator: r && (!D || E) && (D || O ? a : i)
			}
		]),
		onMouseDown: onPointerDown,
		onTouchStart: onPointerDown,
		onClick: v ? onClick : void 0,
		ref: p,
		...f,
		children: c
	});
}
var Ce = f(fixedForwardRef(HandleComponent));
function InputNode({ data: e, isConnectable: t, sourcePosition: n = F.Bottom }) {
	return S(b, { children: [e?.label, x(Ce, {
		type: "source",
		position: n,
		isConnectable: t
	})] });
}
function DefaultNode({ data: e, isConnectable: t, targetPosition: n = F.Top, sourcePosition: r = F.Bottom }) {
	return S(b, { children: [
		x(Ce, {
			type: "target",
			position: n,
			isConnectable: t
		}),
		e?.label,
		x(Ce, {
			type: "source",
			position: r,
			isConnectable: t
		})
	] });
}
function GroupNode() {
	return null;
}
function OutputNode({ data: e, isConnectable: t, targetPosition: n = F.Top }) {
	return S(b, { children: [x(Ce, {
		type: "target",
		position: n,
		isConnectable: t
	}), e?.label] });
}
var we = {
	ArrowUp: {
		x: 0,
		y: -1
	},
	ArrowDown: {
		x: 0,
		y: 1
	},
	ArrowLeft: {
		x: -1,
		y: 0
	},
	ArrowRight: {
		x: 1,
		y: 0
	}
}, Te = {
	input: InputNode,
	default: DefaultNode,
	output: OutputNode,
	group: GroupNode
};
function getNodeInlineStyleDimensions(e) {
	return e.internals.handleBounds === void 0 ? {
		width: e.width ?? e.initialWidth ?? e.style?.width,
		height: e.height ?? e.initialHeight ?? e.style?.height
	} : {
		width: e.width ?? e.style?.width,
		height: e.height ?? e.style?.height
	};
}
var selector$e = (e) => {
	let { width: t, height: n, x: r, y: i } = getInternalNodesBounds(e.nodeLookup, { filter: (e) => !!e.selected });
	return {
		width: isNumeric(t) ? t : null,
		height: isNumeric(n) ? n : null,
		userSelectionActive: e.userSelectionActive,
		transformString: `translate(${e.transform[0]}px,${e.transform[1]}px) scale(${e.transform[2]}) translate(${r}px,${i}px)`
	};
};
function NodesSelection({ onSelectionContextMenu: e, noPanClassName: t, disableKeyboardA11y: n }) {
	let r = useStoreApi(), { width: i, height: a, transformString: o, userSelectionActive: s } = useStore(selector$e, shallow$1), c = useMoveSelectedNodes(), l = v(null);
	h(() => {
		n || l.current?.focus({ preventScroll: !0 });
	}, [n]);
	let u = !s && i !== null && a !== null;
	if (useDrag({
		nodeRef: l,
		disabled: !u
	}), !u) return null;
	let d = e ? (t) => {
		e(t, r.getState().nodes.filter((e) => e.selected));
	} : void 0, onKeyDown = (e) => {
		Object.prototype.hasOwnProperty.call(we, e.key) && (e.preventDefault(), c({
			direction: we[e.key],
			factor: e.shiftKey ? 4 : 1
		}));
	};
	return x("div", {
		className: cc([
			"react-flow__nodesselection",
			"react-flow__container",
			t
		]),
		style: { transform: o },
		children: x("div", {
			ref: l,
			className: "react-flow__nodesselection-rect",
			onContextMenu: d,
			tabIndex: n ? void 0 : -1,
			onKeyDown: n ? void 0 : onKeyDown,
			style: {
				width: i,
				height: a
			}
		})
	});
}
var Ee = typeof window < "u" ? window : void 0, selector$d = (e) => ({
	nodesSelectionActive: e.nodesSelectionActive,
	userSelectionActive: e.userSelectionActive
});
function FlowRendererComponent({ children: e, onPaneClick: t, onPaneMouseEnter: n, onPaneMouseMove: r, onPaneMouseLeave: i, onPaneContextMenu: a, onPaneScroll: o, paneClickDistance: s, deleteKeyCode: c, selectionKeyCode: l, selectionOnDrag: u, selectionMode: d, onSelectionStart: f, onSelectionEnd: p, multiSelectionKeyCode: m, panActivationKeyCode: h, zoomActivationKeyCode: g, elementsSelectable: _, zoomOnScroll: v, zoomOnPinch: y, panOnScroll: b, panOnScrollSpeed: C, panOnScrollMode: w, zoomOnDoubleClick: T, panOnDrag: E, autoPanOnSelection: D, defaultViewport: O, translateExtent: k, minZoom: A, maxZoom: j, preventScrolling: M, onSelectionContextMenu: N, noWheelClassName: P, noPanClassName: F, disableKeyboardA11y: I, onViewportChange: L, isControlledViewport: R }) {
	let { nodesSelectionActive: z, userSelectionActive: B } = useStore(selector$d, shallow$1), V = useKeyPress(l, { target: Ee }), H = useKeyPress(h, { target: Ee }), U = H || E, W = H || b, G = u && U !== !0, K = V || B || G;
	return useGlobalKeyHandler({
		deleteKeyCode: c,
		multiSelectionKeyCode: m
	}), x(ZoomPane, {
		onPaneContextMenu: a,
		elementsSelectable: _,
		zoomOnScroll: v,
		zoomOnPinch: y,
		panOnScroll: W,
		panActivationKeyPressed: H,
		panOnScrollSpeed: C,
		panOnScrollMode: w,
		zoomOnDoubleClick: T,
		panOnDrag: !V && U,
		defaultViewport: O,
		translateExtent: k,
		minZoom: A,
		maxZoom: j,
		zoomActivationKeyCode: g,
		preventScrolling: M,
		noWheelClassName: P,
		noPanClassName: F,
		onViewportChange: L,
		isControlledViewport: R,
		paneClickDistance: s,
		selectionOnDrag: G,
		children: S(Pane, {
			onSelectionStart: f,
			onSelectionEnd: p,
			onPaneClick: t,
			onPaneMouseEnter: n,
			onPaneMouseMove: r,
			onPaneMouseLeave: i,
			onPaneContextMenu: a,
			onPaneScroll: o,
			panOnDrag: U,
			autoPanOnSelection: D,
			isSelecting: !!K,
			selectionMode: d,
			selectionKeyPressed: V,
			paneClickDistance: s,
			selectionOnDrag: G,
			children: [e, z && x(NodesSelection, {
				onSelectionContextMenu: N,
				noPanClassName: F,
				disableKeyboardA11y: I
			})]
		})
	});
}
FlowRendererComponent.displayName = "FlowRenderer";
var De = f(FlowRendererComponent), selector$c = (e) => (t) => e ? getNodesInside(t.nodeLookup, {
	x: 0,
	y: 0,
	width: t.width,
	height: t.height
}, t.transform, !0).map((e) => e.id) : Array.from(t.nodeLookup.keys());
function useVisibleNodeIds(e) {
	return useStore(p(selector$c(e), [e]), shallow$1);
}
var selector$b = (e) => e.updateNodeInternals;
function useResizeObserver() {
	let e = useStore(selector$b), [t] = y(() => typeof ResizeObserver > "u" ? null : new ResizeObserver((t) => {
		let n = /* @__PURE__ */ new Map();
		t.forEach((e) => {
			let t = e.target.getAttribute("data-id");
			n.set(t, {
				id: t,
				nodeElement: e.target,
				force: !0
			});
		}), e(n);
	}));
	return h(() => () => {
		t?.disconnect();
	}, [t]), t;
}
function useNodeObserver({ node: e, nodeType: t, hasDimensions: n, resizeObserver: r }) {
	let i = useStoreApi(), a = v(null), o = v(null), s = v(e.sourcePosition), c = v(e.targetPosition), l = v(t), u = n && !!e.internals.handleBounds;
	return h(() => {
		a.current && !e.hidden && (!u || o.current !== a.current) && (o.current && r?.unobserve(o.current), r?.observe(a.current), o.current = a.current);
	}, [u, e.hidden]), h(() => () => {
		o.current &&= (r?.unobserve(o.current), null);
	}, []), h(() => {
		if (a.current) {
			let n = l.current !== t, r = s.current !== e.sourcePosition, o = c.current !== e.targetPosition;
			(n || r || o) && (l.current = t, s.current = e.sourcePosition, c.current = e.targetPosition, i.getState().updateNodeInternals(/* @__PURE__ */ new Map([[e.id, {
				id: e.id,
				nodeElement: a.current,
				force: !0
			}]])));
		}
	}, [
		e.id,
		t,
		e.sourcePosition,
		e.targetPosition
	]), a;
}
function NodeWrapper({ id: e, onClick: t, onMouseEnter: n, onMouseMove: r, onMouseLeave: i, onContextMenu: a, onDoubleClick: o, nodesDraggable: s, elementsSelectable: c, nodesConnectable: l, nodesFocusable: u, resizeObserver: d, noDragClassName: f, noPanClassName: p, disableKeyboardA11y: m, rfId: h, nodeTypes: g, nodeClickDistance: _, onError: v }) {
	let { node: y, internals: b, isParent: S } = useStore((t) => {
		let n = t.nodeLookup.get(e), r = t.parentLookup.has(e);
		return {
			node: n,
			internals: n.internals,
			isParent: r
		};
	}, shallow$1), C = y.type || "default", w = g?.[C] || Te[C];
	w === void 0 && (v?.("003", T.error003(C)), C = "default", w = g?.default || Te.default);
	let E = !!(y.draggable || s && y.draggable === void 0), O = !!(y.selectable || c && y.selectable === void 0), k = !!(y.connectable || l && y.connectable === void 0), A = !!(y.focusable || u && y.focusable === void 0), j = useStoreApi(), M = nodeHasDimensions(y), N = useNodeObserver({
		node: y,
		nodeType: C,
		hasDimensions: M,
		resizeObserver: d
	}), P = useDrag({
		nodeRef: N,
		disabled: y.hidden || !E,
		noDragClassName: f,
		handleSelector: y.dragHandle,
		nodeId: e,
		isSelectable: O,
		nodeClickDistance: _
	}), F = useMoveSelectedNodes();
	if (y.hidden) return null;
	let I = getNodeDimensions(y), L = getNodeInlineStyleDimensions(y), R = O || E || t || n || r || i, z = n ? (e) => n(e, { ...b.userNode }) : void 0, B = r ? (e) => r(e, { ...b.userNode }) : void 0, V = i ? (e) => i(e, { ...b.userNode }) : void 0, H = a ? (e) => a(e, { ...b.userNode }) : void 0, U = o ? (e) => o(e, { ...b.userNode }) : void 0, onSelectNodeHandler = (n) => {
		let { selectNodesOnDrag: r, nodeDragThreshold: i } = j.getState();
		O && (!r || !E || i > 0) && handleNodeClick({
			id: e,
			store: j,
			nodeRef: N
		}), t && t(n, { ...b.userNode });
	}, onKeyDown = (t) => {
		if (!(isInputDOMNode(t.nativeEvent) || m)) {
			if (D.includes(t.key) && O) {
				let n = t.key === "Escape";
				handleNodeClick({
					id: e,
					store: j,
					unselect: n,
					nodeRef: N
				});
			} else if (E && y.selected && Object.prototype.hasOwnProperty.call(we, t.key)) {
				t.preventDefault();
				let { ariaLabelConfig: e } = j.getState();
				j.setState({ ariaLiveMessage: e["node.a11yDescription.ariaLiveMessage"]({
					direction: t.key.replace("Arrow", "").toLowerCase(),
					x: ~~b.positionAbsolute.x,
					y: ~~b.positionAbsolute.y
				}) }), F({
					direction: we[t.key],
					factor: t.shiftKey ? 4 : 1
				});
			}
		}
	}, onFocus = () => {
		if (m || !N.current?.matches(":focus-visible")) return;
		let { transform: t, width: n, height: r, autoPanOnNodeFocus: i, setCenter: a } = j.getState();
		i && (getNodesInside(/* @__PURE__ */ new Map([[e, y]]), {
			x: 0,
			y: 0,
			width: n,
			height: r
		}, t, !0).length > 0 || a(y.position.x + I.width / 2, y.position.y + I.height / 2, { zoom: t[2] }));
	};
	return x("div", {
		className: cc([
			"react-flow__node",
			`react-flow__node-${C}`,
			{ [p]: E },
			y.className,
			{
				selected: y.selected,
				selectable: O,
				parent: S,
				draggable: E,
				dragging: P
			}
		]),
		ref: N,
		style: {
			zIndex: b.z,
			transform: `translate(${b.positionAbsolute.x}px,${b.positionAbsolute.y}px)`,
			pointerEvents: R ? "all" : "none",
			visibility: M ? "visible" : "hidden",
			...y.style,
			...L
		},
		"data-id": e,
		"data-testid": `rf__node-${e}`,
		onMouseEnter: z,
		onMouseMove: B,
		onMouseLeave: V,
		onContextMenu: H,
		onClick: onSelectNodeHandler,
		onDoubleClick: U,
		onKeyDown: A ? onKeyDown : void 0,
		tabIndex: A ? 0 : void 0,
		onFocus: A ? onFocus : void 0,
		role: y.ariaRole ?? (A ? "group" : void 0),
		"aria-roledescription": "node",
		"aria-describedby": m ? void 0 : `${oe}-${h}`,
		"aria-label": y.ariaLabel,
		...y.domAttributes,
		children: x(be, {
			value: e,
			children: x(w, {
				id: e,
				data: y.data,
				type: C,
				positionAbsoluteX: b.positionAbsolute.x,
				positionAbsoluteY: b.positionAbsolute.y,
				selected: y.selected ?? !1,
				selectable: O,
				draggable: E,
				deletable: y.deletable ?? !0,
				isConnectable: k,
				sourcePosition: y.sourcePosition,
				targetPosition: y.targetPosition,
				dragging: P,
				dragHandle: y.dragHandle,
				zIndex: b.z,
				parentId: y.parentId,
				...I
			})
		})
	});
}
var Oe = f(NodeWrapper), selector$a = (e) => ({
	nodesConnectable: e.nodesConnectable,
	nodesFocusable: e.nodesFocusable,
	elementsSelectable: e.elementsSelectable,
	onError: e.onError
});
function NodeRendererComponent(e) {
	let { nodesConnectable: t, nodesFocusable: n, elementsSelectable: r, onError: i } = useStore(selector$a, shallow$1), a = useVisibleNodeIds(e.onlyRenderVisibleElements), o = useResizeObserver();
	return x("div", {
		className: "react-flow__nodes",
		style: Q,
		children: a.map((a) => x(Oe, {
			id: a,
			nodeTypes: e.nodeTypes,
			nodeExtent: e.nodeExtent,
			onClick: e.onNodeClick,
			onMouseEnter: e.onNodeMouseEnter,
			onMouseMove: e.onNodeMouseMove,
			onMouseLeave: e.onNodeMouseLeave,
			onContextMenu: e.onNodeContextMenu,
			onDoubleClick: e.onNodeDoubleClick,
			noDragClassName: e.noDragClassName,
			noPanClassName: e.noPanClassName,
			rfId: e.rfId,
			disableKeyboardA11y: e.disableKeyboardA11y,
			resizeObserver: o,
			nodesDraggable: e.nodesDraggable ?? !0,
			nodesConnectable: t,
			nodesFocusable: n,
			elementsSelectable: r,
			nodeClickDistance: e.nodeClickDistance,
			onError: i
		}, a))
	});
}
NodeRendererComponent.displayName = "NodeRenderer";
var ke = f(NodeRendererComponent);
function useVisibleEdgeIds(e) {
	return useStore(p((t) => {
		if (!e) return t.edges.map((e) => e.id);
		let n = [];
		if (t.width && t.height) for (let e of t.edges) {
			let r = t.nodeLookup.get(e.source), i = t.nodeLookup.get(e.target);
			r && i && isEdgeVisible({
				sourceNode: r,
				targetNode: i,
				width: t.width,
				height: t.height,
				transform: t.transform
			}) && n.push(e.id);
		}
		return n;
	}, [e]), shallow$1);
}
var ArrowSymbol = ({ color: e = "none", strokeWidth: t = 1 }) => {
	let n = {
		strokeWidth: t,
		...e && { stroke: e }
	};
	return x("polyline", {
		className: "arrow",
		style: n,
		strokeLinecap: "round",
		fill: "none",
		strokeLinejoin: "round",
		points: "-5,-4 0,0 -5,4"
	});
}, ArrowClosedSymbol = ({ color: e = "none", strokeWidth: t = 1 }) => {
	let n = {
		strokeWidth: t,
		...e && {
			stroke: e,
			fill: e
		}
	};
	return x("polyline", {
		className: "arrowclosed",
		style: n,
		strokeLinecap: "round",
		strokeLinejoin: "round",
		points: "-5,-4 0,0 -5,4 -5,-4"
	});
}, Ae = {
	[P.Arrow]: ArrowSymbol,
	[P.ArrowClosed]: ArrowClosedSymbol
};
function useMarkerSymbol(e) {
	let t = useStoreApi();
	return _(() => Object.prototype.hasOwnProperty.call(Ae, e) ? Ae[e] : (t.getState().onError?.("009", T.error009(e)), null), [e]);
}
var Marker = ({ id: e, type: t, color: n, width: r = 12.5, height: i = 12.5, markerUnits: a = "strokeWidth", strokeWidth: o, orient: s = "auto-start-reverse" }) => {
	let c = useMarkerSymbol(t);
	return c ? x("marker", {
		className: "react-flow__arrowhead",
		id: e,
		markerWidth: `${r}`,
		markerHeight: `${i}`,
		viewBox: "-10 -10 20 20",
		markerUnits: a,
		orient: s,
		refX: "0",
		refY: "0",
		children: x(c, {
			color: n,
			strokeWidth: o
		})
	}) : null;
}, MarkerDefinitions = ({ defaultColor: e, rfId: t }) => {
	let n = useStore((e) => e.edges), r = useStore((e) => e.defaultEdgeOptions), i = _(() => createMarkerIds(n, {
		id: t,
		defaultColor: e,
		defaultMarkerStart: r?.markerStart,
		defaultMarkerEnd: r?.markerEnd
	}), [
		n,
		r,
		t,
		e
	]);
	return i.length ? x("svg", {
		className: "react-flow__marker",
		"aria-hidden": "true",
		children: x("defs", { children: i.map((e) => x(Marker, {
			id: e.id,
			type: e.type,
			color: e.color,
			width: e.width,
			height: e.height,
			markerUnits: e.markerUnits,
			strokeWidth: e.strokeWidth,
			orient: e.orient
		}, e.id)) })
	}) : null;
};
MarkerDefinitions.displayName = "MarkerDefinitions";
var je = f(MarkerDefinitions);
function EdgeTextComponent({ x: e, y: t, label: n, labelStyle: r, labelShowBg: i = !0, labelBgStyle: a, labelBgPadding: o = [2, 4], labelBgBorderRadius: s = 2, children: c, className: l, ...u }) {
	let [d, f] = y({
		x: 1,
		y: 0,
		width: 0,
		height: 0
	}), p = cc(["react-flow__edge-textwrapper", l]), m = v(null);
	return h(() => {
		if (m.current) {
			let e = m.current.getBBox();
			f({
				x: e.x,
				y: e.y,
				width: e.width,
				height: e.height
			});
		}
	}, [n]), n ? S("g", {
		transform: `translate(${e - d.width / 2} ${t - d.height / 2})`,
		className: p,
		visibility: d.width ? "visible" : "hidden",
		...u,
		children: [
			i && x("rect", {
				width: d.width + 2 * o[0],
				x: -o[0],
				y: -o[1],
				height: d.height + 2 * o[1],
				className: "react-flow__edge-textbg",
				style: a,
				rx: s,
				ry: s
			}),
			x("text", {
				className: "react-flow__edge-text",
				y: d.height / 2,
				dy: "0.3em",
				ref: m,
				style: r,
				children: n
			}),
			c
		]
	}) : null;
}
EdgeTextComponent.displayName = "EdgeText";
var Me = f(EdgeTextComponent);
function BaseEdge({ path: e, labelX: t, labelY: n, label: r, labelStyle: i, labelShowBg: a, labelBgStyle: o, labelBgPadding: s, labelBgBorderRadius: c, interactionWidth: l = 20, ...u }) {
	return S(b, { children: [
		x("path", {
			...u,
			d: e,
			fill: "none",
			className: cc(["react-flow__edge-path", u.className])
		}),
		l ? x("path", {
			d: e,
			fill: "none",
			strokeOpacity: 0,
			strokeWidth: l,
			className: "react-flow__edge-interaction"
		}) : null,
		r && isNumeric(t) && isNumeric(n) ? x(Me, {
			x: t,
			y: n,
			label: r,
			labelStyle: i,
			labelShowBg: a,
			labelBgStyle: o,
			labelBgPadding: s,
			labelBgBorderRadius: c
		}) : null
	] });
}
function getControl({ pos: e, x1: t, y1: n, x2: r, y2: i }) {
	return e === F.Left || e === F.Right ? [.5 * (t + r), n] : [t, .5 * (n + i)];
}
function getSimpleBezierPath({ sourceX: e, sourceY: t, sourcePosition: n = F.Bottom, targetX: r, targetY: i, targetPosition: a = F.Top }) {
	let [o, s] = getControl({
		pos: n,
		x1: e,
		y1: t,
		x2: r,
		y2: i
	}), [c, l] = getControl({
		pos: a,
		x1: r,
		y1: i,
		x2: e,
		y2: t
	}), [u, d, f, p] = getBezierEdgeCenter({
		sourceX: e,
		sourceY: t,
		targetX: r,
		targetY: i,
		sourceControlX: o,
		sourceControlY: s,
		targetControlX: c,
		targetControlY: l
	});
	return [
		`M${e},${t} C${o},${s} ${c},${l} ${r},${i}`,
		u,
		d,
		f,
		p
	];
}
function createSimpleBezierEdge(e) {
	return f(({ id: t, sourceX: n, sourceY: r, targetX: i, targetY: a, sourcePosition: o, targetPosition: s, label: c, labelStyle: l, labelShowBg: u, labelBgStyle: d, labelBgPadding: f, labelBgBorderRadius: p, style: m, markerEnd: h, markerStart: g, interactionWidth: _ }) => {
		let [v, y, b] = getSimpleBezierPath({
			sourceX: n,
			sourceY: r,
			sourcePosition: o,
			targetX: i,
			targetY: a,
			targetPosition: s
		}), S = e.isInternal ? void 0 : t;
		return x(BaseEdge, {
			id: S,
			path: v,
			labelX: y,
			labelY: b,
			label: c,
			labelStyle: l,
			labelShowBg: u,
			labelBgStyle: d,
			labelBgPadding: f,
			labelBgBorderRadius: p,
			style: m,
			markerEnd: h,
			markerStart: g,
			interactionWidth: _
		});
	});
}
var Ne = createSimpleBezierEdge({ isInternal: !1 }), Pe = createSimpleBezierEdge({ isInternal: !0 });
Ne.displayName = "SimpleBezierEdge", Pe.displayName = "SimpleBezierEdgeInternal";
function createSmoothStepEdge(e) {
	return f(({ id: t, sourceX: n, sourceY: r, targetX: i, targetY: a, label: o, labelStyle: s, labelShowBg: c, labelBgStyle: l, labelBgPadding: u, labelBgBorderRadius: d, style: f, sourcePosition: p = F.Bottom, targetPosition: m = F.Top, markerEnd: h, markerStart: g, pathOptions: _, interactionWidth: v }) => {
		let [y, b, S] = getSmoothStepPath({
			sourceX: n,
			sourceY: r,
			sourcePosition: p,
			targetX: i,
			targetY: a,
			targetPosition: m,
			borderRadius: _?.borderRadius,
			offset: _?.offset,
			stepPosition: _?.stepPosition
		}), C = e.isInternal ? void 0 : t;
		return x(BaseEdge, {
			id: C,
			path: y,
			labelX: b,
			labelY: S,
			label: o,
			labelStyle: s,
			labelShowBg: c,
			labelBgStyle: l,
			labelBgPadding: u,
			labelBgBorderRadius: d,
			style: f,
			markerEnd: h,
			markerStart: g,
			interactionWidth: v
		});
	});
}
var Fe = createSmoothStepEdge({ isInternal: !1 }), Ie = createSmoothStepEdge({ isInternal: !0 });
Fe.displayName = "SmoothStepEdge", Ie.displayName = "SmoothStepEdgeInternal";
function createStepEdge(e) {
	return f(({ id: t, ...n }) => {
		let r = e.isInternal ? void 0 : t;
		return x(Fe, {
			...n,
			id: r,
			pathOptions: _(() => ({
				borderRadius: 0,
				offset: n.pathOptions?.offset
			}), [n.pathOptions?.offset])
		});
	});
}
var Le = createStepEdge({ isInternal: !1 }), Re = createStepEdge({ isInternal: !0 });
Le.displayName = "StepEdge", Re.displayName = "StepEdgeInternal";
function createStraightEdge(e) {
	return f(({ id: t, sourceX: n, sourceY: r, targetX: i, targetY: a, label: o, labelStyle: s, labelShowBg: c, labelBgStyle: l, labelBgPadding: u, labelBgBorderRadius: d, style: f, markerEnd: p, markerStart: m, interactionWidth: h }) => {
		let [g, _, v] = getStraightPath({
			sourceX: n,
			sourceY: r,
			targetX: i,
			targetY: a
		}), y = e.isInternal ? void 0 : t;
		return x(BaseEdge, {
			id: y,
			path: g,
			labelX: _,
			labelY: v,
			label: o,
			labelStyle: s,
			labelShowBg: c,
			labelBgStyle: l,
			labelBgPadding: u,
			labelBgBorderRadius: d,
			style: f,
			markerEnd: p,
			markerStart: m,
			interactionWidth: h
		});
	});
}
var ze = createStraightEdge({ isInternal: !1 }), Be = createStraightEdge({ isInternal: !0 });
ze.displayName = "StraightEdge", Be.displayName = "StraightEdgeInternal";
function createBezierEdge(e) {
	return f(({ id: t, sourceX: n, sourceY: r, targetX: i, targetY: a, sourcePosition: o = F.Bottom, targetPosition: s = F.Top, label: c, labelStyle: l, labelShowBg: u, labelBgStyle: d, labelBgPadding: f, labelBgBorderRadius: p, style: m, markerEnd: h, markerStart: g, pathOptions: _, interactionWidth: v }) => {
		let [y, b, S] = getBezierPath({
			sourceX: n,
			sourceY: r,
			sourcePosition: o,
			targetX: i,
			targetY: a,
			targetPosition: s,
			curvature: _?.curvature
		}), C = e.isInternal ? void 0 : t;
		return x(BaseEdge, {
			id: C,
			path: y,
			labelX: b,
			labelY: S,
			label: c,
			labelStyle: l,
			labelShowBg: u,
			labelBgStyle: d,
			labelBgPadding: f,
			labelBgBorderRadius: p,
			style: m,
			markerEnd: h,
			markerStart: g,
			interactionWidth: v
		});
	});
}
var Ve = createBezierEdge({ isInternal: !1 }), He = createBezierEdge({ isInternal: !0 });
Ve.displayName = "BezierEdge", He.displayName = "BezierEdgeInternal";
var Ue = {
	default: He,
	straight: Be,
	step: Re,
	smoothstep: Ie,
	simplebezier: Pe
}, We = {
	sourceX: null,
	sourceY: null,
	targetX: null,
	targetY: null,
	sourcePosition: null,
	targetPosition: null,
	zIndex: void 0
}, shiftX = (e, t, n) => n === F.Left ? e - t : n === F.Right ? e + t : e, shiftY = (e, t, n) => n === F.Top ? e - t : n === F.Bottom ? e + t : e, Ge = "react-flow__edgeupdater";
function EdgeAnchor({ position: e, centerX: t, centerY: n, radius: r = 10, onMouseDown: i, onMouseEnter: a, onMouseOut: o, type: s }) {
	return x("circle", {
		onMouseDown: i,
		onMouseEnter: a,
		onMouseOut: o,
		className: cc([Ge, `${Ge}-${s}`]),
		cx: shiftX(t, r, e),
		cy: shiftY(n, r, e),
		r,
		stroke: "transparent",
		fill: "transparent"
	});
}
function EdgeUpdateAnchors({ isReconnectable: e, reconnectRadius: t, edge: n, sourceX: r, sourceY: i, targetX: a, targetY: o, sourcePosition: s, targetPosition: c, onReconnect: l, onReconnectStart: u, onReconnectEnd: d, setReconnecting: f, setUpdateHover: p }) {
	let m = useStoreApi(), handleEdgeUpdater = (e, t) => {
		if (e.button !== 0) return;
		let { autoPanOnConnect: r, domNode: i, connectionMode: a, connectionRadius: o, lib: s, onConnectStart: c, cancelConnection: p, nodeLookup: h, rfId: g, panBy: _, updateConnection: v } = m.getState(), y = t.type === "target", _onReconnectEnd = (e, r) => {
			f(!1), d?.(e, n, t.type, r);
		}, onConnectEdge = (e) => l?.(n, e), _onConnectStart = (r, i) => {
			f(!0), u?.(e, n, t.type), c?.(r, i);
		};
		G.onPointerDown(e.nativeEvent, {
			autoPanOnConnect: r,
			connectionMode: a,
			connectionRadius: o,
			domNode: i,
			handleId: t.id,
			nodeId: t.nodeId,
			nodeLookup: h,
			isTarget: y,
			edgeUpdaterType: t.type,
			lib: s,
			flowId: g,
			cancelConnection: p,
			panBy: _,
			isValidConnection: (...e) => m.getState().isValidConnection?.(...e) ?? !0,
			onConnect: onConnectEdge,
			onConnectStart: _onConnectStart,
			onConnectEnd: (...e) => m.getState().onConnectEnd?.(...e),
			onReconnectEnd: _onReconnectEnd,
			updateConnection: v,
			getTransform: () => m.getState().transform,
			getFromHandle: () => m.getState().connection.fromHandle,
			dragThreshold: m.getState().connectionDragThreshold,
			handleDomNode: e.currentTarget
		});
	}, onReconnectSourceMouseDown = (e) => handleEdgeUpdater(e, {
		nodeId: n.target,
		id: n.targetHandle ?? null,
		type: "target"
	}), onReconnectTargetMouseDown = (e) => handleEdgeUpdater(e, {
		nodeId: n.source,
		id: n.sourceHandle ?? null,
		type: "source"
	}), onReconnectMouseEnter = () => p(!0), onReconnectMouseOut = () => p(!1);
	return S(b, { children: [(e === !0 || e === "source") && x(EdgeAnchor, {
		position: s,
		centerX: r,
		centerY: i,
		radius: t,
		onMouseDown: onReconnectSourceMouseDown,
		onMouseEnter: onReconnectMouseEnter,
		onMouseOut: onReconnectMouseOut,
		type: "source"
	}), (e === !0 || e === "target") && x(EdgeAnchor, {
		position: c,
		centerX: a,
		centerY: o,
		radius: t,
		onMouseDown: onReconnectTargetMouseDown,
		onMouseEnter: onReconnectMouseEnter,
		onMouseOut: onReconnectMouseOut,
		type: "target"
	})] });
}
function EdgeWrapper({ id: e, edgesFocusable: t, edgesReconnectable: n, elementsSelectable: r, onClick: i, onDoubleClick: a, onContextMenu: o, onMouseEnter: s, onMouseMove: c, onMouseLeave: l, reconnectRadius: u, onReconnect: d, onReconnectStart: f, onReconnectEnd: m, rfId: h, edgeTypes: g, noPanClassName: b, onError: C, disableKeyboardA11y: w }) {
	let E = useStore((t) => t.edgeLookup.get(e)), O = useStore((e) => e.defaultEdgeOptions);
	E = O ? {
		...O,
		...E
	} : E;
	let k = E.type || "default", A = g?.[k] || Ue[k];
	A === void 0 && (C?.("011", T.error011(k)), k = "default", A = g?.default || Ue.default);
	let j = !!(E.focusable || t && E.focusable === void 0), M = d !== void 0 && (E.reconnectable || n && E.reconnectable === void 0), N = !!(E.selectable || r && E.selectable === void 0), P = v(null), [F, I] = y(!1), [L, R] = y(!1), z = useStoreApi(), { zIndex: B = E.zIndex, sourceX: V, sourceY: H, targetX: U, targetY: W, sourcePosition: G, targetPosition: K } = useStore(p((t) => {
		let n = t.nodeLookup.get(E.source), r = t.nodeLookup.get(E.target);
		if (!n || !r) return We;
		let i = getEdgePosition({
			id: e,
			sourceNode: n,
			targetNode: r,
			sourceHandle: E.sourceHandle || null,
			targetHandle: E.targetHandle || null,
			connectionMode: t.connectionMode,
			onError: C
		}), a = getElevatedEdgeZIndex({
			selected: E.selected,
			zIndex: E.zIndex,
			sourceNode: n,
			targetNode: r,
			elevateOnSelect: t.elevateEdgesOnSelect,
			zIndexMode: t.zIndexMode
		});
		return {
			...i || We,
			zIndex: a
		};
	}, [
		E.source,
		E.target,
		E.sourceHandle,
		E.targetHandle,
		E.selected,
		E.zIndex,
		C
	]), shallow$1), q = _(() => E.markerStart ? `url('#${getMarkerId(E.markerStart, h)}')` : void 0, [E.markerStart, h]), J = _(() => E.markerEnd ? `url('#${getMarkerId(E.markerEnd, h)}')` : void 0, [E.markerEnd, h]);
	if (E.hidden || V === null || H === null || U === null || W === null) return null;
	let onEdgeClick = (t) => {
		let { addSelectedEdges: n, unselectNodesAndEdges: r, multiSelectionActive: a } = z.getState();
		N && (z.setState({ nodesSelectionActive: !1 }), E.selected && a ? (r({
			nodes: [],
			edges: [E]
		}), P.current?.blur()) : n([e])), i && i(t, E);
	}, ee = a ? (e) => {
		a(e, { ...E });
	} : void 0, Y = o ? (e) => {
		o(e, { ...E });
	} : void 0, te = s ? (e) => {
		s(e, { ...E });
	} : void 0, X = c ? (e) => {
		c(e, { ...E });
	} : void 0, ne = l ? (e) => {
		l(e, { ...E });
	} : void 0, onKeyDown = (t) => {
		if (!w && D.includes(t.key) && N) {
			let { unselectNodesAndEdges: n, addSelectedEdges: r } = z.getState();
			t.key === "Escape" ? (P.current?.blur(), n({ edges: [E] })) : r([e]);
		}
	};
	return x("svg", {
		style: { zIndex: B },
		children: S("g", {
			className: cc([
				"react-flow__edge",
				`react-flow__edge-${k}`,
				E.className,
				b,
				{
					selected: E.selected,
					animated: E.animated,
					inactive: !N && !i,
					updating: F,
					selectable: N
				}
			]),
			onClick: onEdgeClick,
			onDoubleClick: ee,
			onContextMenu: Y,
			onMouseEnter: te,
			onMouseMove: X,
			onMouseLeave: ne,
			onKeyDown: j ? onKeyDown : void 0,
			tabIndex: j ? 0 : void 0,
			role: E.ariaRole ?? (j ? "group" : "img"),
			"aria-roledescription": "edge",
			"data-id": e,
			"data-testid": `rf__edge-${e}`,
			"aria-label": E.ariaLabel === null ? void 0 : E.ariaLabel || `Edge from ${E.source} to ${E.target}`,
			"aria-describedby": j ? `${se}-${h}` : void 0,
			ref: P,
			...E.domAttributes,
			children: [!L && x(A, {
				id: e,
				source: E.source,
				target: E.target,
				type: E.type,
				selected: E.selected,
				animated: E.animated,
				selectable: N,
				deletable: E.deletable ?? !0,
				label: E.label,
				labelStyle: E.labelStyle,
				labelShowBg: E.labelShowBg,
				labelBgStyle: E.labelBgStyle,
				labelBgPadding: E.labelBgPadding,
				labelBgBorderRadius: E.labelBgBorderRadius,
				sourceX: V,
				sourceY: H,
				targetX: U,
				targetY: W,
				sourcePosition: G,
				targetPosition: K,
				data: E.data,
				style: E.style,
				sourceHandleId: E.sourceHandle,
				targetHandleId: E.targetHandle,
				markerStart: q,
				markerEnd: J,
				pathOptions: "pathOptions" in E ? E.pathOptions : void 0,
				interactionWidth: E.interactionWidth
			}), M && x(EdgeUpdateAnchors, {
				edge: E,
				isReconnectable: M,
				reconnectRadius: u,
				onReconnect: d,
				onReconnectStart: f,
				onReconnectEnd: m,
				sourceX: V,
				sourceY: H,
				targetX: U,
				targetY: W,
				sourcePosition: G,
				targetPosition: K,
				setUpdateHover: I,
				setReconnecting: R
			})]
		})
	});
}
var Ke = f(EdgeWrapper), selector$9 = (e) => ({
	edgesFocusable: e.edgesFocusable,
	edgesReconnectable: e.edgesReconnectable,
	elementsSelectable: e.elementsSelectable,
	connectionMode: e.connectionMode,
	onError: e.onError
});
function EdgeRendererComponent({ defaultMarkerColor: e, onlyRenderVisibleElements: t, rfId: n, edgeTypes: r, noPanClassName: i, onReconnect: a, onEdgeContextMenu: o, onEdgeMouseEnter: s, onEdgeMouseMove: c, onEdgeMouseLeave: l, onEdgeClick: u, reconnectRadius: d, onEdgeDoubleClick: f, onReconnectStart: p, onReconnectEnd: m, disableKeyboardA11y: h }) {
	let { edgesFocusable: g, edgesReconnectable: _, elementsSelectable: v, onError: y } = useStore(selector$9, shallow$1), b = useVisibleEdgeIds(t);
	return S("div", {
		className: "react-flow__edges",
		children: [x(je, {
			defaultColor: e,
			rfId: n
		}), b.map((e) => x(Ke, {
			id: e,
			edgesFocusable: g,
			edgesReconnectable: _,
			elementsSelectable: v,
			noPanClassName: i,
			onReconnect: a,
			onContextMenu: o,
			onMouseEnter: s,
			onMouseMove: c,
			onMouseLeave: l,
			onClick: u,
			reconnectRadius: d,
			onDoubleClick: f,
			onReconnectStart: p,
			onReconnectEnd: m,
			rfId: n,
			onError: y,
			edgeTypes: r,
			disableKeyboardA11y: h
		}, e))]
	});
}
EdgeRendererComponent.displayName = "EdgeRenderer";
var qe = f(EdgeRendererComponent), toTransformString = (e) => `translate(${e[0]}px,${e[1]}px) scale(${e[2]})`;
function Viewport({ children: e }) {
	let t = useStoreApi(), n = v(null), [r] = y(() => t.getState().transform);
	return ge(() => {
		let e = null, applyTransform = () => {
			let r = t.getState().transform;
			e && r[0] === e[0] && r[1] === e[1] && r[2] === e[2] || (e = r, n.current && (n.current.style.transform = toTransformString(r)));
		};
		return applyTransform(), t.subscribe(applyTransform);
	}, [t]), x("div", {
		ref: n,
		className: "react-flow__viewport xyflow__viewport react-flow__container",
		style: { transform: toTransformString(r) },
		children: e
	});
}
function useOnInitHandler(e) {
	let t = useReactFlow(), n = v(!1);
	h(() => {
		!n.current && t.viewportInitialized && e && (setTimeout(() => e(t), 1), n.current = !0);
	}, [e, t.viewportInitialized]);
}
var selector$8 = (e) => e.panZoom?.syncViewport;
function useViewportSync(e) {
	let t = useStore(selector$8), n = useStoreApi();
	return h(() => {
		e && (t?.(e), n.setState({ transform: [
			e.x,
			e.y,
			e.zoom
		] }));
	}, [e, t]), null;
}
function storeSelector$1(e) {
	return e.connection.inProgress ? {
		...e.connection,
		to: pointToRendererPoint(e.connection.to, e.transform)
	} : { ...e.connection };
}
function getSelector(e) {
	if (e) {
		let combinedSelector = (t) => e(storeSelector$1(t));
		return combinedSelector;
	}
	return storeSelector$1;
}
function useConnection(e) {
	return useStore(getSelector(e), shallow$1);
}
var selector$7 = (e) => ({
	nodesConnectable: e.nodesConnectable,
	isValid: e.connection.isValid,
	inProgress: e.connection.inProgress,
	width: e.width,
	height: e.height
});
function ConnectionLineWrapper({ containerStyle: e, style: t, type: n, component: r }) {
	let { nodesConnectable: i, width: a, height: o, isValid: s, inProgress: c } = useStore(selector$7, shallow$1);
	return a && i && c ? x("svg", {
		style: e,
		width: a,
		height: o,
		className: "react-flow__connectionline react-flow__container",
		children: x("g", {
			className: cc(["react-flow__connection", getConnectionStatus(s)]),
			children: x(ConnectionLine, {
				style: t,
				type: n,
				CustomComponent: r,
				isValid: s
			})
		})
	}) : null;
}
var ConnectionLine = ({ style: e, type: t = N.Bezier, CustomComponent: n, isValid: r }) => {
	let { inProgress: i, from: a, fromNode: o, fromHandle: s, fromPosition: c, to: l, toNode: u, toHandle: d, toPosition: f, pointer: p } = useConnection();
	if (!i) return;
	if (n) return x(n, {
		connectionLineType: t,
		connectionLineStyle: e,
		fromNode: o,
		fromHandle: s,
		fromX: a.x,
		fromY: a.y,
		toX: l.x,
		toY: l.y,
		fromPosition: c,
		toPosition: f,
		connectionStatus: getConnectionStatus(r),
		toNode: u,
		toHandle: d,
		pointer: p
	});
	let m = "", h = {
		sourceX: a.x,
		sourceY: a.y,
		sourcePosition: c,
		targetX: l.x,
		targetY: l.y,
		targetPosition: f
	};
	switch (t) {
		case N.Bezier:
			[m] = getBezierPath(h);
			break;
		case N.SimpleBezier:
			[m] = getSimpleBezierPath(h);
			break;
		case N.Step:
			[m] = getSmoothStepPath({
				...h,
				borderRadius: 0
			});
			break;
		case N.SmoothStep:
			[m] = getSmoothStepPath(h);
			break;
		default: [m] = getStraightPath(h);
	}
	return x("path", {
		d: m,
		fill: "none",
		className: "react-flow__connection-path",
		style: e
	});
};
ConnectionLine.displayName = "ConnectionLine";
var Je = {};
function useNodeOrEdgeTypesWarning(e = Je) {
	v(e), useStoreApi(), h(() => {}, [e]);
}
function useStylesLoadedWarning() {
	useStoreApi(), v(!1), h(() => {}, []);
}
function GraphViewComponent({ nodeTypes: e, edgeTypes: t, onInit: n, onNodeClick: r, onEdgeClick: i, onNodeDoubleClick: a, onEdgeDoubleClick: o, onNodeMouseEnter: s, onNodeMouseMove: c, onNodeMouseLeave: l, onNodeContextMenu: u, onSelectionContextMenu: d, onSelectionStart: f, onSelectionEnd: p, connectionLineType: m, connectionLineStyle: h, connectionLineComponent: g, connectionLineContainerStyle: _, selectionKeyCode: v, selectionOnDrag: y, selectionMode: b, multiSelectionKeyCode: C, panActivationKeyCode: w, zoomActivationKeyCode: T, deleteKeyCode: E, onlyRenderVisibleElements: D, elementsSelectable: O, defaultViewport: k, translateExtent: A, minZoom: j, maxZoom: M, preventScrolling: N, defaultMarkerColor: P, zoomOnScroll: F, zoomOnPinch: I, panOnScroll: L, panOnScrollSpeed: R, panOnScrollMode: z, zoomOnDoubleClick: B, panOnDrag: V, autoPanOnSelection: H, onPaneClick: U, onPaneMouseEnter: W, onPaneMouseMove: G, onPaneMouseLeave: K, onPaneScroll: q, onPaneContextMenu: J, paneClickDistance: ee, nodeClickDistance: Y, onEdgeContextMenu: te, onEdgeMouseEnter: X, onEdgeMouseMove: ne, onEdgeMouseLeave: re, reconnectRadius: ie, onReconnect: ae, onReconnectStart: oe, onReconnectEnd: se, noDragClassName: ce, noWheelClassName: Z, noPanClassName: le, disableKeyboardA11y: ue, nodeExtent: de, rfId: fe, viewport: pe, onViewportChange: me, nodesDraggable: he }) {
	return useNodeOrEdgeTypesWarning(e), useNodeOrEdgeTypesWarning(t), useStylesLoadedWarning(), useOnInitHandler(n), useViewportSync(pe), x(De, {
		onPaneClick: U,
		onPaneMouseEnter: W,
		onPaneMouseMove: G,
		onPaneMouseLeave: K,
		onPaneContextMenu: J,
		onPaneScroll: q,
		paneClickDistance: ee,
		deleteKeyCode: E,
		selectionKeyCode: v,
		selectionOnDrag: y,
		selectionMode: b,
		onSelectionStart: f,
		onSelectionEnd: p,
		multiSelectionKeyCode: C,
		panActivationKeyCode: w,
		zoomActivationKeyCode: T,
		elementsSelectable: O,
		zoomOnScroll: F,
		zoomOnPinch: I,
		zoomOnDoubleClick: B,
		panOnScroll: L,
		panOnScrollSpeed: R,
		panOnScrollMode: z,
		panOnDrag: V,
		autoPanOnSelection: H,
		defaultViewport: k,
		translateExtent: A,
		minZoom: j,
		maxZoom: M,
		onSelectionContextMenu: d,
		preventScrolling: N,
		noDragClassName: ce,
		noWheelClassName: Z,
		noPanClassName: le,
		disableKeyboardA11y: ue,
		onViewportChange: me,
		isControlledViewport: !!pe,
		children: S(Viewport, { children: [
			x(qe, {
				edgeTypes: t,
				onEdgeClick: i,
				onEdgeDoubleClick: o,
				onReconnect: ae,
				onReconnectStart: oe,
				onReconnectEnd: se,
				onlyRenderVisibleElements: D,
				onEdgeContextMenu: te,
				onEdgeMouseEnter: X,
				onEdgeMouseMove: ne,
				onEdgeMouseLeave: re,
				reconnectRadius: ie,
				defaultMarkerColor: P,
				noPanClassName: le,
				disableKeyboardA11y: ue,
				rfId: fe
			}),
			x(ConnectionLineWrapper, {
				style: h,
				type: m,
				component: g,
				containerStyle: _
			}),
			x("div", { className: "react-flow__edgelabel-renderer" }),
			x(ke, {
				nodeTypes: e,
				onNodeClick: r,
				onNodeDoubleClick: a,
				onNodeMouseEnter: s,
				onNodeMouseMove: c,
				onNodeMouseLeave: l,
				onNodeContextMenu: u,
				nodeClickDistance: Y,
				onlyRenderVisibleElements: D,
				noPanClassName: le,
				noDragClassName: ce,
				disableKeyboardA11y: ue,
				nodeExtent: de,
				rfId: fe,
				nodesDraggable: he
			}),
			x("div", { className: "react-flow__viewport-portal" })
		] })
	});
}
GraphViewComponent.displayName = "GraphView";
var Ye = f(GraphViewComponent), Xe = createDevWarn("React Flow", "https://reactflow.dev/"), getInitialState = ({ nodes: e, edges: t, defaultNodes: n, defaultEdges: r, width: i, height: a, fitView: o, fitViewOptions: s, minZoom: c = .5, maxZoom: l = 2, nodeOrigin: u, nodeExtent: d, zIndexMode: f = "basic" } = {}) => {
	let p = /* @__PURE__ */ new Map(), m = /* @__PURE__ */ new Map(), h = /* @__PURE__ */ new Map(), g = /* @__PURE__ */ new Map(), _ = r ?? t ?? [], v = n ?? e ?? [], y = u ?? [0, 0], b = d ?? E;
	updateConnectionLookup(h, g, _);
	let { nodesInitialized: x } = adoptUserNodes(v, p, m, {
		nodeOrigin: y,
		nodeExtent: b,
		zIndexMode: f
	}), S = [
		0,
		0,
		1
	];
	if (o && i && a) {
		let { x: e, y: t, zoom: n } = getViewportForBounds(getInternalNodesBounds(p, { filter: (e) => !!((e.width || e.initialWidth) && (e.height || e.initialHeight)) }), i, a, c, l, s?.padding ?? .1);
		S = [
			e,
			t,
			n
		];
	}
	return {
		rfId: "1",
		width: i ?? 0,
		height: a ?? 0,
		transform: S,
		nodes: v,
		nodesInitialized: x,
		nodeLookup: p,
		parentLookup: m,
		edges: _,
		edgeLookup: g,
		connectionLookup: h,
		onNodesChange: null,
		onEdgesChange: null,
		hasDefaultNodes: n !== void 0,
		hasDefaultEdges: r !== void 0,
		panZoom: null,
		minZoom: c,
		maxZoom: l,
		translateExtent: E,
		nodeExtent: b,
		nodesSelectionActive: !1,
		userSelectionActive: !1,
		userSelectionRect: null,
		connectionMode: k.Strict,
		domNode: null,
		paneDragging: !1,
		noPanClassName: "nopan",
		nodeOrigin: y,
		nodeDragThreshold: 1,
		connectionDragThreshold: 1,
		snapGrid: [15, 15],
		snapToGrid: !1,
		nodesDraggable: !0,
		nodesConnectable: !0,
		nodesFocusable: !0,
		edgesFocusable: !0,
		edgesReconnectable: !0,
		elementsSelectable: !0,
		elevateNodesOnSelect: !0,
		elevateEdgesOnSelect: !0,
		selectNodesOnDrag: !0,
		multiSelectionActive: !1,
		fitViewQueued: o ?? !1,
		fitViewOptions: s,
		fitViewResolver: null,
		connection: { ...M },
		connectionClickStartHandle: null,
		connectOnClick: !0,
		ariaLiveMessage: "",
		autoPanOnConnect: !0,
		autoPanOnNodeDrag: !0,
		autoPanOnNodeFocus: !0,
		autoPanSpeed: 15,
		connectionRadius: 20,
		onError: Xe,
		isValidConnection: void 0,
		onSelectionChangeHandlers: [],
		lib: "react",
		debug: !1,
		ariaLabelConfig: O,
		zIndexMode: f,
		defaultEdgeOptions: void 0,
		onNodesChangeMiddlewareMap: /* @__PURE__ */ new Map(),
		onEdgesChangeMiddlewareMap: /* @__PURE__ */ new Map(),
		onNodesDelete: void 0,
		onEdgesDelete: void 0,
		onDelete: void 0,
		onBeforeDelete: void 0,
		onViewportChangeStart: void 0,
		onViewportChange: void 0,
		onViewportChangeEnd: void 0,
		onNodeDragStart: void 0,
		onNodeDrag: void 0,
		onNodeDragStop: void 0,
		onSelectionDragStart: void 0,
		onSelectionDrag: void 0,
		onSelectionDragStop: void 0,
		onMoveStart: void 0,
		onMove: void 0,
		onMoveEnd: void 0,
		onConnect: void 0,
		onConnectStart: void 0,
		onConnectEnd: void 0,
		onClickConnectStart: void 0,
		onClickConnectEnd: void 0
	};
}, createStore = ({ nodes: e, edges: t, defaultNodes: n, defaultEdges: r, width: i, height: a, fitView: o, fitViewOptions: s, minZoom: c, maxZoom: l, nodeOrigin: u, nodeExtent: d, zIndexMode: f }) => createWithEqualityFn((p, m) => {
	async function resolveFitView() {
		let { nodeLookup: e, panZoom: t, fitViewOptions: n, fitViewResolver: r, width: i, height: a, minZoom: o, maxZoom: s } = m();
		t && (await fitViewport({
			nodes: e,
			width: i,
			height: a,
			panZoom: t,
			minZoom: o,
			maxZoom: s
		}, n), r?.resolve(!0), p({ fitViewResolver: null }));
	}
	return {
		...getInitialState({
			nodes: e,
			edges: t,
			width: i,
			height: a,
			fitView: o,
			fitViewOptions: s,
			minZoom: c,
			maxZoom: l,
			nodeOrigin: u,
			nodeExtent: d,
			defaultNodes: n,
			defaultEdges: r,
			zIndexMode: f
		}),
		setNodes: (e) => {
			let { nodeLookup: t, parentLookup: n, nodeOrigin: r, nodeExtent: i, elevateNodesOnSelect: a, fitViewQueued: o, zIndexMode: s, nodesSelectionActive: c } = m(), { nodesInitialized: l, hasSelectedNodes: u } = adoptUserNodes(e, t, n, {
				nodeOrigin: r,
				nodeExtent: i,
				elevateNodesOnSelect: a,
				checkEquality: !0,
				zIndexMode: s
			}), d = c && u;
			o && l ? (resolveFitView(), p({
				nodes: e,
				nodesInitialized: l,
				fitViewQueued: !1,
				fitViewOptions: void 0,
				nodesSelectionActive: d
			})) : p({
				nodes: e,
				nodesInitialized: l,
				nodesSelectionActive: d
			});
		},
		setEdges: (e) => {
			let { connectionLookup: t, edgeLookup: n } = m();
			updateConnectionLookup(t, n, e), p({ edges: e });
		},
		setDefaultNodesAndEdges: (e, t) => {
			if (e) {
				let { setNodes: t } = m();
				t(e), p({ hasDefaultNodes: !0 });
			}
			if (t) {
				let { setEdges: e } = m();
				e(t), p({ hasDefaultEdges: !0 });
			}
		},
		updateNodeInternals: (e) => {
			let { triggerNodeChanges: t, nodeLookup: n, parentLookup: r, domNode: i, nodeOrigin: a, nodeExtent: o, debug: s, fitViewQueued: c, zIndexMode: l } = m(), { changes: u, updatedInternals: d } = updateNodeInternals(e, n, r, i, a, o, l);
			d && (updateAbsolutePositions(n, r, {
				nodeOrigin: a,
				nodeExtent: o,
				zIndexMode: l
			}), c ? (resolveFitView(), p({
				fitViewQueued: !1,
				fitViewOptions: void 0
			})) : p({}), u?.length > 0 && (s && console.log("React Flow: trigger node changes", u), t?.(u)));
		},
		updateNodePositions: (e, t = !1) => {
			let n = [], r = [], { nodeLookup: i, triggerNodeChanges: a, connection: o, updateConnection: s, onNodesChangeMiddlewareMap: c } = m();
			for (let [a, c] of e) {
				let e = i.get(a), l = !!(e?.expandParent && e?.parentId && c?.position), u = {
					id: a,
					type: "position",
					position: l ? {
						x: Math.max(0, c.position.x),
						y: Math.max(0, c.position.y)
					} : c.position,
					dragging: t
				};
				if (e && o.inProgress && o.fromNode.id === e.id) {
					let t = getHandlePosition(e, o.fromHandle, F.Left, !0);
					s({
						...o,
						from: t
					});
				}
				l && e.parentId && n.push({
					id: a,
					parentId: e.parentId,
					rect: {
						...c.internals.positionAbsolute,
						width: c.measured.width ?? 0,
						height: c.measured.height ?? 0
					}
				}), r.push(u);
			}
			if (n.length > 0) {
				let { parentLookup: e, nodeOrigin: t } = m(), a = handleExpandParent(n, i, e, t);
				r.push(...a);
			}
			for (let e of c.values()) r = e(r);
			a(r);
		},
		triggerNodeChanges: (e) => {
			let { onNodesChange: t, setNodes: n, nodes: r, hasDefaultNodes: i, debug: a } = m();
			e?.length && (i && n(applyNodeChanges(e, r)), a && console.log("React Flow: trigger node changes", e), t?.(e));
		},
		triggerEdgeChanges: (e) => {
			let { onEdgesChange: t, setEdges: n, edges: r, hasDefaultEdges: i, debug: a } = m();
			e?.length && (i && n(applyEdgeChanges(e, r)), a && console.log("React Flow: trigger edge changes", e), t?.(e));
		},
		addSelectedNodes: (e) => {
			let { multiSelectionActive: t, edgeLookup: n, nodeLookup: r, triggerNodeChanges: i, triggerEdgeChanges: a } = m();
			if (t) {
				i(e.map((e) => createSelectionChange(e, !0)));
				return;
			}
			i(getSelectionChanges(r, /* @__PURE__ */ new Set([...e]), !0)), a(getSelectionChanges(n));
		},
		addSelectedEdges: (e) => {
			let { multiSelectionActive: t, edgeLookup: n, nodeLookup: r, triggerNodeChanges: i, triggerEdgeChanges: a } = m();
			if (t) {
				a(e.map((e) => createSelectionChange(e, !0)));
				return;
			}
			a(getSelectionChanges(n, /* @__PURE__ */ new Set([...e]))), i(getSelectionChanges(r, /* @__PURE__ */ new Set(), !0));
		},
		unselectNodesAndEdges: ({ nodes: e, edges: t } = {}) => {
			let { edges: n, nodes: r, nodeLookup: i, triggerNodeChanges: a, triggerEdgeChanges: o } = m(), s = e || r, c = t || n, l = [];
			for (let e of s) {
				if (!e.selected) continue;
				let t = i.get(e.id);
				t && (t.selected = !1), l.push(createSelectionChange(e.id, !1));
			}
			let u = [];
			for (let e of c) e.selected && u.push(createSelectionChange(e.id, !1));
			a(l), o(u);
		},
		setMinZoom: (e) => {
			let { panZoom: t, maxZoom: n } = m();
			t?.setScaleExtent([e, n]), p({ minZoom: e });
		},
		setMaxZoom: (e) => {
			let { panZoom: t, minZoom: n } = m();
			t?.setScaleExtent([n, e]), p({ maxZoom: e });
		},
		setTranslateExtent: (e) => {
			m().panZoom?.setTranslateExtent(e), p({ translateExtent: e });
		},
		resetSelectedElements: () => {
			let { edges: e, nodes: t, triggerNodeChanges: n, triggerEdgeChanges: r, elementsSelectable: i } = m();
			if (!i) return;
			let a = t.reduce((e, t) => t.selected ? [...e, createSelectionChange(t.id, !1)] : e, []), o = e.reduce((e, t) => t.selected ? [...e, createSelectionChange(t.id, !1)] : e, []);
			n(a), r(o);
		},
		setNodeExtent: (e) => {
			let { nodes: t, nodeLookup: n, parentLookup: r, nodeOrigin: i, elevateNodesOnSelect: a, nodeExtent: o, zIndexMode: s } = m();
			(e[0][0] !== o[0][0] || e[0][1] !== o[0][1] || e[1][0] !== o[1][0] || e[1][1] !== o[1][1]) && (adoptUserNodes(t, n, r, {
				nodeOrigin: i,
				nodeExtent: e,
				elevateNodesOnSelect: a,
				checkEquality: !1,
				zIndexMode: s
			}), p({ nodeExtent: e }));
		},
		panBy: (e) => {
			let { transform: t, width: n, height: r, panZoom: i, translateExtent: a } = m();
			return panBy({
				delta: e,
				panZoom: i,
				transform: t,
				translateExtent: a,
				width: n,
				height: r
			});
		},
		setCenter: async (e, t, n) => {
			let { width: r, height: i, maxZoom: a, panZoom: o } = m();
			if (!o) return !1;
			let s = n?.zoom === void 0 ? a : n.zoom;
			return await o.setViewport({
				x: r / 2 - e * s,
				y: i / 2 - t * s,
				zoom: s
			}, {
				duration: n?.duration,
				ease: n?.ease,
				interpolate: n?.interpolate
			}), !0;
		},
		cancelConnection: () => {
			p({ connection: { ...M } });
		},
		updateConnection: (e) => {
			p({ connection: e });
		},
		reset: () => p({ ...getInitialState() })
	};
}, Object.is);
function ReactFlowProvider({ initialNodes: e, initialEdges: t, defaultNodes: n, defaultEdges: r, initialWidth: i, initialHeight: a, initialMinZoom: o, initialMaxZoom: s, initialFitViewOptions: c, fitView: l, nodeOrigin: u, nodeExtent: d, zIndexMode: f, children: p }) {
	let [m] = y(() => createStore({
		nodes: e,
		edges: t,
		defaultNodes: n,
		defaultEdges: r,
		width: i,
		height: a,
		fitView: l,
		minZoom: o,
		maxZoom: s,
		fitViewOptions: c,
		nodeOrigin: u,
		nodeExtent: d,
		zIndexMode: f
	}));
	return x(ne, {
		value: m,
		children: x(BatchProvider, { children: x(HandleConfigProvider, { children: p }) })
	});
}
function Wrapper({ children: e, nodes: t, edges: n, defaultNodes: r, defaultEdges: i, width: a, height: o, fitView: s, fitViewOptions: c, minZoom: l, maxZoom: u, nodeOrigin: d, nodeExtent: f, zIndexMode: p }) {
	return m(X) ? x(b, { children: e }) : x(ReactFlowProvider, {
		initialNodes: t,
		initialEdges: n,
		defaultNodes: r,
		defaultEdges: i,
		initialWidth: a,
		initialHeight: o,
		fitView: s,
		initialFitViewOptions: c,
		initialMinZoom: l,
		initialMaxZoom: u,
		nodeOrigin: d,
		nodeExtent: f,
		zIndexMode: p,
		children: e
	});
}
var Ze = {
	width: "100%",
	height: "100%",
	overflow: "hidden",
	position: "relative",
	zIndex: 0
};
function ReactFlow({ nodes: e, edges: t, defaultNodes: n, defaultEdges: r, className: i, nodeTypes: a, edgeTypes: o, onNodeClick: s, onEdgeClick: c, onInit: l, onMove: u, onMoveStart: d, onMoveEnd: f, onConnect: m, onConnectStart: h, onConnectEnd: g, onClickConnectStart: _, onClickConnectEnd: v, onNodeMouseEnter: y, onNodeMouseMove: b, onNodeMouseLeave: C, onNodeContextMenu: w, onNodeDoubleClick: T, onNodeDragStart: D, onNodeDrag: O, onNodeDragStop: k, onNodesDelete: M, onEdgesDelete: P, onDelete: F, onSelectionChange: I, onSelectionDragStart: L, onSelectionDrag: R, onSelectionDragStop: z, onSelectionContextMenu: B, onSelectionStart: V, onSelectionEnd: H, onBeforeDelete: U, connectionMode: W, connectionLineType: G = N.Bezier, connectionLineStyle: K, connectionLineComponent: q, connectionLineContainerStyle: J, deleteKeyCode: ee = "Backspace", selectionKeyCode: Y = "Shift", selectionOnDrag: te = !1, selectionMode: X = j.Full, panActivationKeyCode: ne = "Space", multiSelectionKeyCode: re = isMacOs() ? "Meta" : "Control", zoomActivationKeyCode: ie = isMacOs() ? "Meta" : "Control", snapToGrid: ae, snapGrid: oe, onlyRenderVisibleElements: se = !1, selectNodesOnDrag: ce, nodesDraggable: Z, autoPanOnNodeFocus: le, nodesConnectable: fe, nodesFocusable: pe, nodeOrigin: me = ue, edgesFocusable: he, edgesReconnectable: ge, elementsSelectable: _e = !0, defaultViewport: ve = de, minZoom: Q = .5, maxZoom: ye = 2, translateExtent: be = E, preventScrolling: xe = !0, nodeExtent: Se, defaultMarkerColor: Ce = "#b1b1b7", zoomOnScroll: we = !0, zoomOnPinch: Te = !0, panOnScroll: Ee = !1, panOnScrollSpeed: De = .5, panOnScrollMode: Oe = A.Free, zoomOnDoubleClick: ke = !0, panOnDrag: Ae = !0, onPaneClick: je, onPaneMouseEnter: Me, onPaneMouseMove: Ne, onPaneMouseLeave: Pe, onPaneScroll: Fe, onPaneContextMenu: Ie, paneClickDistance: Le = 1, nodeClickDistance: Re = 0, children: ze, onReconnect: Be, onReconnectStart: Ve, onReconnectEnd: He, onEdgeContextMenu: Ue, onEdgeDoubleClick: We, onEdgeMouseEnter: Ge, onEdgeMouseMove: Ke, onEdgeMouseLeave: qe, reconnectRadius: Je = 10, onNodesChange: Xe, onEdgesChange: Qe, noDragClassName: $ = "nodrag", noWheelClassName: $e = "nowheel", noPanClassName: et = "nopan", fitView: tt, fitViewOptions: nt, connectOnClick: rt, attributionPosition: it, proOptions: at, defaultEdgeOptions: ot, elevateNodesOnSelect: st = !0, elevateEdgesOnSelect: ct = !1, disableKeyboardA11y: lt = !1, autoPanOnConnect: ut, autoPanOnNodeDrag: dt, autoPanOnSelection: ft = !0, autoPanSpeed: pt, connectionRadius: mt, isValidConnection: ht, onError: gt, style: _t, id: vt, nodeDragThreshold: yt, connectionDragThreshold: bt, viewport: xt, onViewportChange: St, width: Ct, height: wt, colorMode: Tt = "light", debug: Et, onScroll: Dt, ariaLabelConfig: Ot, zIndexMode: kt = "basic", ...At }, jt) {
	let Mt = vt || "1", Nt = useColorModeClass(Tt), Pt = p((e) => {
		e.currentTarget.scrollTo({
			top: 0,
			left: 0,
			behavior: "instant"
		}), Dt?.(e);
	}, [Dt]);
	return x("div", {
		"data-testid": "rf__wrapper",
		...At,
		onScroll: Pt,
		style: {
			..._t,
			...Ze
		},
		ref: jt,
		className: cc([
			"react-flow",
			i,
			Nt
		]),
		id: vt,
		role: "application",
		children: S(Wrapper, {
			nodes: e,
			edges: t,
			width: Ct,
			height: wt,
			fitView: tt,
			fitViewOptions: nt,
			minZoom: Q,
			maxZoom: ye,
			nodeOrigin: me,
			nodeExtent: Se,
			zIndexMode: kt,
			children: [
				x(StoreUpdater, {
					nodes: e,
					edges: t,
					defaultNodes: n,
					defaultEdges: r,
					onConnect: m,
					onConnectStart: h,
					onConnectEnd: g,
					onClickConnectStart: _,
					onClickConnectEnd: v,
					nodesDraggable: Z,
					autoPanOnNodeFocus: le,
					nodesConnectable: fe,
					nodesFocusable: pe,
					edgesFocusable: he,
					edgesReconnectable: ge,
					elementsSelectable: _e,
					elevateNodesOnSelect: st,
					elevateEdgesOnSelect: ct,
					minZoom: Q,
					maxZoom: ye,
					nodeExtent: Se,
					onNodesChange: Xe,
					onEdgesChange: Qe,
					snapToGrid: ae,
					snapGrid: oe,
					connectionMode: W,
					translateExtent: be,
					connectOnClick: rt,
					defaultEdgeOptions: ot,
					fitView: tt,
					fitViewOptions: nt,
					onNodesDelete: M,
					onEdgesDelete: P,
					onDelete: F,
					onNodeDragStart: D,
					onNodeDrag: O,
					onNodeDragStop: k,
					onSelectionDrag: R,
					onSelectionDragStart: L,
					onSelectionDragStop: z,
					onMove: u,
					onMoveStart: d,
					onMoveEnd: f,
					noPanClassName: et,
					nodeOrigin: me,
					rfId: Mt,
					autoPanOnConnect: ut,
					autoPanOnNodeDrag: dt,
					autoPanSpeed: pt,
					onError: gt,
					connectionRadius: mt,
					isValidConnection: ht,
					selectNodesOnDrag: ce,
					nodeDragThreshold: yt,
					connectionDragThreshold: bt,
					onBeforeDelete: U,
					debug: Et,
					ariaLabelConfig: Ot,
					zIndexMode: kt
				}),
				x(Ye, {
					onInit: l,
					onNodeClick: s,
					onEdgeClick: c,
					onNodeMouseEnter: y,
					onNodeMouseMove: b,
					onNodeMouseLeave: C,
					onNodeContextMenu: w,
					onNodeDoubleClick: T,
					nodeTypes: a,
					edgeTypes: o,
					connectionLineType: G,
					connectionLineStyle: K,
					connectionLineComponent: q,
					connectionLineContainerStyle: J,
					selectionKeyCode: Y,
					selectionOnDrag: te,
					selectionMode: X,
					deleteKeyCode: ee,
					multiSelectionKeyCode: re,
					panActivationKeyCode: ne,
					zoomActivationKeyCode: ie,
					onlyRenderVisibleElements: se,
					defaultViewport: ve,
					translateExtent: be,
					minZoom: Q,
					maxZoom: ye,
					preventScrolling: xe,
					zoomOnScroll: we,
					zoomOnPinch: Te,
					zoomOnDoubleClick: ke,
					panOnScroll: Ee,
					panOnScrollSpeed: De,
					panOnScrollMode: Oe,
					panOnDrag: Ae,
					autoPanOnSelection: ft,
					onPaneClick: je,
					onPaneMouseEnter: Me,
					onPaneMouseMove: Ne,
					onPaneMouseLeave: Pe,
					onPaneScroll: Fe,
					onPaneContextMenu: Ie,
					paneClickDistance: Le,
					nodeClickDistance: Re,
					onSelectionContextMenu: B,
					onSelectionStart: V,
					onSelectionEnd: H,
					onReconnect: Be,
					onReconnectStart: Ve,
					onReconnectEnd: He,
					onEdgeContextMenu: Ue,
					onEdgeDoubleClick: We,
					onEdgeMouseEnter: Ge,
					onEdgeMouseMove: Ke,
					onEdgeMouseLeave: qe,
					reconnectRadius: Je,
					defaultMarkerColor: Ce,
					noDragClassName: $,
					noWheelClassName: $e,
					noPanClassName: et,
					rfId: Mt,
					disableKeyboardA11y: lt,
					nodeExtent: Se,
					viewport: xt,
					onViewportChange: St,
					nodesDraggable: Z
				}),
				x(SelectionListener, { onSelectionChange: I }),
				ze,
				x(Attribution, {
					proOptions: at,
					position: it
				}),
				x(A11yDescriptions, {
					rfId: Mt,
					disableKeyboardA11y: lt
				})
			]
		})
	});
}
var Qe = fixedForwardRef(ReactFlow), selector$6 = (e) => e.domNode?.querySelector(".react-flow__edgelabel-renderer");
function EdgeLabelRenderer({ children: e }) {
	let t = useStore(selector$6);
	return t ? w(e, t) : null;
}
var selector$5 = (e) => e.domNode?.querySelector(".react-flow__viewport-portal");
function ViewportPortal({ children: e }) {
	let t = useStore(selector$5);
	return t ? w(e, t) : null;
}
T.error014();
function useNodesData(e) {
	return useStore(p((t) => {
		let n = [], r = Array.isArray(e), i = r ? e : [e];
		for (let e of i) {
			let r = t.nodeLookup.get(e);
			r && n.push({
				id: r.id,
				type: r.type,
				data: r.data
			});
		}
		return r ? n : n[0] ?? null;
	}, [e]), shallowNodeData);
}
function LinePattern({ dimensions: e, lineWidth: t, variant: n, className: r }) {
	return x("path", {
		strokeWidth: t,
		d: `M${e[0] / 2} 0 V${e[1]} M0 ${e[1] / 2} H${e[0]}`,
		className: cc([
			"react-flow__background-pattern",
			n,
			r
		])
	});
}
function DotPattern({ radius: e, className: t }) {
	return x("circle", {
		cx: e,
		cy: e,
		r: e,
		className: cc([
			"react-flow__background-pattern",
			"dots",
			t
		])
	});
}
var $;
(function(e) {
	e.Lines = "lines", e.Dots = "dots", e.Cross = "cross";
})($ ||= {});
var $e = {
	[$.Dots]: 1,
	[$.Lines]: 1,
	[$.Cross]: 6
}, selector$3 = (e) => ({
	transform: e.transform,
	patternId: `pattern-${e.rfId}`
});
function BackgroundComponent({ id: e, variant: t = $.Dots, gap: n = 20, size: r, lineWidth: i = 1, offset: a = 0, color: o, bgColor: s, style: c, className: l, patternClassName: u }) {
	let d = v(null), { transform: f, patternId: p } = useStore(selector$3, shallow$1), m = r || $e[t], h = t === $.Dots, g = t === $.Cross, _ = Array.isArray(n) ? n : [n, n], y = [_[0] * f[2] || 1, _[1] * f[2] || 1], b = m * f[2], C = Array.isArray(a) ? a : [a, a], w = g ? [b, b] : y, T = [C[0] * f[2] + w[0] / 2, C[1] * f[2] + w[1] / 2], E = `${p}${e || ""}`;
	return S("svg", {
		className: cc(["react-flow__background", l]),
		style: {
			...c,
			...Q,
			"--xy-background-color-props": s,
			"--xy-background-pattern-color-props": o
		},
		ref: d,
		"data-testid": "rf__background",
		children: [x("pattern", {
			id: E,
			x: f[0] % y[0],
			y: f[1] % y[1],
			width: y[0],
			height: y[1],
			patternUnits: "userSpaceOnUse",
			patternTransform: `translate(-${T[0]},-${T[1]})`,
			children: h ? x(DotPattern, {
				radius: b / 2,
				className: u
			}) : x(LinePattern, {
				dimensions: w,
				lineWidth: i,
				variant: t,
				className: u
			})
		}), x("rect", {
			x: "0",
			y: "0",
			width: "100%",
			height: "100%",
			fill: `url(#${E})`
		})]
	});
}
BackgroundComponent.displayName = "Background";
var et = f(BackgroundComponent);
function PlusIcon() {
	return x("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 32 32",
		children: x("path", { d: "M32 18.133H18.133V32h-4.266V18.133H0v-4.266h13.867V0h4.266v13.867H32z" })
	});
}
function MinusIcon() {
	return x("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 32 5",
		children: x("path", { d: "M0 0h32v4.2H0z" })
	});
}
function FitViewIcon() {
	return x("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 32 30",
		children: x("path", { d: "M3.692 4.63c0-.53.4-.938.939-.938h5.215V0H4.708C2.13 0 0 2.054 0 4.63v5.216h3.692V4.631zM27.354 0h-5.2v3.692h5.17c.53 0 .984.4.984.939v5.215H32V4.631A4.624 4.624 0 0027.354 0zm.954 24.83c0 .532-.4.94-.939.94h-5.215v3.768h5.215c2.577 0 4.631-2.13 4.631-4.707v-5.139h-3.692v5.139zm-23.677.94c-.531 0-.939-.4-.939-.94v-5.138H0v5.139c0 2.577 2.13 4.707 4.708 4.707h5.138V25.77H4.631z" })
	});
}
function LockIcon() {
	return x("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 25 32",
		children: x("path", { d: "M21.333 10.667H19.81V7.619C19.81 3.429 16.38 0 12.19 0 8 0 4.571 3.429 4.571 7.619v3.048H3.048A3.056 3.056 0 000 13.714v15.238A3.056 3.056 0 003.048 32h18.285a3.056 3.056 0 003.048-3.048V13.714a3.056 3.056 0 00-3.048-3.047zM12.19 24.533a3.056 3.056 0 01-3.047-3.047 3.056 3.056 0 013.047-3.048 3.056 3.056 0 013.048 3.048 3.056 3.056 0 01-3.048 3.047zm4.724-13.866H7.467V7.619c0-2.59 2.133-4.724 4.723-4.724 2.591 0 4.724 2.133 4.724 4.724v3.048z" })
	});
}
function UnlockIcon() {
	return x("svg", {
		xmlns: "http://www.w3.org/2000/svg",
		viewBox: "0 0 25 32",
		children: x("path", { d: "M21.333 10.667H19.81V7.619C19.81 3.429 16.38 0 12.19 0c-4.114 1.828-1.37 2.133.305 2.438 1.676.305 4.42 2.59 4.42 5.181v3.048H3.047A3.056 3.056 0 000 13.714v15.238A3.056 3.056 0 003.048 32h18.285a3.056 3.056 0 003.048-3.048V13.714a3.056 3.056 0 00-3.048-3.047zM12.19 24.533a3.056 3.056 0 01-3.047-3.047 3.056 3.056 0 013.047-3.048 3.056 3.056 0 013.048 3.048 3.056 3.056 0 01-3.048 3.047z" })
	});
}
function ControlButton({ children: e, className: t, ...n }) {
	return x("button", {
		type: "button",
		className: cc(["react-flow__controls-button", t]),
		...n,
		children: e
	});
}
var selector$2 = (e) => ({
	isInteractive: e.nodesDraggable || e.nodesConnectable || e.elementsSelectable,
	minZoomReached: e.transform[2] <= e.minZoom,
	maxZoomReached: e.transform[2] >= e.maxZoom,
	ariaLabelConfig: e.ariaLabelConfig
});
function ControlsComponent({ style: e, showZoom: t = !0, showFitView: n = !0, showInteractive: r = !0, fitViewOptions: i, onZoomIn: a, onZoomOut: o, onFitView: s, onInteractiveChange: c, className: l, children: u, position: d = "bottom-left", orientation: f = "vertical", "aria-label": p }) {
	let m = useStoreApi(), { isInteractive: h, minZoomReached: g, maxZoomReached: _, ariaLabelConfig: v } = useStore(selector$2, shallow$1), { zoomIn: y, zoomOut: C, fitView: w } = useReactFlow(), onZoomInHandler = () => {
		y(), a?.();
	}, onZoomOutHandler = () => {
		C(), o?.();
	}, onFitViewHandler = () => {
		w(i), s?.();
	}, onToggleInteractivity = () => {
		m.setState({
			nodesDraggable: !h,
			nodesConnectable: !h,
			elementsSelectable: !h
		}), c?.(!h);
	};
	return S(Z, {
		className: cc([
			"react-flow__controls",
			f === "horizontal" ? "horizontal" : "vertical",
			l
		]),
		position: d,
		style: e,
		"data-testid": "rf__controls",
		"aria-label": p ?? v["controls.ariaLabel"],
		children: [
			t && S(b, { children: [x(ControlButton, {
				onClick: onZoomInHandler,
				className: "react-flow__controls-zoomin",
				title: v["controls.zoomIn.ariaLabel"],
				"aria-label": v["controls.zoomIn.ariaLabel"],
				disabled: _,
				children: x(PlusIcon, {})
			}), x(ControlButton, {
				onClick: onZoomOutHandler,
				className: "react-flow__controls-zoomout",
				title: v["controls.zoomOut.ariaLabel"],
				"aria-label": v["controls.zoomOut.ariaLabel"],
				disabled: g,
				children: x(MinusIcon, {})
			})] }),
			n && x(ControlButton, {
				className: "react-flow__controls-fitview",
				onClick: onFitViewHandler,
				title: v["controls.fitView.ariaLabel"],
				"aria-label": v["controls.fitView.ariaLabel"],
				children: x(FitViewIcon, {})
			}),
			r && x(ControlButton, {
				className: "react-flow__controls-interactive",
				onClick: onToggleInteractivity,
				title: v["controls.interactive.ariaLabel"],
				"aria-label": v["controls.interactive.ariaLabel"],
				children: x(h ? UnlockIcon : LockIcon, {})
			}),
			u
		]
	});
}
ControlsComponent.displayName = "Controls";
var tt = f(ControlsComponent);
function MiniMapNodeComponent({ id: e, x: t, y: n, width: r, height: i, style: a, color: o, strokeColor: s, strokeWidth: c, className: l, borderRadius: u, shapeRendering: d, selected: f, onClick: p }) {
	let { background: m, backgroundColor: h } = a || {}, g = o || m || h;
	return x("rect", {
		className: cc([
			"react-flow__minimap-node",
			{ selected: f },
			l
		]),
		x: t,
		y: n,
		rx: u,
		ry: u,
		width: r,
		height: i,
		style: {
			fill: g,
			stroke: s,
			strokeWidth: c
		},
		shapeRendering: d,
		onClick: p ? (t) => p(t, e) : void 0
	});
}
var nt = f(MiniMapNodeComponent), selectorNodeIds = (e) => e.nodes.map((e) => e.id), getAttrFunction = (e) => e instanceof Function ? e : () => e;
function MiniMapNodes({ nodeStrokeColor: e, nodeColor: t, nodeClassName: n = "", nodeBorderRadius: r = 5, nodeStrokeWidth: i, nodeComponent: a = nt, onClick: o }) {
	let s = useStore(selectorNodeIds, shallow$1), c = getAttrFunction(t), l = getAttrFunction(e), u = getAttrFunction(n), d = typeof window > "u" || window.chrome ? "crispEdges" : "geometricPrecision";
	return x(b, { children: s.map((e) => x(rt, {
		id: e,
		nodeColorFunc: c,
		nodeStrokeColorFunc: l,
		nodeClassNameFunc: u,
		nodeBorderRadius: r,
		nodeStrokeWidth: i,
		NodeComponent: a,
		onClick: o,
		shapeRendering: d
	}, e)) });
}
function NodeComponentWrapperInner({ id: e, nodeColorFunc: t, nodeStrokeColorFunc: n, nodeClassNameFunc: r, nodeBorderRadius: i, nodeStrokeWidth: a, shapeRendering: o, NodeComponent: s, onClick: c }) {
	let { node: l, x: u, y: d, width: f, height: p } = useStore((t) => {
		let n = t.nodeLookup.get(e);
		if (!n) return {
			node: void 0,
			x: 0,
			y: 0,
			width: 0,
			height: 0
		};
		let r = n.internals.userNode, { x: i, y: a } = n.internals.positionAbsolute, { width: o, height: s } = getNodeDimensions(r);
		return {
			node: r,
			x: i,
			y: a,
			width: o,
			height: s
		};
	}, shallow$1);
	return !l || l.hidden || !nodeHasDimensions(l) ? null : x(s, {
		x: u,
		y: d,
		width: f,
		height: p,
		style: l.style,
		selected: !!l.selected,
		className: r(l),
		color: t(l),
		borderRadius: i,
		strokeColor: n(l),
		strokeWidth: a,
		shapeRendering: o,
		onClick: c,
		id: l.id
	});
}
var rt = f(NodeComponentWrapperInner), it = f(MiniMapNodes), at = 200, ot = 150, filterHidden = (e) => !e.hidden, selector$1 = (e) => {
	let t = {
		x: -e.transform[0] / e.transform[2],
		y: -e.transform[1] / e.transform[2],
		width: e.width / e.transform[2],
		height: e.height / e.transform[2]
	}, n = !1;
	for (let t of e.nodeLookup.values()) if (!t.hidden) {
		n = !0;
		break;
	}
	return {
		viewBB: t,
		boundingRect: n ? getBoundsOfRects(getInternalNodesBounds(e.nodeLookup, { filter: filterHidden }), t) : t,
		rfId: e.rfId,
		panZoom: e.panZoom,
		translateExtent: e.translateExtent,
		flowWidth: e.width,
		flowHeight: e.height,
		ariaLabelConfig: e.ariaLabelConfig
	};
}, rectEqual = (e, t) => e.x === t.x && e.y === t.y && e.width === t.width && e.height === t.height, areEqual = (e, t) => rectEqual(e.viewBB, t.viewBB) && rectEqual(e.boundingRect, t.boundingRect) && e.rfId === t.rfId && e.panZoom === t.panZoom && e.translateExtent === t.translateExtent && e.flowWidth === t.flowWidth && e.flowHeight === t.flowHeight && e.ariaLabelConfig === t.ariaLabelConfig, st = "react-flow__minimap-desc";
function MiniMapComponent({ style: e, className: t, nodeStrokeColor: n, nodeColor: r, nodeClassName: i = "", nodeBorderRadius: a = 5, nodeStrokeWidth: o, nodeComponent: s, bgColor: c, maskColor: l, maskStrokeColor: u, maskStrokeWidth: d, position: f = "bottom-right", onClick: m, onNodeClick: g, pannable: _ = !1, zoomable: y = !1, ariaLabel: b, inversePan: C, zoomStep: w = 1, offsetScale: T = 5 }) {
	let E = useStoreApi(), D = v(null), { boundingRect: O, panZoom: k, viewBB: A, rfId: j, translateExtent: M, flowWidth: N, flowHeight: P, ariaLabelConfig: F } = useStore(selector$1, areEqual), I = e?.width ?? at, L = e?.height ?? ot, R = O.width / I, z = O.height / L, B = Math.max(R, z), V = B * I, H = B * L, U = T * B, W = O.x - (V - O.width) / 2 - U, G = O.y - (H - O.height) / 2 - U, K = V + U * 2, q = H + U * 2, J = `${st}-${j}`, ee = v(0), Y = v();
	ee.current = B, h(() => {
		let e = E.getState().panZoom;
		if (D.current && e) return Y.current = XYMinimap({
			domNode: D.current,
			panZoom: e,
			getTransform: () => E.getState().transform,
			getViewScale: () => ee.current
		}), () => {
			Y.current?.destroy();
		};
	}, [k]), h(() => {
		Y.current?.update({
			translateExtent: M,
			width: N,
			height: P,
			inversePan: C,
			pannable: _,
			zoomStep: w,
			zoomable: y
		});
	}, [
		_,
		y,
		C,
		w,
		M,
		N,
		P
	]);
	let te = m ? (e) => {
		let [t, n] = Y.current?.pointer(e) || [0, 0];
		m(e, {
			x: t,
			y: n
		});
	} : void 0, X = p((e, t) => {
		let n = E.getState().nodeLookup.get(t).internals.userNode;
		g?.(e, n);
	}, [g]), ne = g ? X : void 0, re = b ?? F["minimap.ariaLabel"];
	return x(Z, {
		position: f,
		style: {
			...e,
			"--xy-minimap-background-color-props": typeof c == "string" ? c : void 0,
			"--xy-minimap-mask-background-color-props": typeof l == "string" ? l : void 0,
			"--xy-minimap-mask-stroke-color-props": typeof u == "string" ? u : void 0,
			"--xy-minimap-mask-stroke-width-props": typeof d == "number" ? d * B : void 0,
			"--xy-minimap-node-background-color-props": typeof r == "string" ? r : void 0,
			"--xy-minimap-node-stroke-color-props": typeof n == "string" ? n : void 0,
			"--xy-minimap-node-stroke-width-props": typeof o == "number" ? o : void 0
		},
		className: cc(["react-flow__minimap", t]),
		"data-testid": "rf__minimap",
		children: S("svg", {
			width: I,
			height: L,
			viewBox: `${W} ${G} ${K} ${q}`,
			className: "react-flow__minimap-svg",
			role: "img",
			"aria-labelledby": J,
			ref: D,
			onClick: te,
			children: [
				re && x("title", {
					id: J,
					children: re
				}),
				x(it, {
					onClick: ne,
					nodeColor: r,
					nodeStrokeColor: n,
					nodeBorderRadius: a,
					nodeClassName: i,
					nodeStrokeWidth: o,
					nodeComponent: s
				}),
				x("path", {
					className: "react-flow__minimap-mask",
					d: `M${W - U},${G - U}h${K + U * 2}v${q + U * 2}h${-K - U * 2}z
        M${A.x},${A.y}h${A.width}v${A.height}h${-A.width}z`,
					fillRule: "evenodd",
					pointerEvents: "none"
				})
			]
		})
	});
}
MiniMapComponent.displayName = "MiniMap", f(MiniMapComponent);
var scaleSelector = (e) => (t) => e ? `${Math.max(1 / t.transform[2], 1)}` : void 0, ct = {
	[K.Line]: "right",
	[K.Handle]: "bottom-right"
};
function ResizeControl({ nodeId: e, position: t, variant: n = K.Handle, className: r, style: i = void 0, children: a, color: o, minWidth: s = 10, minHeight: c = 10, maxWidth: l = Number.MAX_VALUE, maxHeight: u = Number.MAX_VALUE, keepAspectRatio: d = !1, resizeDirection: f, autoScale: m = !0, shouldResize: g, onResizeStart: _, onResize: y, onResizeEnd: b }) {
	let S = useNodeId(), C = typeof e == "string" ? e : S, w = useStoreApi(), T = v(null), E = n === K.Handle, D = useStore(p(scaleSelector(E && m), [E, m]), shallow$1), O = v(null), k = t ?? ct[n];
	h(() => {
		if (!(!T.current || !C)) return O.current ||= XYResizer({
			domNode: T.current,
			nodeId: C,
			getStoreItems: () => {
				let { nodeLookup: e, transform: t, snapGrid: n, snapToGrid: r, nodeOrigin: i, domNode: a } = w.getState();
				return {
					nodeLookup: e,
					transform: t,
					snapGrid: n,
					snapToGrid: r,
					nodeOrigin: i,
					paneDomNode: a
				};
			},
			onChange: (e, t) => {
				let { triggerNodeChanges: n, nodeLookup: r, parentLookup: i, nodeOrigin: a } = w.getState(), o = [], s = {
					x: e.x,
					y: e.y
				}, c = r.get(C);
				if (c && c.expandParent && c.parentId) {
					let t = c.origin ?? a, n = e.width ?? c.measured.width ?? 0, l = e.height ?? c.measured.height ?? 0, u = handleExpandParent([{
						id: c.id,
						parentId: c.parentId,
						rect: {
							width: n,
							height: l,
							...evaluateAbsolutePosition({
								x: e.x ?? c.position.x,
								y: e.y ?? c.position.y
							}, {
								width: n,
								height: l
							}, c.parentId, r, t)
						}
					}], r, i, a);
					o.push(...u), s.x = e.x ? Math.max(t[0] * n, e.x) : void 0, s.y = e.y ? Math.max(t[1] * l, e.y) : void 0;
				}
				if (s.x !== void 0 && s.y !== void 0) {
					let e = {
						id: C,
						type: "position",
						position: { ...s }
					};
					o.push(e);
				}
				if (e.width !== void 0 && e.height !== void 0) {
					let t = {
						id: C,
						type: "dimensions",
						resizing: !0,
						setAttributes: f ? f === "horizontal" ? "width" : "height" : !0,
						dimensions: {
							width: e.width,
							height: e.height
						}
					};
					o.push(t);
				}
				for (let e of t) {
					let t = {
						...e,
						type: "position"
					};
					o.push(t);
				}
				n(o);
			},
			onEnd: ({ width: e, height: t }) => {
				let n = {
					id: C,
					type: "dimensions",
					resizing: !1,
					dimensions: {
						width: e,
						height: t
					}
				};
				w.getState().triggerNodeChanges([n]);
			}
		}), O.current.update({
			controlPosition: k,
			boundaries: {
				minWidth: s,
				minHeight: c,
				maxWidth: l,
				maxHeight: u
			},
			keepAspectRatio: d,
			resizeDirection: f,
			onResizeStart: _,
			onResize: y,
			onResizeEnd: b,
			shouldResize: g
		}), () => {
			O.current?.destroy();
		};
	}, [
		k,
		s,
		c,
		l,
		u,
		d,
		_,
		y,
		b,
		g
	]);
	let A = k.split("-");
	return x("div", {
		className: cc([
			"react-flow__resize-control",
			"nodrag",
			...A,
			n,
			r
		]),
		ref: T,
		style: {
			...i,
			scale: D,
			...o && { [E ? "backgroundColor" : "borderColor"]: o }
		},
		children: a
	});
}
f(ResizeControl);
var selector = (e) => e.domNode?.querySelector(".react-flow__renderer");
function NodeToolbarPortal({ children: e }) {
	let t = useStore(selector);
	return t ? w(e, t) : null;
}
var nodeEqualityFn = (e, t) => e?.internals.positionAbsolute.x !== t?.internals.positionAbsolute.x || e?.internals.positionAbsolute.y !== t?.internals.positionAbsolute.y || e?.measured.width !== t?.measured.width || e?.measured.height !== t?.measured.height || e?.selected !== t?.selected || e?.internals.z !== t?.internals.z, nodesEqualityFn = (e, t) => {
	if (e.size !== t.size) return !1;
	for (let [n, r] of e) if (nodeEqualityFn(r, t.get(n))) return !1;
	return !0;
}, storeSelector = (e) => ({
	x: e.transform[0],
	y: e.transform[1],
	zoom: e.transform[2],
	selectedNodesCount: e.nodes.filter((e) => e.selected).length
});
function NodeToolbar({ nodeId: e, children: t, className: n, style: r, isVisible: i, position: a = F.Top, offset: o = 10, align: s = "center", ...c }) {
	let l = useNodeId(), u = useStore(p((t) => (Array.isArray(e) ? e : [e || l || ""]).reduce((e, n) => {
		let r = t.nodeLookup.get(n);
		return r && e.set(r.id, r), e;
	}, /* @__PURE__ */ new Map()), [e, l]), nodesEqualityFn), { x: d, y: f, zoom: m, selectedNodesCount: h } = useStore(storeSelector, shallow$1);
	if (!(typeof i == "boolean" ? i : u.size === 1 && u.values().next().value?.selected && h === 1) || !u.size) return null;
	let g = getInternalNodesBounds(u), _ = Array.from(u.values()), v = Math.max(..._.map((e) => e.internals.z + 1)), y = {
		position: "absolute",
		transform: getNodeToolbarTransform(g, {
			x: d,
			y: f,
			zoom: m
		}, a, o, s),
		zIndex: v,
		...r
	};
	return x(NodeToolbarPortal, { children: x("div", {
		style: y,
		className: cc(["react-flow__node-toolbar", n]),
		...c,
		"data-id": _.reduce((e, t) => `${e}${t.id} `, "").trim(),
		children: t
	}) });
}
//#endregion
export { getViewportForBounds as C, getSmoothStepPath as S, nodeToRect as T, F as _, Ce as a, getNodeDimensions as b, ReactFlowProvider as c, applyNodeChanges as d, Qe as f, useStoreApi as g, useStore as h, EdgeLabelRenderer as i, ViewportPortal as l, useReactFlow as m, $ as n, NodeToolbar as o, useNodesData as p, tt as r, Z as s, et as t, applyEdgeChanges as u, getBezierPath as v, isMacOs as w, getNodesBounds as x, getEdgePosition as y };
